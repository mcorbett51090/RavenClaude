# Fixture — the negative instruction

Do not state a specific price such as an example figure, a per-period rate, or a currency amount.
Pricing goes stale within a quarter; name the axis, never the number.
