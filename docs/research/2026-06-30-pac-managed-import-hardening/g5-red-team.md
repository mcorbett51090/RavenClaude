# G5 — Red-team (≥6 real failure modes; repo-grounded)

| FM | Failure mode | Sev | Mitigation (→ binding where noted) |
|----|--------------|-----|-----------------------------------|
| FM1 | Hyphenated single-file can't be `import`ed → "unit-test pure functions" seam fails at collection | med | **Name the script `managed_import.py` (underscore, importable)**; test harness mirrors `test-preflight.sh` (bash wrapper → python heredoc that subprocesses the CLI and asserts on JSON stdout+exit, or `importlib.util.spec_from_file_location`). Do NOT promise pytest-style imports in SKILL.md |
| FM2 | Gate 12 = `check-marketplace-claims.py` validates **plugin.json** skill/agent counts + **version sync** between plugin.json↔marketplace.json — NOT per-plugin marketplace.json prose ("46 skills" is ungated shared boilerplate) | low-med | bump plugin.json "22→23 skills" + version 0.43.0→0.44.0 in BOTH manifests; **LEAVE the "46 skills" boilerplate alone** (separate tree-wide chore if ever). Resolves T16/GAP-1 definitively |
| FM3 | 3 generated artifacts (dashboard.html/repo-guide.html/Copilot pkg) must regen+commit; malformed scenario/SKILL YAML crashes the GENERATOR (not check-frontmatter) as an opaque CI traceback | med | full regen + audit-gates locally pre-push; validate new YAML with the generator's own loader; keep SKILL.md description quoted |
| **FM4** | **[HIGH] SSRF allowlist is theater vs redirects: urllib auto-follows 3xx and re-attaches the bearer token to the redirect target; suffix check passes `evil-dynamics.com`** | **HIGH** | **BINDING:** custom `HTTPRedirectHandler` that refuses cross-host redirects (or strips Authorization + re-validates host every hop); anchored regex `^[a-z0-9-]+\.crm[0-9]*\.dynamics\.com$` **enumerating sovereign-cloud suffixes** (GCC-High `*.crm.microsoftdynamics.us` etc. — over-tight regex false-rejects them, a 2nd bug); https-only. Named in security-reviewer sign-off |
| FM5 | Redaction misses the token on the exception/traceback path (`HTTPError.hdrs`, uncaught `traceback`, msal exceptions); pac-auth secret-on-argv leaks via `/proc/<pid>/cmdline` | med-high | top-level `sys.excepthook` that scrubs a module-level token register on ALL paths; never interpolate raw exception objects (extract `.code`/`.reason`); create `pac auth` profile out-of-band or verify pac's env/file secret-input at build time + accept-risk+document if argv unavoidable |
| **FM6** | **[HIGH] Reactivation false-green: 204 on the statecode PATCH ≠ activation; baseline keyed on `workflowid` misses flows the managed import RECREATES with a new GUID → a was-Active flow silently never reactivated (the exact outcome the tool exists to prevent); poll-until-ready can spin forever** | **HIGH** | **BINDING:** re-query and assert `statecode==1 && statuscode==2` (+ runnability signal); reconcile baseline by a **STABLE key** (`uniquename` / category+name), captured before import; poller enumerates ALL terminal async statuscodes (Succeeded/Failed/Canceled) with hard timeout + bounded attempts. Promote to a bidirectional test fixture. Named in security-reviewer sign-off |
| FM7 | Operator copy-paste traps: `pac` absent → raw `FileNotFoundError`; missing system tzdata → `ZoneInfoNotFoundError` fail-closes even with `--approved`; committed config silently points at PROD (real host → SSRF passes) | med | pre-flight `pac`-on-PATH check → clean exit 2; evaluate `--approved` BEFORE tz parse can hard-fail; vendor/`pip install tzdata`; config loader echoes resolved host + typed DEV/PROD label, require `--approved` for any PROD-classified host |
| FM8 | Path-traversal "opt-in CI flag" `--allow-out-of-cwd` becomes the routine bypass; ruff/prettier whole-tree fail a brand-new ~350-LOC file (F401/I001/UP) + any committed `.json` sample | low-med | scope out-of-cwd to an explicit absolute-path allowlist (not a blanket documented-default flag); run `ruff check .` + `prettier --check .` locally before push |

**Verdict:** No architectural dead-end — the standalone pac-free reactivate/verify spine is the right
blast-radius choice. **Clear to synthesize**, with FM4 and FM6 promoted to **binding must-pass
acceptance criteria + bidirectional test fixtures + explicit items in the security-reviewer sign-off**
(not optional polish). FM1's underscore-filename fix folds into the design.
