# G0-control scratch test

This is a disposable file backing a scratch PR used only to run the
source-control-coordinator build plan's Gate G0-control(b) positive control
(`subscribe_pr_activity` delivers a wake for a comment on a live PR). The PR
will be closed, unmerged, once the check completes; this file has no lasting
value and the branch will be archived afterward.
