# Overnight work plan — 2026-06-04

_Generated by the main Claude session after the BMA-CSP execution shipped (PR #260) and the background overnight-work analysis returned. Open this in any terminal to read; reply in the main session terminal with the option(s) you want to ship while you sleep._

---

## Current state (end of session)

| Thread | Status |
|---|---|
| **PR #259 — unified-dashboard-shell** | ✅ MERGED to main as `e5675b2`. All 4 buildable phases + 4 CI hotfixes shipped. v0.114.0 of `ravenclaude-core`. Only Phase 3 (manual visual regression DoD in a browser) remains, and it's a self-audit not a gate. |
| **PR #260 — BMA-CSP session lessons** | 🟡 DRAFT. 3 new knowledge files + 3 extensions + 4 priors + v0.24.0 of `power-platform`. CI initially failed on `Validate manifests and hooks` — same family as #259 (pre-existing gate not caused by this PR). Being fixed now. |
| **Partner Success Dashboard spec** | ✅ landed on `main` as `b8b63c7` (docs/research/2026-06-04-partner-success-dashboard-requirements/). Architecture decision **answered:** staged — Phase 1 single-PSM personal cockpit for Matt's wife, Phase 2 multi-tenant only if Phase 1 proves valuable. Logged to memory as `project_partner_success_staged_architecture`. NO `/forge` has run on it yet. |
| **Overnight-work analysis** | ✅ complete. The 3 candidate items below are the corrected version (the agent's analysis was pre-#259 merge so I corrected for shipped state). |

---

## Three candidate overnight items

### 🥇 Option 1 — Mímir Phases 3+4+5 (generator tab + Gate 49 + ship)

**Effort:** M (~90 min, single PR)
**Risk:** Low — fully specified plan, no Matt input needed
**Why high value:** the `/__mimir` server endpoint shipped in #255 but the **UI tab doesn't exist yet**. Mímir is invisible until Phase 3 lands. Closes the entire Mímir arc end-to-end (P3 generator tab + client render → P4 render-gate fixture as Gate 49 → P5 ship: version bump + CLAUDE.md milestone + prettier + regen).

**Plan:** `docs/plans/2026-06-03-mimir-session-tab/`

**First action when greenlit:**
```
git checkout main && git pull --ff-only
git checkout -b feat/rc-mimir-p3-tab
# Read scripts/generate-dashboards.py around the Heimdall tab pattern
# Read plugins/ravenclaude-core/scripts/serve-dashboards.py:_read_mimir for the JSON contract
```

---

### 🥈 Option 2 — Cross-plugin scenario-retrieval link rot fix

**Effort:** S (~20 min, single PR)
**Risk:** Very low — pure mechanical link rename
**Why high value:** memory `project_followups_skill_format_cleanup` has tracked this since **2026-05-26**. ~11 broken `../../ravenclaude-core/skills/scenario-retrieval.md` links across 5 plugin CLAUDE.mds — wrong path depth and stale `.md` form pointing at a skill that's now in `SKILL.md` directory format. Marketplace catalog surfaces this rot in `repo-guide.html`.

**First action when greenlit:**
```
grep -rn '\.\./\.\./ravenclaude-core/skills/scenario-retrieval' plugins/*/CLAUDE.md
# Repoint each to: ../ravenclaude-core/skills/scenario-retrieval/SKILL.md
# Bump each touched plugin's PATCH version + regen
```

---

### 🥉 Option 3 — data-viz-designer v0.1.0 (BIG BET)

**Effort:** L (>90 min, likely multi-PR series)
**Risk:** Medium — larger blast radius; recommend PR-and-confirm rather than direct-merge
**Why high value:** the LARGEST single piece of parked, fully-planned, two-panel-reviewed work in the repo. Closes "Power BI Slop" with verified canon. Memory `project_data_viz_designer_in_flight` says the strategic + build plan v1 are on origin/main (d3ef360) "awaiting Ultraplan execution" but no Ultraplan PR has landed in days.

**Plan:** `docs/research/2026-06-02-data-viz-agent/` (strategic plan v2 + build plan v1)

**First action when greenlit:**
```
git checkout main && git pull --ff-only
git checkout -b feat/rc-data-viz-designer-v0.1.0
# Read docs/research/2026-06-02-data-viz-agent/build-plan.md Phase 0 + Phase 1
# Note: Panel 2 caught that pbir-enhanced-reference.md lives in plugins/power-platform/, not ravenclaude-core/
# Note: audit-gates 49-52 shifted to 49-51 per the webfetch-hardening offset
```

---

## Default order if you pick "you pick — autopilot until empty"

1. **First (fastest win):** scenario-retrieval link rot fix → PR opened, CI green, ready for your merge in the morning
2. **Second (closes an arc):** Mímir P3+4+5 → PR opened, CI green, ready
3. **Third (big bet — PR-and-confirm, NOT direct-merge):** data-viz-designer v0.1.0 → draft PR opened, you decide whether to merge after reviewing in the morning

Per memory `feedback_thing_autoapprover_and_dev_exempt`, the main session has natural autopilot authority for #1 and #2 (small + medium, well-scoped, no Matt preference call). For #3 I default to **draft PR + wait for your morning review** rather than direct-merge.

---

## Items that should WAIT for you (do NOT autopilot)

- **PR #256 (EdTech CS dashboard scaffolding)** — explicitly waits on "SOPs from PSM" to layer into the plan; your wife's input drives the next move. Could pair with Phase 1 of the Partner Success Dashboard architecture (now resolved as single-PSM-first per `project_partner_success_staged_architecture`).
- **PR #210 (ravenpower aesthetic)** — open 2 days; carries three "Decisions deferred to you" (constellation motion, color, raven sigil mark). Genuine preference calls.
- **Agent-dispatch-evaluator Phases 3–6** — Phase 3 hook needs an empirical "does the deny actually prevent dispatch?" check; Phase 6 is a 2-week-soak gated release. Don't autopilot.
- **competitor-analysis** + **research-dashboard plugins** — memory says "do NOT design proactively; ship when a real engagement need surfaces."
- **A `/forge` on the Partner Success Dashboard spec** — the architecture answer ("Phase 1 single-PSM, Phase 2 only if P1 succeeds") is now logged but a real FORGE pipeline against the spec hasn't run. Worth running when you're awake and can answer panel-surface questions.

---

## How to direct from another terminal

You don't need to. Just reply in the main session terminal:

- `option 1` / `option 2` / `option 3` — pick one
- `1 and 2` — pick two
- `you pick` — autopilot the default order above
- `wait — let's talk` — hold all overnight work

---

_File: `/workspaces/RavenClaude/docs/plans/2026-06-04-overnight-work-plan.md` — open with `cat`, `less`, `code`, or any editor. Lives in the repo so it survives a Codespace restart._
