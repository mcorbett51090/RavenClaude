# Markdown: skipped from prettier formatting/checks. Rewriting prose with
# proseWrap or reflowing list indentation produces a 160+ file diff that's
# pure noise and would conflict with the human voice maintained across
# agent definitions, skills, and rules. Markdown style is reviewed by humans
# per AGENTS.md "Code style" rules ("short, scannable, action-oriented"),
# not by prettier.
*.md
*.mdx

# This workflow file has a multi-line JS template literal at column 0 inside
# a YAML `script: |` block. PyYAML rejects the leading `**Trigger**` as an
# alias reference. GitHub Actions' Ruby YAML parser accepts it (the workflow
# runs in prod), but prettier and yamllint both choke. Excluding here until
# the template literal is refactored to use array .join('\n') or proper
# indentation that survives both parsers.
.github/workflows/researcher-reminder.yml

# Generated synthetic fixture — produced by reference-build/data/synthesize.py
# (seeded, byte-deterministic). It's a reference data sample, not source; Python's
# json.dump emit (one element per line for arrays) is canonical for it, and prettier
# would only churn short-array wrapping. Regeneration stays a clean `python3
# synthesize.py`. Same rationale as the generated artifacts above.
docs/plans/2026-06-04-partner-success-command-center/reference-build/data/data.json

# Plan artifact HTML — a self-contained build-plan page whose inline CSS/JS
# prettier's HTML printer rewrites in ways that drift between prettier versions
# (it was already failing the whole-tree gate in main, blocking every PR). Same
# rationale as the generated dashboard/index/report HTML excluded below.
docs/plans/2026-06-04-partner-success-command-center/build-plan.html

# Bundled third-party content keeps its upstream formatting.
plugins/power-platform/skills/record-screen/extension/
plugins/power-platform/skills/record-screen/scripts/
scripts/vendor/


# Generated artifacts — produced by scripts/generate-dashboards.py from each
# plugin's dashboard-schema.json. Same rationale as the other generated artifacts.
plugins/*/dashboard.html

# Generated artifact — the redesigned landing dashboard produced by
# scripts/generate-index-dashboard.py from the marketplace + plugin manifests
# + the comfort-posture schema. Re-running the generator produces canonical
# output guarded by its own --check freshness gate; prettier's HTML printer
# would rewrite the inline CSS / JS in ways that don't match the emit style.
# Same rationale as the other generated artifacts.
index.html

# Generated artifact — the "Problems & Resolutions" view produced by
# scripts/generate-feedback-report.py from the scenario corpus
# (plugins/*/scenarios/*.md). Byte-deterministic, guarded by its own --check
# freshness gate; prettier's HTML printer would rewrite the inline CSS / JS in
# ways that don't match the generator's emit style. Same rationale as index.html.
feedback-report.html

# Generated artifact — the self-contained Power-BI/Tableau-style plugin report
# produced by scripts/generate-bi-report.py from plugins/<plugin>/bi-report/data.json.
# Byte-deterministic, guarded by its own --check freshness gate; prettier's HTML
# printer would rewrite the inline SVG / CSS / JS in ways that don't match the
# generator's emit style. Same rationale as the generated dashboards above.
plugins/*/report.html

# Generated artifacts — the Learn-tab concept registry (scripts/concepts.py) and
# the pre-rendered, theme-normalized concept diagrams + render manifest
# (scripts/render-concepts.py). Both emit canonical, byte-deterministic output
# guarded by their own --check freshness gates; prettier would only churn them.
plugins/*/concepts.json
plugins/*/knowledge/concepts/visuals/

# Generated artifacts — pre-rendered, theme-normalized decision-tree diagrams +
# render manifest (scripts/render-trees.py), guarded by their own --check
# freshness gate. Byte-deterministic; prettier has no SVG parser and would only
# churn the manifest JSON. Same rationale as the concept visuals above.
plugins/ravenclaude-core/knowledge/tree-visuals/

# Local-only session-buffer storage for the remember system (now.md, recent.md,
# today-*.md, tmp/last-*.{ts,json}). The whole `.remember/` dir is git-ignored
# and never reaches CI — but a local `prettier --check .` scans the working tree
# regardless, so the scratch files would otherwise trip the gate locally even
# though the prod-CI run is clean. Same rationale as the bundled-vendor and
# generated-artifact exclusions above.
.remember/

# Local-only run substrate: Sága/tribunal decision JSON, FORGE gate artifacts,
# posture-event audit lines. Git-ignored and never reaches CI, but — same as
# `.remember/` above — a local `prettier --check .` scans the working tree
# regardless, and scratch `.json` files under runs/ ARE prettier-parsed, so a
# torn or differently-printed run artifact would trip the gate locally on a
# clean PR (FORGE red-team finding E6, 2026-06-04).
.ravenclaude/runs/
.ravenclaude/posture-events.jsonl

# Brand logo assets (raven-logo.svg / .png). Hand-exported SVGs don't survive
# prettier's printer cleanly, and they're not ours to reformat. README.md here
# is already covered by the *.md rule above.
plugins/*/dashboard-assets/brand/

# Generated artifact — a deterministic close-package produced by
# plugins/finance/scripts/controller_cycle.py from synthetic fixtures.
# Same rationale as the other generated HTML artifacts above.
plugins/finance/skills/produce-gaap-statements/examples/sample-close-package.html

# Self-contained controller-autopilot pitch (inline CSS/JS); prettier would rewrite it.
plugins/finance/docs/controller-autopilot-pitch.html

# Generated artifact — deterministic per-entity dashboard from entity_dashboard.py.
plugins/finance/skills/per-entity-dashboard/examples/sample-entity-dashboard.html

# Byte-exact input fixture — the report-regeneration seed-defect injectors and the
# fidelity/QA harness depend on this file's EXACT bytes; prettier reflowing its
# HTML would break the tests. Treated as reference data, not source.
tests/fixtures/report-regeneration/sample-report.html

# Byte-exact CLEAN-CONTROL fixtures — clean-output.html is a real rebind_html.rebind()
# product asserted on byte-for-byte by the fidelity harness's clean-control + V2
# frozen-complement legs (test_harness.py / test_harness_office.py); reflowing it would
# corrupt the control. The sibling manifest/new-data JSON are its byte-exact inputs.
plugins/report-regeneration/skills/report-fidelity-harness/fixtures/
