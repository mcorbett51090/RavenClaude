---
description: "Scope the project to a vertical slice that proves the core loop is fun before scaling content, to de-risk the build. Reach for this at greenlight."
argument-hint: "[the situation, e.g. the metric/segment in question]"
---

# Scope to a vertical slice

You are running `/game-development:scope-to-a-vertical-slice` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Define the slice — The minimum build that proves the core loop end-to-end (§3 #1).
2. Sequence the risk — Put the riskiest unknowns (fun, tech) first (§3 #3).
3. Set the fun bar — Define what 'the loop is fun' means to pass the gate.
4. Gate the full build — Only greenlight content scaling after the slice passes.

## Output
A vertical-slice scope, a risk sequence, a fun bar, and a build gate. See [`../skills/scope-to-vertical-slice/SKILL.md`](../skills/scope-to-vertical-slice/SKILL.md).

## Guardrails
- Apply the §3 house opinions before any method.
- No client PII; cite or mark every external figure.
