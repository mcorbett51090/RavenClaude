---
description: "Build an executable data-subject-request pipeline (access/erasure/portability) that locates and actions data across systems."
argument-hint: "[systems holding personal data]"
---

You are running `/data-governance-privacy:build-dsr-pipeline`. Use `privacy-compliance-engineer` + the `privacy-mechanics` skill.

## Steps
1. Use the catalog/lineage to locate ALL of a subject's data (flag gaps).
2. Build verify -> locate -> action (access/erasure/portability) -> verify -> respond.
3. Handle lawful retention exceptions (route legal interpretation out).
4. Emit (from `templates/dsr-runbook.md`) + Structured Output block.
