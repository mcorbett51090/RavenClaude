---
description: "Design and apply a usable classification scheme and produce the data inventory/record of processing."
argument-hint: "[datasets + sensitivity]"
---

You are running `/data-governance-privacy:classify-data`. Use `data-governance-architect` / `privacy-compliance-engineer`.

## Steps
1. Traverse the classification tree per dataset (PII? sensitive? harm-if-disclosed?).
2. Apply the scheme + PII flag; map to controls.
3. Produce the inventory/RoPA record (owner/basis/retention/systems).
4. Emit (from `templates/classification-scheme.md` + `data-inventory.md`) + Structured Output block.
