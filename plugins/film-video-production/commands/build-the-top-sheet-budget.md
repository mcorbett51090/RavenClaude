---
description: "Build the budget bottom-up to a top-sheet with a risk-sized contingency, so the number is defensible. Reach for this on any budget question."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Build the top-sheet budget

You are running `/film-video-production:build-the-top-sheet-budget` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Build the categories — Above-the-line, below-the-line, post, and contingency (§3 #1).
2. Build below-the-line bottom-up — Crew/gear/locations as rate × time × risk (§3 #7).
3. Size the contingency — Set contingency to the project's risk (§3 #4).
4. Reconcile to the spec — Check the budget delivers the spec (§3 #6).

## Output
A top-sheet budget with a bottom-up build, sized contingency, and spec reconciliation. See [`../skills/build-the-top-sheet/SKILL.md`](../skills/build-the-top-sheet/SKILL.md). Traverse the matching tree in [`../knowledge/production-decision-trees.md`](../knowledge/production-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
