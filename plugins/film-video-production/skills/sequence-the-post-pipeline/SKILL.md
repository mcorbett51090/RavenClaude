---
name: sequence-the-post-pipeline
description: "Sequence post as a dependency chain keyed off picture lock, so the delivery date rests on the critical path. Reach for this on a post plan."
---

# Skill: Sequence the post pipeline

Starting color before the edit locks is rework.

## Step 1 — Map the dependencies
Editorial → VFX → color → sound → conform → deliver (§3 #3).

## Step 2 — Protect picture lock
Set the lock as the gate finishing waits on (§3 #5).

## Step 3 — Find the critical path
Identify what sets the delivery date.

## Step 4 — Schedule to deliver
Sequence finishing to the delivery spec and date (§3 #6).

## Output
A post-pipeline dependency map, a protected lock, the critical path, and a delivery schedule.
