# Community Health Scorecard — <community>

> Health is funnel conversion, not member count. Size each stage, track the conversions, name the
> stuck stage and its intervention.

## 1. The funnel

| Stage | Population | Conversion to next | Trend |
|---|---|---|---|
| Lurker | `<n>` | `<%>` | `<↑/→/↓>` |
| Asker | `<n>` | `<%>` | |
| Answerer | `<n>` | `<%>` | |
| Contributor | `<n>` | `<%>` | |
| Champion | `<n>` | — | |

**Stuck stage:** `<transition>` — **intervention:** `<recognition / good-first-issues / SLA / ambassador tier>`.

## 2. Response coverage

| Metric | Current | Target |
|---|---|---|
| Answer rate (answered ÷ asked) | `<%>` | `<%>` |
| Time-to-first-response (median / p90) | `<h>` / `<h>` | `<h>` / `<h>` |
| Self-answer ratio (community ÷ total answers) | `<%>` | `<%>` |

## 3. Contribution & champions

| Metric | Current | Target |
|---|---|---|
| Contributor conversion (contributors ÷ active) | `<%>` | `<%>` |
| Active ambassadors | `<n>` | `<n>` |
| Ambassador value-exchange healthy? (give ↔ get happening) | `<y/n>` | yes |

## 4. Docs-gap signal

Top recurring questions this period (each = a docs/quickstart edit to route to DX):

1. `<question>` → `<docs fix>`
2. `<question>` → `<docs fix>`
3. `<question>` → `<docs fix>`
