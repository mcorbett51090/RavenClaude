# Quickstart: <get a developer to first hello-world on PRODUCT>

> The golden path — **one happy path, done flawlessly**. Every code block must run
> unmodified (copy-paste-run, no unresolved `<placeholder>`), and the doc must name
> a measurable success criterion. Goal: the smallest **TTFHW** (time-to-first-hello-world)
> possible. Exhaustive options and edge cases belong in the reference docs, not here.

**Audience:** <which developer> · **Estimated TTFHW:** <e.g. ~90 seconds on a clean machine> · **You'll build:** <the first real result>

## Prerequisites (keep this list ruthlessly short)

- <runtime / version — only what's truly required to reach first hello-world>
- <NO signup/API-key step here if it can be deferred past first hello-world — use a sandbox/demo key>

## 1. Install

```bash
# every block is language-tagged and runs unmodified
<install command>
```

## 2. First hello-world (the activation event)

```bash
# the smallest real, runnable result — a returned response / rendered output / passing call
<minimal runnable example, no placeholders the reader can't resolve from this page>
```

## 3. Success check — "you should see …"

> The developer must be able to tell, unambiguously, that it worked.

```
<expected output, verbatim — e.g. {"status": "ok"}>
```

If you don't see this: <the one most common failure + its one-line fix>.

## 4. Next step (one link, no menu)

- <the single best next thing — a slightly richer sample, or the reference for depth → technical-writing-docs>

---

## Author checklist (delete before publishing)

- [ ] Every code block has a runnable language tag (no bare ``` fences).
- [ ] Ran the whole path on a clean machine — it works unmodified.
- [ ] First hello-world is the *smallest real* result; nothing before it is overhead.
- [ ] Signup / API-key deferred past first hello-world where possible (sandbox key).
- [ ] An explicit "you should see …" success check is present.
- [ ] TTFHW measured (or estimated) and stated at the top.
- [ ] Every friction point hit while writing this was logged to PM/eng.
