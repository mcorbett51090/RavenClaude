# Conference Talk Brief — <conference / talk title>

> A talk that converts, not one that just gets applause. Fill the angle, the outline, the activation
> path, and the measurement.

## 1. CFP angle (problem-first)

- Conference + audience: `<who's in the room>`
- The specific developer problem: `<problem>`
- The concrete takeaway (what they can do Monday): `<takeaway>`
- Title a developer would search for: `<title>`
- Anchor (a real number or hard-won lesson): `<e.g. 800ms → 80ms>`

## 2. Funnel stage & goal

- Stage: `<awareness / evaluation>`
- Goal metric: `<quickstart starts / sign-ups attributable to the talk>`

## 3. Outline (job-to-be-done arc)

1. The problem the audience feels — `<hook>`
2. The journey (including what didn't work) — `<approaches>`
3. The technique that worked — `<core content>`
4. Runnable proof — `<demo / repo>`
5. The next step — `<activation path>`

## 4. Activation path

- During/after the talk, the audience goes to: `<short URL → repo / quickstart / sandbox>`
- It is specific and runnable (not "check our docs").

## 5. Measurement

| Metric | Plan |
|---|---|
| Post-talk repo visits | `<tracked link>` |
| Quickstart starts attributable | `<UTM / referral>` |
| Activations attributable | `<content_roi input>` |

> Applause and headcount are inputs; activations are the outcome (constitution rule 2).
