# Quickstart: <do the thing> with <product>

> **In about <N> minutes**, you'll <concrete outcome> and see <success signal>.
> _Time-to-first-success target: under <N> minutes._

## Before you start

You need:

- [ ] <only the prerequisites that truly can't be removed — each one is a cliff>
- [ ] <prefer: a sandbox key / one-line installer / hosted try-it that removes a cliff>

## 1. <First step>

```<lang>
<copy-paste block that runs as written, in order, with no hidden steps>
```

## 2. <Second step>

```<lang>
<copy-paste block>
```

## 3. See it work

```<lang>
<the call that produces the payoff>
```

You should see:

```
<the concrete success signal — what they'll actually see>
```

✅ That's first success. You just <restated outcome>.

## Next

- <advanced option / auth / production setup / other languages — everything that was NOT on the happy path>

---

<!--
AUTHORING CHECKLIST (delete before publishing):
- [ ] TTFS target declared at the top and walked with a timer
- [ ] One happy path only — no branches before first success
- [ ] Every prerequisite is unavoidable; the rest are cut/deferred/sandboxed
- [ ] Every code block runs copy-paste, in order, with no hidden steps
- [ ] The success signal is shown, not just described
- [ ] A CI job extracts these snippets and runs them against the real SDK (docs drift == red build)
-->
