# Developer Journey Map — <product>

> Map the developer's path from first contact to champion. For each stage, capture the developer's
> question, the surface they hit, the friction, the metric, and the DevRel action.

| Stage | Developer's question | Surface they hit | Top friction | Metric | DevRel action |
|---|---|---|---|---|---|
| Awareness | "Does this solve my problem?" | `<search / talk / post>` | `<friction>` | reach → quickstart starts | `<content / talk>` |
| Evaluation | "Will this work for my case?" | `<tutorial / sample>` | `<friction>` | sign-ups | `<sample app / ref arch>` |
| Sign-up | "Let me try it" | `<signup / pricing>` | `<friction>` | sign-up rate | `<frictionless trial>` |
| Activation | "How do I get it working?" | `<quickstart / SDK>` | `<friction — usually hidden steps>` | **time-to-first-value, activation rate** | `<onboarding fix>` |
| Adoption | "How do I run this in prod?" | `<deep docs / support>` | `<friction>` | production adoption | `<depth docs / ref arch>` |
| Expansion | "I'd recommend / contribute" | `<community / GitHub>` | `<friction>` | contributor conversion | `<ambassador / good-first-issue>` |

## Notes

- The **activation row** is almost always the highest-ROI place to invest — instrument it first.
- Every stage's "DevRel action" should end with an activation path (a next concrete step).
- Recurring friction at any stage is product/docs feedback — route it via the feedback loop.
