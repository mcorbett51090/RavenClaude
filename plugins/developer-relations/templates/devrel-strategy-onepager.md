# DevRel Strategy One-Pager — <company / product>

> Fill each section. Keep it to one page. Lead with outcomes, not activities.

## 1. The bottleneck

Where do developers fall out of the journey today?

- Awareness → sign-up: `<rate or "unknown — instrument">`
- Sign-up → activation (first success): `<rate>`
- Activation → production adoption: `<rate>`
- **Primary bottleneck this program targets:** `<stage>`

## 2. Mandate

This DevRel program owns:

- `<owned area 1>`
- `<owned area 2>`

It explicitly does **not** own (non-goals):

- `<non-goal 1>`
- `<non-goal 2>`

## 3. Scorecard (outcomes first)

| Outcome metric | Definition | Source | Target | Decision it informs |
|---|---|---|---|---|
| Activation rate | first-success ÷ sign-ups | `<source>` | `<target>` | DX investment |
| Time-to-first-value | median sign-up → first success | `<source>` | `<target>` | Onboarding fixes |
| Production adoption | `<def>` | `<source>` | `<target>` | Depth-docs / support |

Vanity inputs (only ever reported paired with an outcome above): `<stars / followers / impressions / attendees>`.

## 4. Team sequence

| Hire order | Role | Bottleneck it addresses |
|---|---|---|
| 1 | `<advocate / dx-engineer / community>` | `<stage>` |
| 2 | `<role>` | `<stage>` |

## 5. Exec narrative (3 sentences)

> `<Activity>` drives `<leading metric>`, which moves `<activation/adoption outcome>`, which
> influences `<pipeline/revenue outcome>`. We will report `<outcome metrics>` monthly. We are
> stopping reporting `<vanity metrics>` as headlines.

## 6. Feedback loop

- Capture format: `<what's logged, how tagged>`
- Triage cadence: `<weekly / biweekly>`
- Routing into product: `<path>`
