# NOI Bridge — <property / period>

> EGI-to-NOI bridge (§3 #4). Math from `../scripts/property_management_calc.py` `noi`.

| Line | Amount | % of GPR | Baseline (prior) | Note |
|---|---|---|---|---|
|  |  |  |  |  |

**Value:** NOI ÷ cap rate = <…> (cap-rate source + date, §3 #8).

**Sources:** <URL — retrieval date> for every external benchmark.
