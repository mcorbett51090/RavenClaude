---
description: "Read the operating ratio (expenses ÷ revenue) as the survival headline and decompose it before acting. Reach for this on any profitability question."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Manage the operating ratio

You are running `/fleet-logistics:manage-the-operating-ratio` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Compute the OR — Operating expenses ÷ operating revenue; >100 is a loss (§3 #2).
2. Decompose it — Split into the cost buckets driving the ratio.
3. Benchmark — Compare to the negative-margin 2024 market context, dated.
4. Target the driver — Prescribe to the largest controllable bucket.

## Output
An operating ratio, a cost-bucket decomposition, a benchmark, and the targeted driver. See [`../skills/manage-the-operating-ratio/SKILL.md`](../skills/manage-the-operating-ratio/SKILL.md). Traverse the matching tree in [`../knowledge/fleet-decision-trees.md`](../knowledge/fleet-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
