# Treat Cloud Run as the default; make GKE earn it

For stateless containers and HTTP/event services, Cloud Run gives scale-to-zero and minimal operations. Reach for GKE only when you genuinely need Kubernetes features or multi-cloud portability — and use Autopilot to cut the operational burden when you do. Standing up a cluster for a workload Cloud Run would run is buying ops you didn't need.
