# Tier and escalation design routes complexity — don't make every agent omni.

**Status:** Pattern. **Constitution:** §3 #6, §4.

## Use when
Any customer support & cx operations deliverable where this question is in play — read, applied, and cited whole.

## The rule
Routing simple contacts to a broad front line and complex ones to specialist tiers beats forcing every agent to handle everything; an all-omni model raises handle time, training cost, and burnout while lowering quality.

## Why it matters
This is house opinion §3 #6, distilled into a citable rule. Practitioners act on these deliverables, so a framing error here is not academic — it sends real operating decisions the wrong way. The rule is cheap to apply and expensive to skip.

## How to apply
- Apply this **before** reaching for a method — it sets the framing, not the conclusion.
- Make every number in the deliverable carry a definition, a window, and a baseline (§3 #1).
- Cite a source + date for any external benchmark, or mark it `[unverified — training knowledge]` (§3 #8).
- When this rule and another both apply, route to [`support-ops-lead`](../agents/support-ops-lead.md) to sequence them — overlapping signals usually mean two drivers at once.
- Keep customer PII out of the deliverable; route professional/legal determinations to the qualified authority (§2).
- Close with a recommendation that has an owner, a date, and an expected metric movement.

## The anti-pattern this prevents
The §4 failure mode: acting as if "tier and escalation design routes complexity — don't make every agent omni." weren't true — the most common way an analysis quietly misleads the practitioner who acts on it. The plugin's advisory hook flags a deliverable that reads as if this rule were ignored.

## See also
- [`../CLAUDE.md`](../CLAUDE.md) §3 #6 — the house opinion this rule encodes.
- [`../knowledge/customer-support-cx-operations-decision-trees.md`](../knowledge/customer-support-cx-operations-decision-trees.md) — the decision trees that route to it.
