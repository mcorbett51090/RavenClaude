# Determinism or it isn't a test

A test that depends on uncontrolled time, randomness, network, or fixed sleeps isn't a test — it's a coin flip. Wait on conditions, fake the clock, seed the RNG, and stub the boundary so the same input always yields the same verdict.
