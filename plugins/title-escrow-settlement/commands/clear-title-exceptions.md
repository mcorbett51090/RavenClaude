---
description: "Work a title exception or requirement to a decision — cure, insure over, or except — and structure the commitment's Schedule B-I requirements and B-II exceptions (underwriter specifics verify-at-use)."
argument-hint: "[the exception/defect + search findings + underwriter, if known]"
---

You are running `/title-escrow-settlement:clear-title-exceptions`. Use `title-examiner` + the `commitment-and-curative` skill.

> Advisory, not legal or title-underwriting advice. Every underwriter guideline and curative sufficiency standard is `[verify-at-use]`. The binding insurability call is the underwriter's. No PII — describe matters generally, never a party's records.

## Steps
1. Restate the defect/encumbrance and the chain-of-title context from the search (see the `title-search-and-examination` skill). Confirm nothing is being assumed from a prior policy.
2. Traverse the **clear a title exception (cure vs insure-over vs except)** tree in `knowledge/title-escrow-decision-trees.md` for each matter.
3. For each: assign it to a Schedule B-I **requirement** or a Schedule B-II **exception**, and name the curative action (payoff+release, corrective deed, affidavit, quiet-title) or the underwriter approval needed to insure over — each guideline flagged `[verify-at-use]`.
4. Escalate any off-guideline or above-authority matter to the underwriter with your read; never insure past delegated authority.
5. Emit using `templates/title-commitment-worksheet.md` + the Structured Output block. Confirm the plan drives open requirements to zero **before** closing.
