# Define and rehearse rollback before shipping

Decide the abort condition and the rollback action *before* the deploy, and test the rollback path. The first time you execute a rollback should never be live during an incident.
