# RAG Eval Sheet — <corpus / change>

> Offline eval before/after every change (§3 #3). Math from `../scripts/ai_rag_engineering_calc.py` `retrieval-eval`.

| Query set | Recall@k | Precision@k | Faithfulness | Answer-relevance | Δ vs baseline |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**No eval, no ship.** Retrieval metrics gate generation metrics — fix retrieval first (§3 #1 #3).

**Sources:** <URL — retrieval date> for every external benchmark.
