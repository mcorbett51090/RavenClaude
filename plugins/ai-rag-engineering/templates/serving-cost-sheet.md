# Serving Cost Sheet — <endpoint / period>

> Token cost per request + context economics (§3 #4 #5). Math from `../scripts/ai_rag_engineering_calc.py` `token-cost`/`chunk-budget`.

| Model | Input tokens | Output tokens | Cost/req | Requests/mo | Monthly cost |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**More context is not better.** Prices/limits carry a source + date — verify the live page (§3 #5 #8).

**Sources:** <URL — retrieval date> for every external benchmark.
