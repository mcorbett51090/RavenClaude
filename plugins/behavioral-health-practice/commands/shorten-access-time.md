---
description: "Read intake-to-first-appointment access time as the conversion lever and find where the delay loses referrals. Reach for this on an access or conversion question."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Shorten access time

You are running `/behavioral-health-practice:shorten-access-time` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Measure access time — Days from first contact to first kept appointment, by referral source.
2. Map the conversion drop — Where in the intake-to-first-appointment path referrals fall out (§3 #2).
3. Localize the delay — Scheduling capacity, intake paperwork, or no-show at first visit (§3 #1 #2).
4. Shorten before spending — Fix the access bottleneck before adding marketing (§3 #2).

## Output
An access-time read by referral source naming the delay that loses conversion. Traverse Tree 2 in the decision-trees file. See [`../skills/shorten-access-time/SKILL.md`](../skills/shorten-access-time/SKILL.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No patient PHI in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
