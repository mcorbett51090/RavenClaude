# Referral Source Scorecard — <clinic>

> Manage referrals like a pipeline. Track volume and trend by source, flag cold sources, and measure
> conversion to evaluation.

## 1. Source volume & trend

| Referral source | This period | Prior period | Trend | Status |
|---|---|---|---|---|
| `<physician / group / self-referral>` | `<n>` | `<n>` | `<↑/→/↓>` | `<active / cooling / cold>` |

**Cold/cooling sources to act on:** `<list>` — outreach owner: `<who>`.

## 2. Referral → evaluation conversion

| Stage | Count | Conversion |
|---|---|---|
| Referral received | `<n>` | — |
| Scheduled | `<n>` | `<%>` |
| Arrived | `<n>` | `<%>` |
| Evaluated (POC started) | `<n>` | `<%>` |

**Leak:** `<stage → stage>` — **fix:** `<speed-to-contact / scheduling friction / benefit clarity>`.

## 3. Speed-to-contact

| Metric | Current | Target |
|---|---|---|
| Median time referral → first contact | `<h/d>` | `<within hours>` |

## 4. Loop closure

- Outcome/status updates sent back to referrers? `<y/n>`
- Sources receiving outcome data: `<list>`

> Direct-access scope and payer rules vary by state/payer — flag for verification.
