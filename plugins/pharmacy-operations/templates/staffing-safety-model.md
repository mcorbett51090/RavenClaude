# Staffing & Safety Model — <pharmacy / period>

> Hours to script volume PLUS clinical-service time, verification safety held as the constraint (§3 #1 #5). Math from `../scripts/pharmacy_operations_calc.py` `throughput-staffing`.

| Hour type | Daily volume | Per-hour rate | Hours needed | Hours current | Verification-safety gap |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**Safety note:** verification capacity is non-negotiable — speed never trades off the pharmacist check (§3 #1).

**Sources:** <URL — retrieval date> for every external benchmark.
