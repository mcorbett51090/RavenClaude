# Don't peek; pre-register the analysis

Stopping a test the moment it crosses significance dramatically inflates the false-positive rate, because you get many chances to cross the line by chance. Pre-register the primary metric, the minimum detectable effect, and the duration with applied-statistics, or use a sequential testing method explicitly designed to allow continuous monitoring. Peeking-to-stop manufactures wins that don't replicate.
