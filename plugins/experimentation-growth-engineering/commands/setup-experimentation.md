---
description: "Build trustworthy A/B-test plumbing: assignment, exposure logging, SRM checks, guardrails, pre-registration with applied-statistics."
argument-hint: "[experimentation needs + scale]"
---

You are running `/experimentation-growth-engineering:setup-experimentation`. Use `experimentation-architect` + the `ab-test-plumbing` skill.

## Steps
1. Design deterministic sticky assignment + exposure logging.
2. Add SRM/trustworthiness checks; define guardrails.
3. Pre-register metric/MDE/duration WITH applied-statistics.
4. Emit (from `templates/experiment-design.md`) + Structured Output block — significance left to applied-statistics.
