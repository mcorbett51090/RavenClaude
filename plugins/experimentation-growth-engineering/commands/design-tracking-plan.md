---
description: "Design a tracking plan with a consistent typed event schema, identity stitching, and CDP routing."
argument-hint: "[product + analytics needs]"
---

You are running `/experimentation-growth-engineering:design-tracking-plan`. Use `product-analytics-instrumentation-engineer` + the `event-instrumentation` skill.

## Steps
1. Define events + typed properties + naming convention; version it.
2. Design anonymous->known identity stitching.
3. Route via a CDP; warehouse copy -> data-platform.
4. Add event-quality validation as code.
5. Emit (from `templates/tracking-plan.md`) + Structured Output block.
