{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "description": "Plugin-distributed hooks for web-design. Auto-merged with the consumer's session hooks when the plugin is installed.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-web-anti-patterns.sh \"$CLAUDE_TOOL_FILE_PATH\"",
            "comment": "Flag web anti-patterns: oversized raster images, <img> missing alt, hardcoded hex colors outside token files, HTML pages missing <title>/<meta description>, accidental noindex. Advisory by default; flip exit 0 to exit 1 to enforce."
          }
        ]
      }
    ]
  }
}
