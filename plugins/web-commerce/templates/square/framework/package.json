{
  "name": "square-framework-checkout-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Square Web Payments SDK + POS reconciliation framework tier -- scaffolded by the web-commerce plugin.",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
