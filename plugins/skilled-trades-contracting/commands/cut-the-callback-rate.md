---
description: "Read first-time-fix and quantify the callback labor cost, then fix truck stocking and diagnosis, since a callback is a free truck roll. Reach for this when callbacks are high."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Cut the callback rate

You are running `/skilled-trades-contracting:cut-the-callback-rate` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Measure first-time-fix — Jobs completed in one visit ÷ total (§3 #4).
2. Cost the callbacks — The extra truck-roll labor and schedule cost.
3. Find the cause — Diagnosis, parts-on-truck, or workmanship.
4. Fix and re-measure — Stock the truck and tighten diagnosis (§3 #6).

## Output
A first-time-fix read, the callback cost, the cause, and the fix. See [`../skills/cut-callbacks/SKILL.md`](../skills/cut-callbacks/SKILL.md). Traverse the matching tree in [`../knowledge/trades-decision-trees.md`](../knowledge/trades-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
