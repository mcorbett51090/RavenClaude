# Salesforce — best-practice docs

Named, consumer-facing rules for building on the Salesforce platform with this plugin's agents. Each file is **one rule** — read, applied, and cited as a whole. They are the consumer-facing distillation of the plugin's [15 house opinions](../CLAUDE.md) and the [`knowledge/`](../knowledge/) bank that backs them.

For the full reference material (limits tables, decision trees, sources) see [`../knowledge/`](../knowledge/); for the agents that enforce these rules see [`../agents/`](../agents/).

---

## Index

_50 rules. Each file is one named, citable rule; read and apply it whole._

| Doc | Status | Use when |
|---|---|---|
| [`agentforce-action-grounding-and-guardrails.md`](./agentforce-action-grounding-and-guardrails.md) | Absolute rule — an ungrounded answer or an unconfirmed irreversible action is a defect, not a UX choice. | An Agentforce agent that answers from the model's parametric knowledge instead of from grounded Salesforce data will confidently invent — wrong order totals, ma… |
| [`agentforce-earns-its-non-determinism.md`](./agentforce-earns-its-non-determinism.md) | Absolute rule — never put an agent where a deterministic automation belongs, and never ship one ungated by the Trust Layer. | Agentforce is driven by the Atlas reasoning engine and is **non-deterministic** — the same input can yield a different plan. |
| [`agentforce-scope-topics-tightly-with-explicit-instructions.md`](./agentforce-scope-topics-tightly-with-explicit-instructions.md) | Pattern — strong default; a sprawling, vaguely-instructed topic is a reliability and safety liability. | The Atlas reasoning engine routes a user's request to a **topic** and then plans within that topic's instructions and action set. |
| [`agentforce-test-and-evaluate-before-and-after-ship.md`](./agentforce-test-and-evaluate-before-and-after-ship.md) | Pattern — strong default; shipping a non-deterministic agent on a single happy-path demo is negligence. | A non-deterministic system cannot be verified the way deterministic code is: there is no single "expected output" to assert against, and a demo that works once … |
| [`alm-2gp-unlocked-package-modularization.md`](./alm-2gp-unlocked-package-modularization.md) | Pattern — strong default for internal modular development; deviate only with a written reason. | A single monolithic `force-app` directory deployed as "the org" has no internal boundaries: every change touches everything, every deploy re-tests everything, a… |
| [`alm-ci-cd-with-validation-only-deploys.md`](./alm-ci-cd-with-validation-only-deploys.md) | Absolute rule — a prod deploy that wasn't validated check-only first is a bug in the pipeline. | A Metadata API / SFDX deploy is **transactional per component type but not atomic across a half-finished run that fails the test gate**: a deploy that fails par… |
| [`alm-deletions-go-through-destructive-changes-not-the-org-ui.md`](./alm-deletions-go-through-destructive-changes-not-the-org-ui.md) | Pattern — strong default; manual prod deletions are a pipeline bug. | Source tracking and additive deploys move *new and changed* metadata, but a normal `deploy start` does **not** delete components that disappeared from the sourc… |
| [`alm-pick-the-deploy-test-level-deliberately.md`](./alm-pick-the-deploy-test-level-deliberately.md) | Pattern — strong default; the prod test level is effectively fixed by the platform. | The `--test-level` flag on a Metadata API / SFDX deploy decides *which Apex tests run and whether the deploy is allowed to commit*, and the wrong choice fails o… |
| [`alm-scratch-orgs-and-source-tracking.md`](./alm-scratch-orgs-and-source-tracking.md) | Pattern — strong default for package/feature development; deviate only with a written reason. | A developer sandbox is a long-lived, mutable org whose state drifts: someone clicks a setting, an admin adds a field, and now "works in the sandbox" no longer m… |
| [`apex-async-channel-selection.md`](./apex-async-channel-selection.md) | Pattern — strong default; the choice must be justified against the workload, not picked by habit. | Async Apex moves work off the synchronous transaction for larger data volume, higher limits, and the ability to make callouts. |
| [`apex-bind-variables-in-dynamic-soql.md`](./apex-bind-variables-in-dynamic-soql.md) | Absolute rule — string concatenation of untrusted input into SOQL is an injection vulnerability; the verdict escalates to core. | Apex offers a static query syntax (`[SELECT ...]`) and a dynamic one (`Database.query(String)`). |
| [`apex-collections-and-maps-for-o1-lookups.md`](./apex-collections-and-maps-for-o1-lookups.md) | Pattern — strong default; a nested loop where a map keyed lookup would do is a CPU-time and readability defect. | Once SOQL and DML are hoisted out of loops (the bulkify rule), the next bulk-safety failure is **CPU time**, not query count. |
| [`apex-exception-handling-and-custom-exceptions.md`](./apex-exception-handling-and-custom-exceptions.md) | Pattern — strong default; an empty or over-broad `catch` is a defect, not a style choice. | Apex exception handling fails in two opposite directions, both common. |
| [`apex-one-trigger-per-object-handler.md`](./apex-one-trigger-per-object-handler.md) | Absolute rule — a second trigger on the same object, or logic in a trigger body, is a defect. | Salesforce gives **no guaranteed execution order** when an SObject has more than one trigger for the same event. |
| [`apex-query-selectively-with-indexes.md`](./apex-query-selectively-with-indexes.md) | Absolute rule on high-volume objects — a non-selective query is a `QueryException` waiting for the row count to grow. | The Salesforce **query optimizer** picks an index only when a filter is *selective* — when it estimates the predicate returns fewer rows than a threshold (a row… |
| [`apex-recursion-control-on-handlers.md`](./apex-recursion-control-on-handlers.md) | Absolute rule — a handler that issues DML on its own object with no guard is a defect. | When a trigger handler updates the same object that fired it, the update **re-fires the trigger**, which runs the handler again, which updates again — an infini… |
| [`apex-soql-in-loops-is-a-defect.md`](./apex-soql-in-loops-is-a-defect.md) | Primary diagnostic — when this LimitException appears, the SOQL-in-a-loop is the first thing to check. | `System.LimitException: Too many SOQL queries: 101` is the single most common runtime governor-limit failure, and it has one dominant cause: a `SELECT` executin… |
| [`apex-test-data-with-testfactory-not-seealldata.md`](./apex-test-data-with-testfactory-not-seealldata.md) | Absolute rule — `SeeAllData=true` is banned; tests create their own data. | `@isTest(SeeAllData=true)` lets a test read whatever records happen to live in the org. |
| [`bulkify-every-soql-and-dml.md`](./bulkify-every-soql-and-dml.md) | Absolute rule — a violation is a production bug, not a style preference. | Salesforce runs Apex in a multi-tenant context, so every transaction is capped by **governor limits**. |
| [`data-archive-cold-rows-to-keep-hot-object-small.md`](./data-archive-cold-rows-to-keep-hot-object-small.md) | Pattern — strong default for any object whose row count grows unbounded over time; deviate only when every row stays operationally hot. | Selectivity, indexes, and skinny tables all fight the symptom; archival fights the cause. |
| [`data-avoid-ownership-and-lookup-skew.md`](./data-avoid-ownership-and-lookup-skew.md) | Primary diagnostic — when DML on a high-volume object is slow or row-locks, check for ownership/lookup skew first. | **Data skew** is when too many child records point at a single parent (lookup/master-detail skew) or are owned by a single user (ownership skew). |
| [`data-batch-apex-for-large-result-sets.md`](./data-batch-apex-for-large-result-sets.md) | Absolute rule at volume — a single SOQL that exceeds the per-transaction row limit throws; a heap-busting in-memory list is a `LimitException`, not a slow path. | A single synchronous transaction can only retrieve a bounded number of rows and hold a bounded heap. |
| [`data-defer-sharing-recalculation-on-large-loads.md`](./data-defer-sharing-recalculation-on-large-loads.md) | Pattern — strong default for any large ownership/role/sharing-affecting load; deviate only when the load is small or sharing-neutral. | Every time a record is inserted, an owner changes, a role moves, or a sharing rule is added, Salesforce **recalculates sharing** — it walks the OWD → role-hiera… |
| [`data-loader-vs-bulk-api-selection.md`](./data-loader-vs-bulk-api-selection.md) | Pattern — strong default; deviate only with a written reason naming the row count, cadence, and source-system complexity. | "Load this data into Salesforce" has three structurally different answers, and picking by habit instead of by volume/cadence wastes effort or causes an incident… |
| [`data-owd-most-restrictive-then-open-deliberately.md`](./data-owd-most-restrictive-then-open-deliberately.md) | Pattern — strong default; widening the org-wide default is a one-way door at scale, so start closed and justify every loosening. | Org-wide defaults (OWD) set the **floor** of record visibility; the role hierarchy, sharing rules, and manual/Apex-managed shares only ever *open it up* from th… |
| [`data-selective-soql-on-indexed-fields.md`](./data-selective-soql-on-indexed-fields.md) | Absolute rule on high-volume objects — a non-selective query on an LDV object is a runtime error (`Non-selective query against large object type`), not a slow query. | The Salesforce **query optimizer** picks an index only when a filter is *selective* — when the filter resolves to fewer rows than a threshold (a row count and a… |
| [`data-skinny-tables-and-custom-indexes.md`](./data-skinny-tables-and-custom-indexes.md) | Pattern — a tuning lever for proven hot paths, not a day-one default; deviate only after the selective-query and data-model levers are exhausted. | When a high-volume object is queried frequently for reporting or list views and the query still strains after you've made it selective, two Salesforce-managed l… |
| [`enforce-sharing-and-crud-fls.md`](./enforce-sharing-and-crud-fls.md) | Absolute rule — security defaults are not preferences; the verdict escalates to core. | Salesforce record access is layered: org-wide defaults (OWD) set the floor, the role hierarchy and sharing rules open it up. |
| [`flow-before-save-for-same-record-field-updates.md`](./flow-before-save-for-same-record-field-updates.md) | Pattern — strong default; use after-save (or Apex) only when the work is not a same-record write. | When automation only needs to set fields **on the record that triggered it**, a before-save record-triggered Flow writes those fields *in the same DML the platf… |
| [`flow-bulk-safe-no-dml-in-loop-elements.md`](./flow-bulk-safe-no-dml-in-loop-elements.md) | Absolute rule. A DML or query element inside a Flow Loop is a bug, not a preference. | Flow consumes the **same per-transaction governor limits as Apex** — 100 SOQL queries and 150 DML statements synchronous, and the same row caps. |
| [`flow-entry-conditions-and-fault-paths.md`](./flow-entry-conditions-and-fault-paths.md) | Absolute rule for fault handling; strong-default pattern for entry conditions. | Two silent defects ship in most hand-built Flows. |
| [`flow-extract-reusable-logic-into-subflows.md`](./flow-extract-reusable-logic-into-subflows.md) | Pattern — strong default once the same logic appears in a second Flow. | When the same sequence — a fault-logging routine, a "calculate discount" block, a "notify the owner" pattern — is rebuilt by hand in five Flows, a change to tha… |
| [`flow-one-record-triggered-per-object-per-context.md`](./flow-one-record-triggered-per-object-per-context.md) | Pattern — strong default; deviate only with a written, ordered reason in the design. | Record-triggered Flows on the same object and same save phase have **no guaranteed order unless you set one**, and they recurse against each other the same way … |
| [`flow-pick-the-flow-type-by-trigger-and-timing.md`](./flow-pick-the-flow-type-by-trigger-and-timing.md) | Pattern — strong default; the type follows the trigger, not personal familiarity. | Salesforce ships several Flow types and they are **not interchangeable**: each has a different trigger, a different transaction context, and a different set of … |
| [`flow-vs-apex-one-entry-point.md`](./flow-vs-apex-one-entry-point.md) | Pattern — strong default; deviate only with a written reason in the design. | Two failure modes drive this rule. |
| [`integration-bulk-api-for-batch-not-row-by-row-rest.md`](./integration-bulk-api-for-batch-not-row-by-row-rest.md) | Pattern — strong default; deviate only with a written reason naming the row count and why synchronous calls win at that volume. | A synchronous REST or SOAP call carries one record (or a small batch) and consumes one **daily API request** and a long-running concurrent-request slot per call… |
| [`integration-callout-governor-and-async.md`](./integration-callout-governor-and-async.md) | Absolute rule — a callout inside an open DML transaction is a runtime error; the cumulative limits are a design constraint, not a runtime surprise. | Apex callouts run inside the multi-tenant governor envelope, and two rules catch teams repeatedly. |
| [`integration-idempotent-consumers-and-replay-aware-subscribers.md`](./integration-idempotent-consumers-and-replay-aware-subscribers.md) | Absolute rule — a non-idempotent consumer that double-processes a redelivered event is a data-corruption bug, not a tuning issue. | Platform Events and Change Data Capture (CDC) are **eventually delivered with replay**, not synchronous RPC. |
| [`integration-named-credentials-not-hardcoded-endpoints.md`](./integration-named-credentials-not-hardcoded-endpoints.md) | Absolute rule — a hard-coded endpoint or secret in Apex is a security and portability bug, and the secret-handling verdict escalates to core. | An outbound callout needs three things that change between sandbox and production and that must not live in source: the endpoint URL, the authentication scheme,… |
| [`integration-platform-events-vs-cdc-vs-callout.md`](./integration-platform-events-vs-cdc-vs-callout.md) | Pattern — strong default; deviate only with a written reason naming who owns the event shape and who consumes it. | "Notify another system when something happens in Salesforce" has three structurally different answers, and picking the wrong one bakes in either a maintenance t… |
| [`lwc-accessibility-and-performance.md`](./lwc-accessibility-and-performance.md) | Pattern — strong default; accessibility requirements are non-negotiable for any user-facing component. | Two classes of defect ship invisibly in hand-built LWCs. |
| [`lwc-controller-is-cacheable-fls-aware-and-bulk-safe.md`](./lwc-controller-is-cacheable-fls-aware-and-bulk-safe.md) | Absolute rule for the controller's security posture; pattern for the cacheable/bulk shape. | The Apex controller behind an LWC runs in **user context from the browser** — it is a public entry point a user (or a crafted request) can call directly. |
| [`lwc-events-and-component-communication.md`](./lwc-events-and-component-communication.md) | Pattern — strong default; escalate to a heavier channel only when the lighter one cannot reach. | LWC has a deliberate communication hierarchy, and reaching for the heaviest channel by habit creates components that are impossible to reason about. |
| [`lwc-lds-before-apex.md`](./lwc-lds-before-apex.md) | Pattern — strong default; write Apex only when LDS cannot express the need. | Lightning Data Service (LDS) — the `lightning-record-*-form` components and the UI-API wire adapters (`getRecord`, `getRecords`, `getRelatedListRecords`, `creat… |
| [`lwc-wire-over-imperative-when-cacheable.md`](./lwc-wire-over-imperative-when-cacheable.md) | Pattern — strong default for read paths; use imperative Apex only for user-triggered actions or writes. | `@wire` to a `@AuraEnabled(cacheable=true)` Apex method (or to a UI-API adapter like `getRecord`) is **reactive and cached**. |
| [`package-and-deploy-in-dependency-order.md`](./package-and-deploy-in-dependency-order.md) | Pattern — strong default; deviate only with a written reason and a migration note. | A change set hand-clicked into production has no source of truth, no repeatable order, and no rollback. |
| [`platform-config-as-metadata-not-data.md`](./platform-config-as-metadata-not-data.md) | Absolute rule for environment-varying configuration — a hard-coded ID or a config-bearing data record is a bug. | Configuration that lives as a **data record** (a row in a custom object, or a List Custom Setting) does not travel through the release pipeline — it has to be r… |
| [`platform-declarative-before-code.md`](./platform-declarative-before-code.md) | Pattern — strong default for any new automation or customization; deviate only with a written reason. | Every line of Apex you write is a line you own forever: it needs a test class, a bulk assertion, a deploy gate, a code review, and a developer to change it next… |
| [`platform-org-strategy-and-environments.md`](./platform-org-strategy-and-environments.md) | Pattern — strong default; a multi-org split is a deliberate, expensive, written decision. | "How many production orgs?" is one of the most expensive decisions in Salesforce, and it is nearly irreversible — merging two orgs later is a multi-year migrati… |
| [`security-guest-user-and-experience-cloud-sharing.md`](./security-guest-user-and-experience-cloud-sharing.md) | Absolute rule — a misconfigured guest user is the highest-blast-radius public-exposure mistake; the verdict escalates to `ravenclaude-core/security-reviewer`. | Experience Cloud guest users follow a stricter model (no role, no hierarchy, guest sharing rules only) that internal-sharing intuition gets wrong. |

---


