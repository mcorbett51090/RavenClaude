# Willingness-to-pay study brief — `<product / decision>`

> Output of `willingness-to-pay-research`. The brief is the design; the significance
> read routes to `applied-statistics`.

## 1. Decision the study serves
- **The pricing decision this informs:** `<set first price | defend a change | size a tier | packaging>`
- **What "done" looks like:** `<a band | a number | a feature-value map>`

## 2. Method (from §3 decision tree)
- **Chosen method:** `<Van Westendorp | Gabor-Granger | conjoint/MaxDiff | live A/B test>`
- **Why this method:** `<…>`
- **What it returns (and does NOT):** `<e.g. a range, not a price>`

## 3. Sample design
- **Sample frame (who qualifies, how reached):** `<…>`
- **Who we ask — buyer vs user:** `<…>`
- **Target sample size + precision:** `<…>` → *power/sizing math → `applied-statistics`*

## 4. Instrument & bias guards
- **Questions / price points / choice sets:** `<…>`
- **Anchoring guard:** `<no leading price; randomized order>`
- **Framing guard:** `<neutral wording>`

## 5. Analysis plan
- **How results map to the decision:** `<…>`
- **Behavioral discount applied to stated WTP:** `<how much, based on what data>`
- **Confidence bound on the claim:** `<…>`

## Hand-offs
- [ ] Significance / sample-size math → `applied-statistics`
- [ ] Validated band/number → `pricing-strategist`
- [ ] Conjoint feature-values → `packaging-and-tiering`
