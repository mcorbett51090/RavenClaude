# Stale docs are worse than no docs

A confidently-wrong document sends the reader down a dead end and erodes trust in the whole doc set, which is worse than an honest gap. Date volatile content, tie documentation to releases so it matches the product version, run link-checking in CI, and prune what is no longer true. Maintaining less but accurate documentation beats hoarding more that has quietly rotted.
