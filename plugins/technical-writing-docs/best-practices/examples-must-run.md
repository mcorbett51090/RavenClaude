# Every example must run

Test every code sample, or generate samples from tested code, so a reader who copies and pastes succeeds. A quickstart whose first snippet errors destroys trust faster than a missing example, because the reader now doubts everything else in the docs. Wire example-testing into CI so a broken snippet fails the build like any other regression.
