# Treat docs as code, versioned with the product

Keep documentation in the repo, review it in pull requests, check it in CI, and version it with the code it describes. Documentation in a separate wiki or doc tool drifts out of sync the moment the code changes, because nothing ties the two together. Docs-as-code makes the docs change part of the code change, reviewed by the same people, shipped on the same release.
