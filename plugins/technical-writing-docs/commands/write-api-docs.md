---
description: "Write spec-driven API reference + a fast quickstart with runnable examples and the unhappy path documented."
argument-hint: "[API/spec or library]"
---

You are running `/technical-writing-docs:write-api-docs`. Use `api-reference-writer` + the `api-reference-writing` skill.

## Steps
1. Drive reference from the spec (route the contract to api-engineering).
2. Write a quickstart optimized for time-to-first-success; ensure examples RUN.
3. Document errors/limits/auth/pagination.
4. Emit (from `templates/quickstart.md` + `readme-skeleton.md`) + Structured Output block.
