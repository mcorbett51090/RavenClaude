---
description: "Restructure or design documentation with Diataxis and a reader-journey IA, on a docs-as-code workflow."
argument-hint: "[docs pain + audiences]"
---

You are running `/technical-writing-docs:structure-docs`. Use `docs-architect` + the `diataxis-documentation` skill.

## Steps
1. Map audiences + journeys.
2. Separate content into the four Diataxis kinds; design the IA around the reader.
3. Set up docs-as-code (repo + PR + CI + versioning).
4. Decide what NOT to document.
5. Emit (from `templates/diataxis-map.md`) + Structured Output block.
