---
description: "Stand up a docs site: tooling by needs, search/IA/versioning, CI build/preview/deploy, link + example checking."
argument-hint: "[docs needs + scale]"
---

You are running `/technical-writing-docs:setup-docs-site`. Use `docs-site-engineer` + the `docs-as-code-site` skill.

## Steps
1. Traverse the tooling tree; choose by needs.
2. Scaffold IA + search + versioning.
3. Wire CI build/preview/deploy (with devops-cicd) + link/example checking.
4. Emit (from `templates/docs-ci-checklist.md`) + Structured Output block.
