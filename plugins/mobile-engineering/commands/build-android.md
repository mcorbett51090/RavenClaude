---
description: "Build a native Android feature: state-driven Compose, coroutines/Flow, Keystore, WorkManager, Material-aligned."
argument-hint: "[Android feature or bug]"
---

You are running `/mobile-engineering:build-android`. Use `android-engineer` + the `android-craft` skill.

## Steps
1. State-hoisted Compose; key lists; remember correctly.
2. Lifecycle-scoped coroutines/Flow.
3. Keystore/EncryptedSharedPreferences; WorkManager (respect Doze).
4. Material + TalkBack + predictive back.
5. Emit + Structured Output block.
