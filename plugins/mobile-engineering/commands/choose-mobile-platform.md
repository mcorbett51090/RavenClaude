---
description: "Decide native vs React Native vs Flutter by the app's needs, and set the offline/sync + lifecycle strategy."
argument-hint: "[app + team + UX/perf needs]"
---

You are running `/mobile-engineering:choose-mobile-platform`. Use `mobile-architect` + the `mobile-platform-choice` skill.

## Steps
1. Traverse the platform-choice tree; name the trade.
2. Traverse the offline-sync tree; set source of truth + write queue + conflict policy.
3. Plan lifecycle survival + multiple-live-versions tolerance.
4. Emit (from `templates/platform-decision.md`) + Structured Output block.
