---
description: "Build a native iOS feature: state-driven SwiftUI, Swift Concurrency, Keychain, lifecycle/HIG-aligned."
argument-hint: "[iOS feature or bug]"
---

You are running `/mobile-engineering:build-ios`. Use `ios-engineer` + the `ios-craft` skill.

## Steps
1. State-driven SwiftUI with correct property-wrapper ownership.
2. Swift Concurrency (async/await, actors, @MainActor); avoid retain cycles.
3. Keychain for secrets; handle lifecycle/background.
4. HIG + accessibility.
5. Emit + Structured Output block.
