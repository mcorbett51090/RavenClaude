{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "description": "Plugin-distributed hooks for mobile-engineering. Auto-merged with the consumer's session hooks on install. Paths use ${CLAUDE_PLUGIN_ROOT} so they resolve to the install location.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-mobile-engineering-anti-patterns.sh \"$CLAUDE_TOOL_FILE_PATH\"",
            "comment": "Advisory: flags 4 mechanically-detectable mobile-engineering anti-patterns. Set MOBILE_STRICT=1 to make it blocking."
          }
        ]
      }
    ]
  }
}
