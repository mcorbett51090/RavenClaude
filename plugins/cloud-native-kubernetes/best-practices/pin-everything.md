# Pin images, charts, and API versions

Pin container images by digest, Helm charts by version, and target stable API versions. `latest` and floating chart versions make a cluster non-reproducible and turn every upgrade into an unknown. Pinning is what makes a cluster restorable and an upgrade reviewable.
