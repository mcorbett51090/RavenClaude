# Enrollment Funnel Model — <institution / entering term>

> Fill the counts; the conversions and the leak follow. Tie every yield lever to net tuition revenue.

## 1. Funnel

| Stage | Count | Conversion from prior | Notes |
|---|---|---|---|
| Inquiry | `<n>` | — | |
| Applicant | `<n>` | `<%>` | |
| Admit | `<n>` | `<%>` | |
| Deposit | `<n>` | `<%>` | |
| Enroll | `<n>` | `<%>` | |
| Matriculated (post-melt) | `<n>` | `<%>` | melt = deposit − matriculated |

**Steepest drop (leak):** `<stage → stage>` at `<%>`.

## 2. Segment yield

| Segment | Yield | Price- or fit-sensitive | Lever |
|---|---|---|---|
| `<segment A>` | `<%>` | price | aid timing / leveraging |
| `<segment B>` | `<%>` | fit | engagement / belonging |

## 3. Net tuition revenue

| Line | Value |
|---|---|
| Gross tuition (per student) | `<$>` |
| Institutional aid / discount (per student) | `<$>` |
| **Net tuition revenue (per student)** | `<$>` |
| Discount rate | `<%>` |
| × Enrolled headcount | `<n>` |
| **Total net tuition revenue** | `<$>` |

## 4. Lever plan (net-revenue-tested)

| Lever | Stage it targets | Expected yield effect | Net-revenue effect |
|---|---|---|---|
| `<non-aid lever 1>` | `<stage>` | `<+%>` | `<neutral / +>` |
| `<aid lever — only if non-aid exhausted>` | `<stage>` | `<+%>` | `<sized vs net>` |

> Reach for the aid lever only after non-aid levers are exhausted, and size it against **net**
> tuition revenue.
