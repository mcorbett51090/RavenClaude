---
description: "Size partner-sourced vs partner-influenced pipeline with a defined attribution rule, so the program number is one finance trusts and nothing is double-counted with direct. Reach for this before any partner-revenue claim."
argument-hint: "[the pipeline / partner / period in question]"
---

# Size partner-sourced pipeline

You are running `/partnerships-alliances:size-partner-sourced-pipeline` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Write the attribution rule down first — define sourced vs influenced; ambiguous defaults to influenced (§3 #1).
2. Classify the pipeline — tag each open opportunity using the rule and deal-reg timestamps.
3. Compute both numbers separately — sourced ARR vs baseline AND influenced, never summed.
4. Reconcile against the direct number — confirm no double-count with rep quota attainment (`sales-revops`).
5. Report the conversion — deal-reg → closed-won and the sourced-ARR trend, each with a definition and window.

## Output
A sourced/influenced split with the written attribution rule, partner-sourced ARR vs baseline, deal-reg conversion, and a no-double-count reconciliation note. See [`../skills/size-partner-sourced-pipeline/SKILL.md`](../skills/size-partner-sourced-pipeline/SKILL.md).

## Guardrails
- Apply the §3 house opinions before any method; never report influenced as sourced.
- Every metric ships with a definition, window, and baseline.
- No partner PII; cite a source + date for every external figure (or mark it).
