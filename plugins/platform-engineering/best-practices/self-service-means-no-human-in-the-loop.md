# Self-service means no human in the loop

If a 'self-service' action still ends in a ticket a platform engineer must action, it's a faster queue, not self-service. The bar is that a stream-aligned team gets the thing end-to-end themselves. Design the human out of the provisioning loop; a platform team that takes tickets is an ops team with a new name.
