# Require a team owner on every component

Every catalog component has an owner that is a *team*, not an individual, plus a lifecycle and a tier. An un-owned service is a page nobody answers and an incident escalated to the wrong place. Enforce ownership at ingestion — quarantine or reject entries with no team owner rather than publishing them.
