# Treat a golden path as a versioned, owned product surface

A golden path has a maintainer, a version, and a deprecation/migration story. Breaking a path is a breaking change for every consumer who took it, so version it and migrate consumers deliberately. An unowned, unversioned path is abandonware that breaks consumers silently — worse than no path at all.
