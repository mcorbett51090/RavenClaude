# Pave the thinnest viable platform first

Start with the smallest set of golden paths that removes the most cognitive load — often one well-paved 'new service' or 'get an environment' path — then expand on usage. Building a broad, 12-capability portal before any single path is paved and used is the default failure mode; ship one paved road, learn from real consumers, and grow from there.
