---
description: "Reduce and guard AWS cost: tag-based breakdown, rightsizing, zombie cleanup, budgets/anomaly alerts, Savings Plans after rightsizing."
argument-hint: "[cost pain / scope]"
---

You are running `/aws-cloud:optimize-cost`. Use `aws-ops-finops-engineer` + the `aws-finops` skill.

## Steps
1. Break down cost by tag/service; find rightsizing + zombie opportunities.
2. Set budgets + anomaly detection.
3. Recommend Savings Plans/RIs ONLY after rightsizing.
4. Emit (from `templates/finops-checklist.md`) + Structured Output block.
