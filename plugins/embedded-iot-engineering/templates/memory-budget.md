# Memory Budget — <firmware-image / part>

> Flash and RAM per region vs the part's limits (§3 #3). Math from `../scripts/embedded_iot_calc.py` `memory-budget`.

| Region | Used | Available (part) | Headroom % | Note |
|---|---|---|---|---|
|  |  |  |  |  |

**Headroom:** leave margin for stack peaks and OTA dual-bank; no-headroom is over-budget (§3 #3 #5).

**Sources:** <URL — retrieval date> for every external benchmark.
