---
description: "Design the train->register->serve->monitor MLOps lifecycle matched to team maturity, with a build-vs-buy stack choice."
argument-hint: "[team ML maturity + goals]"
---

You are running `/ml-engineering:design-mlops-platform`. Use `ml-platform-architect`.

## Steps
1. Design the full lifecycle loop (not just training).
2. Match the stack to maturity; make build-vs-buy explicit.
3. Make reproducibility + governance structural.
4. Route significance to applied-statistics, LLM apps to claude-app-engineering.
5. Emit + Structured Output block.
