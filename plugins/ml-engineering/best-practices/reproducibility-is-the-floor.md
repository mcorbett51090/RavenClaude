# Reproducibility is the floor, not a nice-to-have

Version the data, code, configuration, and environment for every model so it can be rebuilt, explained, and audited. A model you cannot reproduce is one you cannot debug when it misbehaves, cannot audit when challenged, and cannot trust when it matters. Reproducibility must be structural — built into the pipeline and registry — not reconstructed after the first un-reproducible production incident.
