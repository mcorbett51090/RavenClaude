---
description: "Prioritize a backlog with a transparent framework (RICE/cost-of-delay), making trade-offs explicit."
argument-hint: "[backlog items + reach/impact/effort info]"
---

You are running `/product-management:prioritize-backlog`. Use `product-metrics-analyst` + the `prioritization` skill.

## Steps
1. Score with RICE (or cost-of-delay); make inputs explicit + arguable.
2. Frame each around its outcome + guardrail.
3. Surface where evidence is weak (low confidence).
4. Emit (from `templates/rice-prioritization.md`) + Structured Output block.
