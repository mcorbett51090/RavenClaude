---
description: "Write a PRD framed as problem + target outcome + riskiest assumptions, leaving solution space open."
argument-hint: "[initiative / problem]"
---

You are running `/product-management:write-prd`. Use `product-discovery-lead` + the `continuous-discovery` skill.

## Steps
1. State the validated problem (or flag it needs validation).
2. Define the target outcome + success metric + guardrail.
3. List riskiest assumptions + cheap tests; keep solution space open.
4. Emit (from `templates/prd.md`) + Structured Output block; route delivery to project-management, validity to applied-statistics.
