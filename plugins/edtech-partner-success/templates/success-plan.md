# Success Plan — `<Partner Name>`

**Plan period:** `<30/60/90 day | quarterly | annual>` starting `<YYYY-MM-DD>`
**Owner (our side):** `<PSM name>`
**Owner (partner side):** `<named decision-maker>`
**Sign-off date:** `<YYYY-MM-DD>` — signed by `<role + name>`
**Last updated:** `<YYYY-MM-DD>`

---

## Partner-stated goals (in their words)

> `<verbatim quote>` — `<champion name, role, date>`

> `<verbatim quote>` — `<champion name, role, date>`

(Add more as captured. Don't paraphrase.)

---

## Measurable outcomes (one per goal)

| Goal | Outcome (measurable) | Baseline | Target | Date | Source |
|---|---|---|---|---|---|
| `<short ref to goal>` | `<specific metric, in plain language>` | `<value + date measured>` | `<value + date>` | `<YYYY-MM-DD>` | `<query / dashboard / export ref>` |

---

## Milestones

### 30 days — by `<YYYY-MM-DD>`

- [ ] `<specific check>` — owner `<name>` — date `<YYYY-MM-DD>`
- [ ] `<specific check>` — owner `<name>` — date `<YYYY-MM-DD>`
- [ ] Baseline measurements captured for all outcomes above
- [ ] Named champion engaged + confirmed alive in role

### 60 days — by `<YYYY-MM-DD>`

- [ ] `<specific check>` — owner `<name>` — date `<YYYY-MM-DD>`
- [ ] Mid-cycle check-in completed
- [ ] Rostering / data-quality validated (see [`../skills/rostering-data-quality.md`](../skills/rostering-data-quality.md) if any issue)
- [ ] First measurable outcome trend visible (even if not yet at target)

### 90 days — by `<YYYY-MM-DD>`

- [ ] `<specific check>` — owner `<name>` — date `<YYYY-MM-DD>`
- [ ] First measurable outcome assessed against target
- [ ] Refresh decision documented (continue / adjust / escalate)
- [ ] QBR materials drafted ([`qbr-deck-outline.md`](qbr-deck-outline.md))

---

## Cadence

| Channel | Frequency | Owner |
|---|---|---|
| Touchpoint email / call | `<biweekly default; segment-aware>` | `<PSM>` |
| Mid-cycle check-in | At 30 + 60 day marks | `<PSM>` + `<partner champion>` |
| QBR | Quarterly, aligned to `<academic / fiscal>` calendar | `<PSM>` |

---

## Risks (top 2–3, mapped to recovery plays)

| Risk | Trigger signal | Recovery action |
|---|---|---|
| `<short description>` | `<what's the observable signal>` | `<which play fires; who runs it>` |
| `<short description>` | `<observable signal>` | `<play; owner>` |

---

## Decision rules

- If outcome `<X>` is below target by `<X%>` at `<milestone>`, escalate to `<leadership / cross-functional>` for refresh conversation.
- If champion engagement drops below `<threshold>` for `<period>`, fire `<recovery play>`.
- If rostering data quality issues persist past `<X>` days, escalate to product.
- `<add more as patterns emerge for this partner>`

---

## Partner-side commitments

What the partner agreed to do, in their own words where possible:

- `<verbatim commitment>` — committed by `<champion>` on `<date>`
- `<verbatim commitment>` — committed by `<champion>` on `<date>`

---

## Our-side commitments

What we agreed to do:

- `<commitment>` — owner `<our-side name>` — by `<YYYY-MM-DD>`
- `<commitment>` — owner `<our-side name>` — by `<YYYY-MM-DD>`

---

## Refresh log

| Date | Refreshed by | What changed | Why |
|---|---|---|---|
| `<YYYY-MM-DD>` | `<PSM>` | `<short>` | `<short>` |
