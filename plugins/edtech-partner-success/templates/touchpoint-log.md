# Touchpoint Log — `<Partner Name>`

Running diary of partner interactions. **Newest entries at the top.** This is the diary; the partner profile is the canon.

---

## Format

```markdown
## YYYY-MM-DD — <touchpoint type: email / call / meeting / async / event>

**With:** `<name + role>`
**Channel:** `<email / phone / Zoom / Teams / in-person / Slack / etc.>`
**Duration:** `<for calls / meetings>`

**What happened:**
- `<substantive bullet — not "synced">`
- `<substantive bullet>`

**Partner said (verbatim, where worth quoting):**
> `<quote>` — `<who>`

**Followups captured:**
- [ ] `<action>` — owner `<name>` — by `<YYYY-MM-DD>`
- [ ] `<action>` — owner `<name>` — by `<YYYY-MM-DD>`

**Signals / health implications:**
- `<adoption signal, engagement signal, sentiment, etc. — what to surface to the analytics layer>`

**Open questions for next touchpoint:**
- `<>`
```

---

## Recent touchpoints (newest first)

<!--
Add entries as they happen. Anti-patterns the hook will catch:
- Entries that say "synced with [name]" with no substance
- Followups without dates
- Followups without named owners
- Multi-recipient comms where partner names are visible in the To: line
-->
