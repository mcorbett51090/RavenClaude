# Annual Partner Review — `<Partner Name>` — `<Year>`

**Review period:** `<YYYY-MM-DD>` to `<YYYY-MM-DD>`
**PSM owner:** `<name>`
**Renewal target date:** `<YYYY-MM-DD>` (typically Year+1)
**Renewal recommendation:** `<renew + multi-year | renew single-year | renew with conditions | recommend non-renewal>`

---

## Year in summary (one paragraph)

`<Honest, source-cited paragraph. What were the partner's stated goals at year-start, where did we land, what was the partner experience.>`

---

## Outcomes vs goals

For each partner-stated goal from the annual success plan:

| Goal (partner's words) | Target | Actual | Delta | Source |
|---|---|---|---|---|
| `<verbatim>` | `<value>` | `<value>` | `<+/-N%>` | `<query / dashboard>` |

---

## Wins (named, specific)

- `<specific win — named user, named feature, named outcome, dated>`
- `<specific win>`

## Misses (honest)

- `<honest gap — what didn't land, why we think so>`
- `<gap>`

---

## Partner experience

- **Sentiment trajectory** (NPS / CSAT / qualitative): `<>`
- **Champion stability:** `<champions added / lost during the year; redundancy at year-end>`
- **Support escalation count:** `<N; how many resolved cleanly; any open>`
- **Cross-functional touchpoints:** `<product calls, leadership meetings, joint events>`

---

## What the partner told us this year (verbatim, attributed)

> `<quote>` — `<who, when, in what context>`

> `<quote>` — `<who, when, in what context>`

(These quotes should feed the partner profile's "partner says" section.)

---

## Commitments delivered vs outstanding

What we promised at year-start (or accumulated through the year):

| Commitment | When made | Status | Notes |
|---|---|---|---|
| `<>` | `<YYYY-MM-DD>` | delivered / partial / open | `<>` |

---

## Renewal posture

**Decision-maker confirmation:** `<named decision-maker; still in role as of YYYY-MM-DD>`

**Recommendation rationale:**
- `<reason 1>`
- `<reason 2>`
- `<reason 3>`

**Risks to the renewal:**
- `<risk + likelihood + recovery play>`
- `<risk + likelihood + recovery play>`

**Multi-year vs single-year:** `<recommendation + rationale>`

**Expansion readiness:** `<yes / not now / never — with rationale; this is independent of renewal>`

---

## Year+1 plan preview

High-level outline of next year's success plan (full plan authored separately via [`success-plan.md`](success-plan.md)):

- Top 2–3 goals (anticipated; to be confirmed with partner)
- Major instrumentation / measurement plans
- Key risks to monitor
- Cadence changes (if any)

---

## Submitted to leadership

- [ ] Reviewed by PSM lead on `<YYYY-MM-DD>`
- [ ] Cross-functional input gathered (product, support) on `<YYYY-MM-DD>`
- [ ] Renewal recommendation surfaced to AE / leadership on `<YYYY-MM-DD>`
- [ ] Partner-facing variant drafted (route to `ferpa-comms-translator` if external audiences will read it)
