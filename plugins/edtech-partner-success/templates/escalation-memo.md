# Escalation Memo — `<Partner Name>` — `<short risk description>`

**Date:** `<YYYY-MM-DD>`
**Author (PSM):** `<name>`
**Escalation level:** `<product / leadership / cross-functional / legal>`
**Urgency:** `<immediate / this week / this month>`

---

## Summary (3 sentences max)

`<What's happening, what's at risk, what we're asking for.>`

---

## The signal

What we observed:

- `<specific signal — source, date range, value>`
- `<specific signal — source, date range, value>`

What's NOT happening (sometimes the silence is the signal):

- `<absent expected behavior>`

---

## What we've already tried

> Per the alternate-methods Capability Grounding Protocol ([`../../ravenclaude-core/CLAUDE.md`](../../ravenclaude-core/CLAUDE.md)), this section is mandatory before escalation. Lists what was attempted AND what was considered-and-ruled-out.

Attempted:
1. `<approach>` — outcome: `<what happened, when>`
2. `<approach>` — outcome: `<what happened, when>`

Considered, not attempted:
1. `<approach>` — ruled out because `<reason>`
2. `<approach>` — ruled out because `<reason>`

---

## What we're asking for

Specific, actionable:

- [ ] `<ask>` — owner `<role/team>` — by `<YYYY-MM-DD>`
- [ ] `<ask>` — owner `<role/team>` — by `<YYYY-MM-DD>`

---

## What the partner doesn't yet know

`<Disclosure plan: who tells the partner, in what channel, with what framing, by when.>`

---

## Stakes

- **Revenue at risk:** `<ARR + contract end date>`
- **Strategic value:** `<reference partner / case study / segment-anchor / etc.>`
- **Time to act:** `<window before this becomes harder to recover>`

---

## Followup cadence

- Daily until `<status>`
- Owner: `<PSM>`
- Cross-functional update: `<channel + cadence>`
