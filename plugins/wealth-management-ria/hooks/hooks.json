{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "description": "Plugin-distributed hooks for wealth-management-ria. Auto-merged with the consumer's session hooks on install. Paths use ${CLAUDE_PLUGIN_ROOT} so they resolve to the install location.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/check-wealth-management-ria-anti-patterns.sh \"$CLAUDE_TOOL_FILE_PATH\"",
            "comment": "Advisory: flags mechanically-detectable advisory anti-patterns (a planning/portfolio output that reads as a personalized recommendation with no not-investment-advice disclaimer; an IPS draft with no rebalancing policy; 'fiduciary' and 'Reg BI' used interchangeably). Set RIA_STRICT=1 to make it blocking."
          }
        ]
      }
    ]
  }
}
