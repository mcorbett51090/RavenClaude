# Surface every assumption — a hidden assumption is a hidden lie

Inflation, expected return, longevity, time horizon, and the savings rate dominate every planning number. Return them as a named, editable list, never buried inside a single confident figure. A plan whose assumptions are visible can be stress-tested and trusted; one that hides them can't. Prefer a scenario range (good / expected / poor sequence) over a false-precision point estimate.
