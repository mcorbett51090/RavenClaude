# WCAG Audit Worksheet — <page set / target>

> Criterion-by-criterion audit against a named WCAG version + level (§3 #1 #2). Contrast math from `../scripts/accessibility_calc.py` `contrast`; score from `conformance`.

| WCAG criterion | Level (A/AA) | Result | Severity | Method (auto/manual/AT) | Note |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**Target:** WCAG <version> level <A/AA/AAA>, scope <page set> (§3 #1). A failing Level-A criterion fails the page (§3 #2).

**Sources:** <URL — retrieval date> for every external benchmark.
