# Staffing-Operations Discovery Questionnaire

**Client:** `<>` · **Interviewee:** `<role>` · **Date:** `<YYYY-MM-DD>`

> Use to establish segment, unit, window, baseline, and data reality before any diagnosis. Don't skip the denominator questions — most fill-rate disputes are denominator disputes.

## 1. Business shape
- Which segments, and what's each as a share of revenue/GP? `<>`
- Service models in play (travel / per-diem / locum / direct-hire / teletherapy)? `<>`
- MSP/VMS exposure — do you supply into managed programs? `<>`

## 2. The presenting problem
- In one sentence, what's wrong? `<>`
- How are you measuring it today, and since when has it moved? `<>`
- What does "good" look like — an SLA, a prior period, a competitor? `<>`

## 3. KPI definitions (pin the denominators)
- How exactly do you compute **fill rate**? (orders received vs. workable vs. submittals?) `<>`
- Does **time-to-fill** stop at offer-accept or run to start (post-credentialing)? `<>`
- How is **margin** computed — is burden fully loaded? `<>`

## 4. Seasonality
- What's your busy/slow cycle? (academic calendar? healthcare surge?) `<>`
- Are current comparisons YoY same-period or calendar-quarter? `<>`

## 5. Funnel & capacity
- Submittals per workable order? Reqs per recruiter? `<>`
- Where do you *feel* candidates fall out? `<>`
- Credentialing/clearance turnaround — how long, who owns it? `<>`

## 6. Data reality
- What systems hold the data (ATS / VMS / finance)? `<>`
- Can you export de-identified order/submittal/margin data? `<>`
- Known data-quality issues? `<>`

## 7. Market & competition
- Who do you lose to, and in which segment? `<>`
- What's changed in your market in the last 12–18 months? `<>`
