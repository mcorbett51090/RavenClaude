# Executive Readout — `<engagement>`

**To:** `<VP Ops / MD>` · **From:** `<consultant>` · **Date:** `<YYYY-MM-DD>` · **Segment(s):** `<>`

## Headline
`<the single sentence the sponsor repeats to the board>`

## The three findings that matter

### 1. `<finding>`
- **Number:** `<value — definition — window — baseline>`
- **Cause:** `<supply vs. order-quality / pricing vs. burden / etc.>`
- **Action:** `<what to do>` — **owner** `<>` — **by** `<YYYY-MM-DD>` — **expected movement** `<>`

### 2. `<finding>`
- **Number / Cause / Action** as above

### 3. `<finding>`
- **Number / Cause / Action** as above

## What we couldn't see (blind spots)
- `<data gaps, stated plainly>`

## Sequencing
- **First 30 days:** `<the cheap, high-leverage moves>`
- **Structural:** `<the harder fix>`

## Market context (if in scope)
- `<one-paragraph SIA-anchored read with the inflection named>`

## Sources
- `<URL — retrieval date>` per external figure
