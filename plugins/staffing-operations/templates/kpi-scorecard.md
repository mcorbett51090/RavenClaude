# Staffing KPI Scorecard — `<division / segment>`

**Period:** `<trailing 30 / 90 days | YoY same-period>` ending `<YYYY-MM-DD>`
**Owner:** `<role>` · **Prepared by:** `<consultant>` · **Last updated:** `<YYYY-MM-DD>`
**Segment:** `<healthcare-travel | locum | allied | per-diem | education-school-based | mixed>`

> Every row carries definition + window + baseline or it doesn't ship (§3 #1). Fill rate pairs with time-to-fill; margin with bill/pay/burden (§3 #2, #3). Mark advisory benchmarks `[ESTIMATE]`.

## Headline
`<the one sentence the operator repeats — e.g., "Allied fill is holding (78%) but time-to-fill slipped to 38 days, costing placements to faster competitors.">`

## Scorecard

| KPI | Definition (denominator named) | Value | Window | Baseline | Δ vs. baseline | Band | Triggered action / owner |
|---|---|---|---|---|---|---|---|
| Fill rate | `<orders filled ÷ workable orders>` | `<%>` | `<>` | `<prior / SLA>` | `<>` | 🟢🟡🔴 | `<>` |
| Time-to-fill (to start) | `<days req-open→start, incl. credentialing>` | `<d>` | `<>` | `<>` | `<>` | | `<>` |
| Time-to-submit/present | `<>` | | | | | | |
| Submittal-to-hire | `<>` | | | | | | |
| Requisition aging | `<median days; % >30d>` | | | | | | |
| Gross margin | `<(bill−pay−burden)÷bill>` | `<%>` | | | | | |
| DSO | `<>` | | | | | | |
| Redeployment rate | `<>` | | | | | | |
| Fall-off rate | `<>` | | | | | | |
| Client / candidate NPS | `<with follow-up captured>` | | | | | | |
| Hires/recruiter (norm.) | `<÷ reqs-per-recruiter>` | | | | | | |

## Drill-downs behind any red
- `<KPI>` red because: `<top 1–2 component drivers>`

## Data gaps / assumptions to validate
- `<what the client must confirm against their ATS/VMS>`

## Recommended next actions
| Action | Owner | Date | Expected metric movement |
|---|---|---|---|
| `<>` | `<>` | `<YYYY-MM-DD>` | `<>` |

## Sources
- `<URL — retrieval date>` for every external benchmark
