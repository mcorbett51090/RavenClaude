# Graph data model

Fill **before** any `CREATE` / DDL.

- **Date:**
- **Workload in one sentence:**
- **Graph vs not** (tree leaf): LPG / RDF / stay relational / stay RAG / stay KV
- **If not a graph, hand-off:** `database-engineering` / `ai-rag-engineering` / other:
- **Node labels + business keys:**
- **Relationship types** (verb, direction, properties on the edge):
- **Supernode risks** (celebrity labels + mitigation):
- **Temporal needs:** none / rel properties / first-class history
- **Load key:** MERGE on:
- **Engine (optional, dated):**
