---
description: "Read overhead as a % of collections against the ~62% median before cutting any single cost, so the diagnosis targets the real driver. Reach for this on a margin problem."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Benchmark overhead

You are running `/dental-practice:benchmark-overhead` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Compute overhead % — Total overhead ÷ collections; wages alone are ~25–30% (§3 #1).
2. Benchmark to the band — Compare to the ~62% median and the 55–60% high-performer band.
3. Decompose the gap — Split the overage into staff, supplies, lab, facility, and admin.
4. Target the driver — Prescribe to the largest reducible component, not 'overhead is high'.

## Output
An overhead %, a benchmark comparison, a component decomposition, and a targeted fix. See [`../skills/benchmark-overhead/SKILL.md`](../skills/benchmark-overhead/SKILL.md). Traverse the matching tree in [`../knowledge/dental-decision-trees.md`](../knowledge/dental-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
