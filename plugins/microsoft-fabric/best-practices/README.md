# Microsoft Fabric best-practice docs

Named, citable rules for Microsoft Fabric engagements — each file is one rule, grounded in this plugin's own [`knowledge/`](../knowledge/) bank and enforced by its [`agents/`](../agents/). Read and apply a doc as a whole.

For the cross-tool rule format and the marketplace-wide index, see [`docs/best-practices/_TEMPLATE.md`](../../../docs/best-practices/_TEMPLATE.md) and [`docs/best-practices/README.md`](../../../docs/best-practices/README.md). For the plugin's house opinions and anti-patterns these rules sit inside, see [`../CLAUDE.md`](../CLAUDE.md) §3–§4.

---

## Index

_24 rules. Each file is one named, citable rule; read and apply it whole._

| Doc | Status | Use when |
|---|---|---|
| [`alm-deploy-via-pipelines-parameterize-sources.md`](./alm-deploy-via-pipelines-parameterize-sources.md) | Absolute rule (no hand-editing prod) / Pattern (parameterize stage-specific bindings) — hand-editing a prod workspace is a bug; relying on autobind for Direct Lake / data-source connections is the documented gotcha. | ALM in Fabric is **Git integration (CI)** + **deployment pipelines (CD)**: connect the *developer* workspace to Git, then promote dev→test→prod throug… |
| [`capacity-isolate-noisy-workloads.md`](./capacity-isolate-noisy-workloads.md) | Pattern — workload isolation plus surge protection is the strong default for any capacity mixing BI with background jobs; "one capacity for everything" is the documented anti-pattern (house opinion #5). | Throttling is **per-capacity**: one overloaded capacity is throttled while others run normally. |
| [`capacity-size-to-average-not-peak.md`](./capacity-size-to-average-not-peak.md) | Pattern — sizing to average-plus-smoothing is the strong default; sizing for peak is the documented over-provisioning anti-pattern (house opinion #5). | Fabric **smooths** consumed CUs over future time: interactive ops over 5–64 minutes, **background ops over 24 hours**. |
| [`delta-optimize-vacuum-cadence.md`](./delta-optimize-vacuum-cadence.md) | Primary diagnostic — when a Delta table is slow or a Direct Lake model errors after maintenance, check the OPTIMIZE/VACUUM cadence first. The 7-day VACUUM floor is an **absolute rule**. | `OPTIMIZE` (bin-compaction) and `VACUUM` solve **different** problems and people conflate them. |
| [`directlake-frame-deliberately.md`](./directlake-frame-deliberately.md) | Pattern — explicit, ETL-aware framing is the strong default for any model fed by a multi-step pipeline; rely on automatic updates only when freshness-on-every-commit is genuinely wanted. | A Direct Lake "refresh" is **framing** — a seconds-long metadata operation that points the model at the *latest committed Delta files*. |
| [`directlake-stay-under-guardrails.md`](./directlake-stay-under-guardrails.md) | Absolute rule (on-OneLake side) / Primary diagnostic — exceeding the per-SKU guardrails either silently degrades to DirectQuery (on-SQL) or errors/fails framing (on-OneLake); the failure shape is mode-specific and must be designed for. | Direct Lake loads Delta into VertiPaq **on demand**, bounded by per-SKU **resource guardrails** (memory, Parquet file count, row-group sizing). |
| [`lakehouse-cluster-dont-partition.md`](./lakehouse-cluster-dont-partition.md) | Pattern — Liquid Clustering / Z-order is the strong default for silver/gold file skipping; static partitioning is reserved for a low-cardinality, high-volume case with a written reason. | Static Hive-style partitioning was the old way to get file skipping, but it has a failure mode that hurts Fabric specifically: a **high-cardinality pa… |
| [`lakehouse-medallion-layer-boundaries.md`](./lakehouse-medallion-layer-boundaries.md) | Pattern — medallion is the strong default; skipping a layer or smearing responsibilities across layers needs a written reason. | The medallion shape (bronze → silver → gold) only pays off when each layer keeps **one job**. |
| [`lakehouse-nondestructive-merge-for-framing.md`](./lakehouse-nondestructive-merge-for-framing.md) | Primary diagnostic — when a Direct Lake model goes cold (slow) after every data load despite small changes, check whether the gold load pattern is destructive first. | Direct Lake's killer feature is **incremental framing**: when a Delta table changes, the semantic model drops only the *affected* column segments and … |
| [`lakehouse-vorder-only-where-it-pays.md`](./lakehouse-vorder-only-where-it-pays.md) | Pattern — V-Order on gold consumed by Direct Lake / SQL is the strong default; V-Order on bronze (or on write-heavy staging) is a write tax with no reader to pay it back. | V-Order is a write-time optimization that reorders and compresses Parquet so VertiPaq (and the SQL engine) read it faster — Direct Lake loads V-Ordere… |
| [`lakehouse-vs-warehouse-choose-from-the-tree.md`](./lakehouse-vs-warehouse-choose-from-the-tree.md) | Pattern — the store-selection decision tree is the strong default; picking a store by habit (or by the presence of the word "SQL" in the ask) is the documented anti-pattern. | Lakehouse and Warehouse share the **same OneLake Delta storage and the same SQL surface**, so the bytes live in the same place either way — which is e… |
| [`name-your-direct-lake-mode.md`](./name-your-direct-lake-mode.md) | Absolute rule — "Direct Lake" without the mode is an unfinished sentence, and the omission changes the fallback behavior you design for. | There are **two** Direct Lake variants and they behave oppositely on fallback. |
| [`one-copy-shortcut-before-copying.md`](./one-copy-shortcut-before-copying.md) | Pattern — strong default; copy only with a written reason. (Calling Mirroring "free" without the caveat is an outright bug.) | Every Fabric store keeps **one copy of data in OneLake in open Delta format** by default, so the architecture choice is about the *engine and write pa… |
| [`pipeline-fast-copy-then-spark.md`](./pipeline-fast-copy-then-spark.md) | Pattern — Dataflow Gen2 **Fast Copy** for extract-load and Spark/notebooks for heavy reshaping is the strong default; pushing complex transforms through the standard mashup engine is the slow path. | Dataflow Gen2 is the low-code (300+ transforms) ingestion path, and its **Fast Copy** is **up to 13–21× faster** than Gen1 because it **bypasses the m… |
| [`pipeline-orchestrate-idempotent-watermarks.md`](./pipeline-orchestrate-idempotent-watermarks.md) | Pattern — incremental, idempotent loads with explicit watermark/control-table state are the strong default for pipeline-based ingestion; full reloads on every run are an anti-pattern outside small/initial loads. | When the data-movement decision tree lands on **Copy job** or **Pipeline** (i.e. |
| [`rti-eventhouse-for-streaming-not-lakehouse.md`](./rti-eventhouse-for-streaming-not-lakehouse.md) | Pattern — Eventhouse/KQL DB is the strong default for data-in-motion (streaming, telemetry, logs, time-series); streaming into a lakehouse Delta table is the documented wrong-store anti-pattern (house opinion #2). | People reach for a lakehouse for everything because it's the general-purpose store — but **streaming into a lakehouse Delta table fights the format**. |
| [`rti-retention-and-caching-policy.md`](./rti-retention-and-caching-policy.md) | Primary diagnostic — Eventhouse over-budget or queries slow/expensive → check hot-cache + retention policy before tuning queries. | Hot-cache and retention are the first-order cost/latency levers in an Eventhouse, and defaults rarely match the query pattern. |
| [`rti-shape-at-ingest-with-update-policies.md`](./rti-shape-at-ingest-with-update-policies.md) | Pattern — transforming on ingest (update policies) and pre-aggregating (materialized views) is the strong default in an Eventhouse; reshaping the same raw stream on every query is the slow, expensive path. | In an Eventhouse, a high-rate raw stream (free-text traces, semi-structured events) is cheap to *ingest* but expensive to *re-parse on every query*. |
| [`warehouse-scd-and-merge-patterns.md`](./warehouse-scd-and-merge-patterns.md) | Pattern — strong default for Fabric Warehouse dimensional ELT; deviate only with a written reason. | The warehouse persona had zero rules vs the lakehouse's seven; surrogate keys, SCD-2, and idempotent MERGE are the recurring dimensional-load decisions. |
| [`warehouse-security-rls-cls-masking.md`](./warehouse-security-rls-cls-masking.md) | Pattern — SQL-native row/column security inside the warehouse (defers the two-plane model to workspace-domain-governance); the verdict escalates to core. | When sensitive data lives in a Fabric Warehouse you enforce RLS/CLS in the SQL engine — and that changes the Direct Lake fallback story. |
| [`semantic-measures-not-calc-columns.md`](./semantic-measures-not-calc-columns.md) | Pattern — explicit DAX measures (and gold-side precomputation) are the strong default; calculated columns for aggregatable values are an anti-pattern that bloats the model and, on Direct Lake, isn't even available. | A **calculated column** is materialized into VertiPaq and stored for every row — it inflates model memory, can't respond to filter context, and recomp… |
| [`semantic-star-schema-over-flat.md`](./semantic-star-schema-over-flat.md) | Pattern — a star schema (conformed dimensions + fact tables) is the strong default for any Direct Lake / Import semantic model; a single wide flat gold table is an anti-pattern outside narrow exceptions. | The instinct after building a clean gold layer is to flatten everything into one wide table "so the report is simple." It backfires on two fronts. |
| [`shape-gold-for-direct-lake.md`](./shape-gold-for-direct-lake.md) | Absolute rule — serving bronze to Direct Lake / the SQL endpoint is a bug; the gold-table optimization targets are the strong default. | Direct Lake reads Delta tables straight from OneLake into VertiPaq on demand, so the **physical shape of the gold Delta table is the performance contr… |
| [`workspace-domain-governance-boundary.md`](./workspace-domain-governance-boundary.md) | Absolute rule (the two-plane model) / Pattern (workspace-per-layer-and-stage topology) — treating workspace roles as data access, or promising RLS/CLS on an engine where it's preview, is a bug (house opinion #6). | Security in Fabric is enforced on **two planes**, and conflating them is the documented governance failure (house opinion #6): the **control plane** (… |

---

## See also

- [`../knowledge/`](../knowledge/) — the citation-grounded reference docs (decision trees + per-topic guides) these rules are extracted from
- [`../CLAUDE.md`](../CLAUDE.md) — the Microsoft Fabric team constitution (house opinions §3, anti-patterns §4, the seams §10)
- [`../../../docs/best-practices/README.md`](../../../docs/best-practices/README.md) — the marketplace-wide best-practice index and format
