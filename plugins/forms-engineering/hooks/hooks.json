{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "description": "Plugin-distributed hooks for forms-engineering. Auto-merged with the consumer's session hooks when the plugin is installed. Paths use ${CLAUDE_PLUGIN_ROOT} so they resolve to the plugin's install location.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/flag-form-antipatterns.sh \"$CLAUDE_TOOL_FILE_PATH\"",
            "comment": "Advisory: check this form surface against the rules forms-engineering owns (honeypot invisibility, challenge-widget verification, duplicate-submission guard, a named denominator, the control-charting floor). Rules owned by web-design are deliberately not detected here. Set FORMS_STRICT=1 to make it blocking."
          }
        ]
      }
    ]
  }
}
