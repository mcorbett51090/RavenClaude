{
  "$schema": "https://json.schemastore.org/claude-code-hooks.json",
  "description": "Plugin-distributed hooks for accounting-bookkeeping. Auto-merged with the consumer's session hooks when the plugin is installed. Paths use ${CLAUDE_PLUGIN_ROOT} so they resolve to the plugin's install location.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/flag-accounting-bookkeeping-antipatterns.sh \"$CLAUDE_TOOL_FILE_PATH\"",
            "comment": "Advisory: review this Accounting & Bookkeeping Practice deliverable against the §3 house opinions (baseline on every metric, source+date on every benchmark, no client financial PII). Set ACCOUNTING_BOOKKEEPING_STRICT=1 to make it blocking."
          }
        ]
      }
    ]
  }
}
