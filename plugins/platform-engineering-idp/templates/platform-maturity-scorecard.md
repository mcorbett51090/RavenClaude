# Platform Maturity Scorecard — `<org / team>`

> Assess honestly. The goal isn't a high score; it's the right *next move*. Stage = the lowest rung
> where you can't yet check most boxes.

- **Date:** `<date>` · **Assessor:** `<name>`

## Current stage (circle one)

`Ad-hoc` → `Paved road` → `Self-service` → `Platform-as-product`

## Evidence checklist

| Dimension | Question | ✓/✗ | Note |
|---|---|---|---|
| Paved road | A supported create-build-deploy-run path exists | | |
| Ergonomics | The supported way is *easier* than DIY | | |
| Escape hatch | Stepping off the road is allowed + unsupported | | |
| Self-service | Common infra is a button (guardrails, no ticket) for the common case | | |
| Portal/catalog | Software catalog exists, as code, every entity owned | | |
| Adoption | Adoption is measured (funnel), not assumed | | |
| Outcomes | Platform KPI is developer outcomes, not features shipped | | |
| Product posture | Adoption earned by pull, not mandated by decree | | |
| Team model | Team Topologies: platform/enabling/stream-aligned clear | | |
| Measurement | Balanced DevEx set (delivery + perception + adoption) | | |

## The highest-frequency × highest-pain journey still unaddressed

`<the one journey to pave/improve next>` — cognitive load it removes: `<…>`

## The 2-3 moves to the next rung

1. `<move>` — owner `<…>` — adoption goal `<…>`
2. `<move>` — owner `<…>` — adoption goal `<…>`
3. `<move>` — owner `<…>` — adoption goal `<…>`

## Handoffs

`<platform-product-lead / idp-portal-engineer / golden-path-engineer / devex-metrics-engineer>`
