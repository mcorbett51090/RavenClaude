# Platform SLO Sheet — <surface / period>

> Platform SLIs/SLOs and the error budget that gates change (§3 #6). Bands carry a source + date (§3 #8).

| SLI | SLO target | Window | Error budget | Budget spent |
|---|---|---|---|---|
|  |  |  |  |  |

**Gate:** freeze platform features when the budget is spent — the platform is a service (§3 #6).

**Sources:** <URL — retrieval date> for every external benchmark.
