# Paved-Road RFC — `<title>`

> A lightweight RFC to propose a new paved-road default (a supported tool/pattern/standard the
> platform will back). Keep it short. The bar is: this becomes the easy, supported, default way — and
> stepping off it stays allowed-but-unsupported.

- **Status:** Draft | In review | Accepted | Superseded
- **Author / owner:** `<name / platform team>`
- **Date:** `<date>`
- **Decision-review:** `<route yes/no through the tribunal per repo convention if applicable>`

## 1. Problem / cognitive load

What recurring friction or inconsistent choice across teams is this paving away? Name the cognitive
load removed.

## 2. The proposed paved-road default

The specific tool / pattern / standard, and what "on the road" looks like in practice.

## 3. Why this default (and what it replaces)

- Options considered: `<A / B / C>`
- Chosen default + the decision-tree leaf or rationale.
- What existing variation it supersedes.

## 4. Escape hatch

How a team with a genuine 20% need steps off the road, and what unsupported means here.

## 5. Migration & adoption

- New services: get it by default via the golden path / template.
- Existing services: opt-in path, timeline, and who helps (enabling-team mode — *no mandate*).
- Adoption signal we'll watch (route to devex-metrics-engineer).

## 6. Rollback / reversibility

If this default proves wrong, how do we retire it without breaking teams that adopted it?

## 7. Seams / handoffs

`<idp-portal-engineer / golden-path-engineer / devops-cicd / terraform-iac / security-reviewer>`
