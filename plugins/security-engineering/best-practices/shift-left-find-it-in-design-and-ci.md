# Shift left — find it in design and CI

The cheapest place to remove a vulnerability is the threat model; the next cheapest is a CI scan on the PR that introduced it. A finding in the pre-launch pen-test is 10–100× more expensive to fix and ships late. Move detection earlier, but keep the verdict where it belongs.
