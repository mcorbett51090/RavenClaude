# Least privilege by default

Every identity, role, token, and grant starts at zero and earns each permission. Prefer short-lived federated credentials over long-lived keys, and periodically remove permissions that telemetry shows were never used. A wildcard action/resource is a finding, not a shortcut.
