# TypeScript strict, and type the boundaries

Enable `strict` mode from the start and type the seams hardest — API responses and component props — with no `any` escape hatches. Types are the cheapest test you will ever write and catch contract drift before runtime; an `any` at the API boundary is a production bug waiting with a type annotation that lies. Retrofitting strictness onto a loose codebase is far more expensive than starting strict.
