# Build accessibility into the markup

Use semantic HTML first (a real `button`, `nav`, `label`), add ARIA only to fill genuine gaps, ensure keyboard operability, and manage focus on route and modal changes. Accessibility is an implementation property, not a layer added later — a `div` with an onClick is a broken control for keyboard and screen-reader users. The WCAG audit belongs to web-design; building it right belongs here.
