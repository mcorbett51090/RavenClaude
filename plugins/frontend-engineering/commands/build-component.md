---
description: "Build a composable, typed, accessible React component with correct hooks and testable markup."
argument-hint: "[component + behavior]"
---

You are running `/frontend-engineering:build-component`. Use `react-implementation-engineer` + the `react-component-craft` skill.

## Steps
1. Compose small components with typed props; lift shared logic to hooks.
2. Use hooks correctly (complete deps, no stale closures).
3. Build accessibility into the markup; add stable test ids.
4. Emit (from `templates/component-skeleton.md`) + Structured Output block; route the WCAG audit to web-design.
