# Unit Economics Sheet — <unit / period>

> Cost per customer/txn/feature and the trend (§3 #2). Math from `../scripts/finops_cloud_cost_calc.py` `unit-cost`.

| Period | Allocated cost | Units | Cost per unit | Trend |
|---|---|---|---|---|
|  |  |  |  |  |

**Read:** rising bill + falling unit cost = healthy; flat bill + rising unit cost = decay (§3 #2).

**Sources:** <URL — retrieval date> for every external benchmark.
