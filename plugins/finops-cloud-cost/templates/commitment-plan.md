# Commitment Plan — <scope / term>

> Commitments modeled on the RIGHTSIZED baseline (§3 #4). Math from `../scripts/finops_cloud_cost_calc.py` `rightsizing`/`commitment`.

| Resource family | Rightsized baseline | Coverage % | Blended cost | Savings | Utilization risk |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**Sequence:** waste -> rightsize -> commit; never commit on a fat baseline (§3 #4 #5). Prices carry a source + date (§3 #8).

**Sources:** <URL — retrieval date> for every external benchmark.
