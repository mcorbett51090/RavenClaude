# Correlate metrics, traces, and logs

Propagate a shared trace/span context across all three pillars so an on-call can pivot: a metric spike → an exemplar trace → that request's logs. Three disconnected tools triple the time-to-diagnosis; one correlated story collapses it.
