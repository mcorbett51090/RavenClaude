# Alert on symptoms, not causes

Page on user-visible pain (latency, error rate, SLO burn), not internal proxies like CPU that may not affect anyone. Cause-based alerts generate noise during harmless spikes and miss real pain that doesn't move the proxy. Reserve causes for when they reliably *precede* user impact.
