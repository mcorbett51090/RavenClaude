---
description: "Compute cost-to-raise-a-dollar per channel, never blended, so the subsidizing channel is visible. Reach for this on a portfolio/efficiency question."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Read cost-per-dollar by channel

You are running `/nonprofit-fundraising:read-cost-per-dollar-by-channel` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Compute the ratio per channel — Cost ÷ dollars raised for events, mail, grants, major gifts, digital (§3 #4).
2. Compare to channel norms — Each channel has a different healthy ratio.
3. Read the mix — Track the restricted/unrestricted split for sustainability (§3 #6).
4. Reallocate — Shift effort toward the efficient channels, accounting for capacity.

## Output
A channel-by-channel cost ratio, the restricted mix, and a reallocation recommendation. See [`../skills/read-cost-per-dollar/SKILL.md`](../skills/read-cost-per-dollar/SKILL.md). Traverse the matching tree in [`../knowledge/fundraising-decision-trees.md`](../knowledge/fundraising-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
