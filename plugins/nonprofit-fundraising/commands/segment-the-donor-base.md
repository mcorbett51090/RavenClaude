---
description: "Segment donors by value, recency, and engagement (RFM-style) to direct cultivation hours where they pay. Reach for this when cultivation is spread thin."
argument-hint: "[the situation, e.g. the metric / segment / matter in question]"
---

# Segment the donor base

You are running `/nonprofit-fundraising:segment-the-donor-base` for `$ARGUMENTS`. Run it the way the team's specialists would — applying the house opinions in [`../CLAUDE.md`](../CLAUDE.md) §3.

## Steps (traverse top-to-bottom; do not skip)
1. Score recency, frequency, value — Build RFM-style segments (§3 #3).
2. Tier the base — Group into acquisition, retention, and major-gift tiers.
3. Assign the strategy — Match each tier to a channel and cultivation intensity.
4. Direct the hours — Concentrate staff time on the highest-value movable segments.

## Output
An RFM segmentation, donor tiers, a per-tier strategy, and an hours allocation. See [`../skills/segment-the-donor-base/SKILL.md`](../skills/segment-the-donor-base/SKILL.md). Traverse the matching tree in [`../knowledge/fundraising-decision-trees.md`](../knowledge/fundraising-decision-trees.md).

## Guardrails
- Apply the §3 house opinions before any method; resist a single-cause story.
- No client PII in the output; cite a source + date for every external figure (or mark it).
- End with owner / date / expected movement on each recommendation.
