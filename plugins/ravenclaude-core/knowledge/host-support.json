{
  "_note": "MH-21 — the single source of truth for WHICH RAVENCLAUDE COMPONENTS ACTUALLY WORK ON WHICH HOST. Created 2026-07-28 by the multi-host audit. Before this file, every surface that needed the answer either hardcoded it (generate-dashboards.py's _HOOK_CAPABLE_HOSTS) or, more often, silently assumed 'everywhere' — which is how the Pipeline tab came to imply guardrails on hosts that run none, and how Heimdall came to report an unwatched perimeter as clean. A per-component annotation would have been ~20 copies of the same few facts, each free to rot independently; ONE map keeps them honest together.",
  "_contract": "`supported` is a claim about whether the component RUNS on that host today, in this repo, as wired by `scripts/ravenclaude`. It is NOT a claim about whether the host is capable in principle — a host can speak a contract perfectly and still be unsupported here because nothing wires it, in which case `supported` is false and `blocked_by` says exactly that. Codex WAS this file's worked example of that distinction until 2026-07-28, when `--host codex` shipped and flipped its hooks + skills cells to true; Cursor is the current example. When a host gains an install path, update this file; do not add a second list somewhere else. `activation_gate` is the per-host silent-disarm class (`hash_trust` | `version_floor` | `none`), consumed by the shared `_rc_rearm_notice` helper at install/update/status (PR 10 / Gate 207). Codex is hash_trust (MH-17); Copilot is version_floor (MH-23). A host whose canary cannot be confirmed to fire ships as unsupported, not silently assumed working.",
  "_provenance": "verified = confirmed in-repo this session by a visible tool call. docs-verified = read from the host's own documentation this session, url given. inferred = neither; MUST be verified before anything is built on it.",
  "schema_version": 1,
  "updated": "2026-08-14",
  "hosts": {
    "claude-code": {
      "label": "Claude Code",
      "native": true,
      "activation_gate": "none"
    },
    "copilot": {
      "label": "GitHub Copilot CLI",
      "native": false,
      "activation_gate": "version_floor"
    },
    "codex": {
      "label": "OpenAI Codex CLI",
      "native": false,
      "activation_gate": "hash_trust"
    },
    "cursor": {
      "label": "Cursor",
      "native": false,
      "activation_gate": "none"
    },
    "gemini": {
      "label": "Gemini CLI",
      "native": false,
      "activation_gate": "none"
    },
    "aider": {
      "label": "Aider",
      "native": false,
      "activation_gate": "none"
    },
    "windsurf": {
      "label": "Windsurf / Devin Desktop",
      "native": false,
      "activation_gate": "none"
    }
  },
  "components": {
    "hooks": {
      "what": "PreToolUse / SessionStart / Stop guardrails — the tribunal, guard-destructive, runaway brake, layout gate, dod gate.",
      "claude-code": {
        "supported": true,
        "how": "reads plugins/*/hooks/hooks.json natively",
        "basis": "verified"
      },
      "copilot": {
        "supported": true,
        "how": "repo-level .github/hooks, wired by `ravenclaude install`, envelope translated by hooks/copilot-hook-adapter.sh",
        "caveat": "PLUGIN-level hooks never fire (github/copilot-cli#2540), which is why they ship repo-level. Tool-name VALUES are lowercase and are normalised in the adapter (f55039ec) — before that fix the tribunal and web guard were silent no-ops. CLI projection ≠ Chat Preview: VS Code Copilot Chat is a distinct product (see knowledge/copilot-chat-customization.md). Chat Preview may load .github/hooks when org policy / chat.hooks.enabled allows; matchers are ignored; live fire on a given machine is [unverified] until probed (CL-19). CONTENTION is two-writers-one-tree, not Chat context isolation.",
        "surfaces": {
          "chat": {
            "supported": false,
            "basis": "docs-verified only — Preview hooks documented 2026-08-14; live PreToolUse fire not confirmed on the owner's machine",
            "note": "Do not flip supported to true without a Phase 0 payload dump."
          }
        },
        "basis": "verified + docs-verified (https://docs.github.com/en/copilot/concepts/agents/hooks)"
      },
      "codex": {
        "supported": true,
        "how": "`ravenclaude install --host codex` writes <project>/.codex/hooks.json in the CLAUDE-shaped schema, each entry wrapped through hooks/codex-hook-env.sh. NO envelope adapter: Codex speaks the Claude contract natively (identical PascalCase events, identical stdin fields, identical exit-2 blocking, identical PascalCase tool-name VALUES). The shim's ONLY job is lifting cwd/session_id from the stdin payload into CLAUDE_PROJECT_DIR/CLAUDE_SESSION_ID, which Codex does not supply.",
        "caveat": "HASH-TRUST (MH-17): Codex tracks hook trust by hash, so any change to a hook script marks it for review and SKIPS it until the user re-trusts via /hooks. Because this repo's update model is `git pull`, the steady state for an un-warned user is guardrails silently off after every update — and no banner says so, because the SessionStart banner is itself a hook. The installer and `ravenclaude update` both print the re-trust notice; `requirements.toml` managed hooks are the only unattended-survival configuration.",
        "note": "CLAUDE_PLUGIN_ROOT and CLAUDE_PLUGIN_DATA ARE supplied by Codex as legacy-compatibility names — an earlier claim in this repo that they were not was wrong and is corrected in knowledge/codex-cli-customization.md. NOTE that _portable.sh's _rc_host_env fallbacks (CODEX_PROJECT_ROOT/SESSION_ID) are speculative names NOT in Codex's documented environment; the shim, not the alias, is what actually closes the gap.",
        "basis": "verified 2026-07-28 (installer wires it end-to-end; Gate 155 proves the shim's invariants) + docs-verified (https://learn.chatgpt.com/docs/hooks)"
      },
      "cursor": {
        "supported": true,
        "how": "`ravenclaude install --host cursor` writes <project>/.cursor/hooks.json, PROJECTED from the canonical manifest by scripts/generate-cursor-hooks.py, with each entry wrapped through hooks/cursor-hook-adapter.sh. 20 hooks wired, 5 explicitly skipped with reasons. The enforcing event is beforeShellExecution — the only one whose full input AND output schema Cursor publishes.",
        "caveat": "⚠ CURSOR FAILS OPEN: a malformed hook response silently ALLOWS the command (Cursor's own bug tracker, verified 2026-07-28) — every other supported host fails CLOSED. The adapter's deny verdict is a fixed literal with no interpolation for exactly this reason. ALSO: the layout gate and web-access guard are NOT enforced here (no verified Cursor event carries a file path pre-write, or an agent web fetch); CI is the backstop and the generated .mdc rules say so. AND: this lane is docs-verified but UNTESTED against a running Cursor — there is no Cursor binary in this repo.",
        "basis": "docs-verified 2026-07-28 (https://cursor.com/docs/agent/hooks) + verified (installer wires it end-to-end; Gates 159/160). NOT verified in a live Cursor session."
      },
      "gemini": {
        "supported": true,
        "how": "`ravenclaude install --host gemini` merges a projected `hooks` block into <project>/.gemini/settings.json (17 wired, 8 explicitly skipped), each entry wrapped through hooks/gemini-hook-adapter.sh. Gemini's contract is nearly Claude's: identical stdin field names, and `exit 2` + stderr IS its block mechanism, so blocking needs NO translation. The shim's one real job is normalising the tool-name vocabulary (run_shell_command -> Bash, read_file -> Read, ...).",
        "caveat": "The tool-name normalisation is not optional: guardrails dispatch on Claude's PascalCase and fall through to `*) exit 0` on anything else — that exact mismatch is MH-01, where the tribunal was fully wired under Copilot and reviewed nothing. NOT enforced here: the layout gate, because its path arrives as argv and Gemini's tool_input path FIELD NAME is unverified; wiring it would look enforced while silently no-opping. CI is the backstop. This lane is docs-verified but UNTESTED against a running Gemini CLI.",
        "basis": "docs-verified 2026-07-29 (geminicli.com/docs/hooks/reference/) + verified (installer wires it; Gates 164/165)"
      },
      "aider": {
        "supported": false,
        "blocked_by": "Aider has no hooks API at all — there is no in-loop enforcement point to wire, so no guardrail in this marketplace can gate an Aider session. CI is the only backstop. This is the one host lane that bridges instruction files WITHOUT bridging enforcement, and the generated CONVENTIONS.md states that plainly rather than implying coverage.",
        "basis": "verified (no adapter, no install path, no host hooks mechanism)"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "no adapter, no install path",
        "basis": "verified"
      }
    },
    "skills": {
      "what": "Agent Skills under .claude/skills — spawn-team, forge, thing, wireframe, and the rest.",
      "claude-code": {
        "supported": true,
        "how": "reads .claude/skills",
        "basis": "verified"
      },
      "copilot": {
        "supported": true,
        "how": "reads .claude/skills live; symlinked by `ravenclaude install`",
        "basis": "verified"
      },
      "codex": {
        "supported": true,
        "how": "`ravenclaude install --host codex` symlinks all 50 skills into <project>/.agents/skills/. Codex scans .agents/skills in EVERY directory from cwd up to the repo root, then $HOME/.agents/skills, then /etc/codex/skills, then bundled. SKILL.md needs name+description frontmatter — the same shape as .claude/skills — so the existing skills project across unchanged, with no per-host rewriting.",
        "caveat": "NOT `.claude/skills` — that is Copilot's and Claude's read path. Wiring a Codex user there is exactly how `ravenclaude setup` used to complete 'successfully' while wiring zero skills into their host (MH-07).",
        "basis": "docs-verified 2026-07-28 (https://learn.chatgpt.com/docs/build-skills) + verified (installer wires it; 50 skills confirmed in a scratch install). NOTE: multiple third-party guides claim `.codex/skills`; the primary source says `.agents/skills`. Trust this row, not a blog."
      },
      "cursor": {
        "supported": false,
        "blocked_by": "no skills mechanism wired",
        "basis": "verified"
      },
      "gemini": {
        "supported": false,
        "blocked_by": "no skills mechanism wired",
        "basis": "verified"
      },
      "aider": {
        "supported": false,
        "blocked_by": "no skills mechanism wired",
        "basis": "verified"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "no skills mechanism wired",
        "basis": "verified"
      }
    },
    "slash_commands": {
      "what": "/forge, /dashboard, /wrap, /set-posture, /wireframe, /stream, /reset-plugin-cache.",
      "claude-code": {
        "supported": true,
        "basis": "verified"
      },
      "copilot": {
        "supported": false,
        "blocked_by": "Copilot CLI has no user slash commands; these are documented shell invocations instead. `rc` is the front door.",
        "basis": "verified (plugin CLAUDE.md states this)"
      },
      "codex": {
        "supported": false,
        "blocked_by": "no slash-command surface wired",
        "basis": "verified"
      },
      "cursor": {
        "supported": false,
        "blocked_by": "no slash-command surface wired for this host",
        "basis": "verified"
      },
      "gemini": {
        "supported": false,
        "blocked_by": "no slash-command surface wired for this host",
        "basis": "verified"
      },
      "aider": {
        "supported": false,
        "blocked_by": "no slash-command surface wired for this host",
        "basis": "verified"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "no slash-command surface wired for this host",
        "basis": "verified"
      }
    },
    "agents": {
      "what": "The 15 specialist subagents.",
      "claude-code": {
        "supported": true,
        "basis": "verified"
      },
      "copilot": {
        "supported": true,
        "how": "generated projection at plugins/ravenclaude-core/copilot/agents/*.agent.md",
        "caveat": "SECURITY GAP — the least-privilege `tools:` allowlist is DROPPED in projection, and a Copilot agent has ALL tools by default (a `tools` spec only restricts). security-reviewer, canonically Read/Grep/Glob/Bash/WebFetch with Write/Edit withheld, can write. Not fixed because Copilot's complete tool vocabulary is unpublished; see the KNOWN GAP block in copilot/README.md.",
        "basis": "verified"
      },
      "codex": {
        "supported": true,
        "basis": "docs-verified",
        "how": "projected to .codex/agents/*.toml by scripts/generate-codex-agents.py and copied in by `ravenclaude install --host codex`. Each carries an explicit sandbox_mode — read-only for the 5 review-only agents — because Codex INHERITS the parent turn's permissions when it is omitted, so leaving it out would silently grant write. Gate 170 holds that.",
        "caveat": "The .toml contract is docs-verified but has NOT been watched being enforced by a running Codex session (no Codex CLI on the authoring machine); see also openai/codex#26868."
      },
      "cursor": {
        "supported": false,
        "blocked_by": "no generated agent projection exists for this host",
        "basis": "verified"
      },
      "gemini": {
        "supported": false,
        "blocked_by": "no generated agent projection exists for this host",
        "basis": "verified"
      },
      "aider": {
        "supported": false,
        "blocked_by": "no generated agent projection exists for this host",
        "basis": "verified"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "no generated agent projection exists for this host",
        "basis": "verified"
      }
    },
    "monitors": {
      "what": "The reactive run-state monitor (push complement to the pull dashboards).",
      "claude-code": {
        "supported": true,
        "basis": "verified"
      },
      "copilot": {
        "supported": false,
        "blocked_by": "Copilot CLI has no monitors component",
        "basis": "verified (plugin CLAUDE.md states this)"
      },
      "codex": {
        "supported": false,
        "blocked_by": "no monitors component wired",
        "basis": "verified"
      },
      "cursor": {
        "supported": false,
        "blocked_by": "no monitors component wired for this host",
        "basis": "verified"
      },
      "gemini": {
        "supported": false,
        "blocked_by": "no monitors component wired for this host",
        "basis": "verified"
      },
      "aider": {
        "supported": false,
        "blocked_by": "no monitors component wired for this host",
        "basis": "verified"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "no monitors component wired for this host",
        "basis": "verified"
      }
    },
    "instruction_files": {
      "what": "Does the host automatically read AGENTS.md (the cross-tool discipline)?",
      "claude-code": {
        "supported": true,
        "how": "CLAUDE.md @-imports AGENTS.md",
        "basis": "verified"
      },
      "copilot": {
        "supported": true,
        "how": "reads AGENTS.md natively, but only from the USER's repo — `ravenclaude install` now writes .github/copilot-instructions.md pointing at the projected copy (3486637e)",
        "basis": "verified + docs-verified"
      },
      "codex": {
        "supported": true,
        "how": "reads AGENTS.md from the repo root",
        "basis": "docs-verified"
      },
      "cursor": {
        "supported": true,
        "how": "`ravenclaude install --host cursor` writes .cursor/rules/*.mdc — Cursor's OWN convention, which its docs describe as the scoped alternative to AGENTS.md rather than a superset. Two rules: an always-on discipline pointer, and a glob-scoped layout-allow-list rule (MH-25).",
        "caveat": "AGENTS.md auto-load by Cursor remains unconfirmed; the .mdc rules are what this repo actually wires, so do not rely on AGENTS.md reaching a Cursor session on its own.",
        "basis": "docs-verified 2026-07-28 (cursor.com/docs, rules) + verified (installer wires it)"
      },
      "gemini": {
        "supported": true,
        "how": "`ravenclaude install --host gemini` appends an @-import to <project>/GEMINI.md: `@AGENTS.md`. Gemini loads GEMINI.md automatically and supports `@file.md` imports, so this is the ONLY non-Claude host that can INCLUDE the canonical file rather than receive a projected copy — nothing to drift.",
        "caveat": "Appended as a marked block; an existing GEMINI.md that already carries the marker is left alone.",
        "basis": "docs-verified 2026-07-29 (geminicli.com/docs/cli/gemini-md/) + verified (installer wires it)"
      },
      "aider": {
        "supported": true,
        "how": "`ravenclaude install --host aider` projects AGENTS.md into CONVENTIONS.md (the file Aider actually reads) AND writes .aider.conf.yml with `read: [CONVENTIONS.md]` — the explicit opt-in Aider requires. Both halves are needed: a pointer file would not be read, and documentation alone would not opt in.",
        "caveat": "Aider still does NOT read AGENTS.md — that claim was false and was corrected in a5d7e4bf. This lane works by projecting to Aider's own documented file, not by making AGENTS.md load. An existing .aider.conf.yml is OVERWRITTEN by the installer; merge other keys by hand.",
        "basis": "docs-verified (https://aider.chat/docs/usage/conventions.html) + verified (installer wires it; Gate 161)"
      },
      "windsurf": {
        "supported": false,
        "blocked_by": "unverified, and the product was reportedly renamed to Devin Desktop (2026-06-02)",
        "basis": "inferred"
      }
    }
  }
}
