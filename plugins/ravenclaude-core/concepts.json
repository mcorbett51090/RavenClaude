{
  "schema_version": 1,
  "categories": [
    {
      "name": "Foundations",
      "order": 1
    },
    {
      "name": "Getting started",
      "order": 5
    },
    {
      "name": "Platform model",
      "order": 10
    },
    {
      "name": "Shipping to production",
      "order": 15
    },
    {
      "name": "Shipping lessons",
      "order": 23
    },
    {
      "name": "Security",
      "order": 25
    },
    {
      "name": "Guardrails",
      "order": 30
    },
    {
      "name": "Observability",
      "order": 40
    },
    {
      "name": "Orientation & capability",
      "order": 40
    },
    {
      "name": "Agent disciplines",
      "order": 50
    },
    {
      "name": "Marketplace engineering",
      "order": 50
    },
    {
      "name": "Planning & contribution",
      "order": 60
    }
  ],
  "concepts": [
    {
      "id": "agent-harness-loop",
      "title": "The agent loop: one turn",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 1,
      "summary": "Every agent turn runs the same loop: assemble prompt → call model → run tools → feed results back → repeat until done. The 'harness' is the software wrapping the model that drives it.",
      "see_also": [
        "tool-use",
        "context-window",
        "subagents"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "The fundamental agent-loop shape changes, or Claude Code/the Agent SDK restructures how a turn is driven.",
      "sources": [
        {
          "label": "Building agents with the Claude Agent SDK",
          "url": "https://code.claude.com/docs/en/agent-sdk"
        },
        {
          "label": "\"The Anatomy of an Agent Harness\" (Akshay Pachaar)",
          "url": "https://x.com/_avichawla/status/2062082282878627946"
        }
      ],
      "body_md": "A language model on its own is **stateless**: it forgets what it did three steps ago, a failed tool call would otherwise vanish silently, and its context window fills with noise. An **agent** is a model plus the software that fixes all that — the **harness**. As the saying goes, *\"if you're not the model, you're the harness.\"*\n\nThe harness drives one **turn** as a loop, and every agentic tool — Claude Code, the Agent SDK, your own script — runs some version of it:\n\n1. **Assemble the prompt** — system prompt + tool schemas + project memory (`CLAUDE.md` / `AGENTS.md`) + the conversation so far + your message.\n2. **Call the model.**\n3. **Classify the output** — did it ask to use a tool, or just answer? Plain text with no tool call ends the turn.\n4. **Execute the requested tools** — and this is where permission gates, hooks, or a review panel can deny, edit, or wave the call through.\n5. **Package the results** as the next message — crucially, an *error* comes back as a result the model can read and self-correct from, not a crash.\n6. **Update context** (compacting when it fills) and loop back to step 1.\n\nThe same loop scales up: a lead agent can **dispatch a subagent** for a slice of work and fold its short summary back in at step 5. A guiding principle the better harnesses inherit: **treat memory as a hint and verify against real state before acting** — which is why a careful agent doesn't stop at \"looks done,\" it stops when a verification step says it's done.\n\nRavenClaude is one such harness layer: it rides on Claude Code's loop and adds orchestration, a review tribunal at step 4, and verification gates at the end — but the loop itself is how *any* agent works by default.",
      "diagram": "flowchart TD\n  U[Your goal] --> P[1 · Assemble prompt]\n  P --> L[2 · Call the model]\n  L --> C{3 · Tool calls?}\n  C -->|yes| T[4 · Execute tools]\n  T --> R[5 · Package results]\n  R --> X[6 · Update context]\n  X --> P\n  C -->|no| D[Done · verified by gates]\n  class P,L,C,T,R,X,D built",
      "diagram_mini": "flowchart LR\n  P[prompt] --> M[model] --> T[tools] --> P\n  class P,M,T built",
      "steps": [
        {
          "caption": "Assemble the prompt: system prompt + tool schemas + CLAUDE.md/AGENTS.md memory + history + your message.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class A built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-1.svg"
        },
        {
          "caption": "Call the model. A lead agent may dispatch a focused subagent for this slice of work.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class B built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-2.svg"
        },
        {
          "caption": "Classify the output: tool calls → execute and loop; plain text with no tool call → the turn ends.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class C built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-3.svg"
        },
        {
          "caption": "Execute tools — a permission layer, hook, or review panel can ALLOW / EDIT / DENY each call before it runs.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class D built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-4.svg"
        },
        {
          "caption": "Package results as observations. Errors return as results so the model can self-correct, not crash.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class E built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-5.svg"
        },
        {
          "caption": "Update context; compact when it fills. A dispatched subagent returns a short summary, not its raw output.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class F built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-6.svg"
        },
        {
          "caption": "Loop until done — then a verification step confirms \"done\" really means done before the turn ends.",
          "diagram": "flowchart LR\n  A[Assemble prompt] --> B[Call model] --> C[Classify output] --> D[Execute tools] --> E[Package results] --> F[Update context] --> G[Loop / done]\n  class G built",
          "svg": "knowledge/concepts/visuals/agent-harness-loop.step-7.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/agent-harness-loop.svg",
      "svg_mini": "knowledge/concepts/visuals/agent-harness-loop.mini.svg"
    },
    {
      "id": "tool-use",
      "title": "Tool use (function calling)",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 2,
      "summary": "A model can't act on its own — it asks for a tool by name, the harness runs it, and the result comes back as the next message. That request/result loop is every agent action.",
      "see_also": [
        "agent-harness-loop",
        "permission-layers"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "Anthropic changes the tool-use request/result message shape, the tool_choice options, or the parallel-tool-call behavior.",
      "sources": [
        {
          "label": "Tool use (function calling) — Claude API",
          "url": "https://docs.claude.com/en/docs/build-with-claude/tool-use"
        },
        {
          "label": "Agent SDK — tools & permissions",
          "url": "https://code.claude.com/docs/en/agent-sdk"
        }
      ],
      "body_md": "A language model only produces **text**. It can't read a file, run a command, or call an API by itself. **Tool use** (also called *function calling*) is the bridge: you describe the available tools — each with a name, a description, and a JSON schema for its inputs — and the model, instead of answering in prose, emits a structured **tool-use request** naming one tool and the arguments to call it with.\n\nThe model never runs anything. The **harness** (Claude Code, the Agent SDK, your own loop) executes the requested tool, captures the output, and feeds it back as a **tool-result** message. The model reads that result on the next turn and decides what to do next — call another tool, or answer. Every single thing an agent *does* — edit a file, run a test, open a PR — is one trip around this request → execute → result cycle.\n\nThree details that bite:\n\n- **The model picks the tool, not the harness.** You can constrain it (`tool_choice` can force a specific tool, force *any* tool, or leave it `auto`), but in normal operation the model chooses. A vague tool description is the most common reason it picks the wrong one or fills arguments badly.\n- **Errors are data, not crashes.** A failed tool call should return its error *as a tool-result* so the model can read it and self-correct. Throwing away the loop on the first failure is the classic broken-agent bug.\n- **Results re-enter the context window.** Tool output isn't free — a command that dumps 10k lines lands all of it back in the prompt. This is why focused tools that return *conclusions* beat raw firehoses.\n\nA permission layer can sit between the request and the execution: the model may *ask* for a tool, but the harness (or a hook, or a tribunal) can still deny, edit, or gate the call before it runs.",
      "diagram": "flowchart TD\n  G[Your goal] --> M[Model reads prompt + tool schemas]\n  M --> D{Answer or act?}\n  D -- answer --> OUT[Plain-text reply · turn ends]\n  D -- act --> REQ[Tool-use request<br/>name + JSON arguments]\n  REQ --> GATE{Permission / hook}\n  GATE -- deny --> BACK[Denied result fed back]\n  GATE -- allow --> RUN[Harness executes the tool]\n  RUN --> RES[Tool-result message<br/>output or error]\n  RES --> M\n  BACK --> M\n  class M,REQ,RUN,RES fact\n  class GATE built",
      "diagram_mini": "flowchart LR\n  M[model] --> R[tool request] --> X[harness runs it] --> O[result] --> M\n  class M,R,X,O fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/tool-use.svg",
      "svg_mini": "knowledge/concepts/visuals/tool-use.mini.svg"
    },
    {
      "id": "context-window",
      "title": "The context window",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 3,
      "summary": "Everything the model 'knows' this turn is the text in its context window — system prompt, tools, history, files. It's finite, it fills up, and when it's full the harness compacts.",
      "see_also": [
        "agent-harness-loop",
        "tool-use",
        "session-start-context"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "Frontier context-window sizes change materially, or Claude Code changes its compaction / auto-compact behavior.",
      "sources": [
        {
          "label": "Context windows — Claude API",
          "url": "https://docs.claude.com/en/docs/build-with-claude/context-windows"
        },
        {
          "label": "Manage context — Claude Code",
          "url": "https://code.claude.com/docs/en/costs"
        }
      ],
      "body_md": "A model has **no memory between turns.** Everything it \"knows\" while answering is whatever text sits in its **context window** right now: the system prompt, the tool schemas, the project instructions (`CLAUDE.md` / `AGENTS.md`), the conversation so far, and the contents of any file or tool result that's been pulled in. Nothing outside that window exists to the model — if it isn't in context, the model can't see it, no matter how relevant.\n\nThe window is **finite** (measured in tokens — roughly ¾ of a word each). Two consequences follow:\n\n- **It fills up.** Long sessions, big files, and chatty tool output all consume the budget. As it fills, the *effective* room for reasoning shrinks, and far-back details get crowded out — the model can genuinely \"forget\" what happened early in a long session.\n- **When it's nearly full, the harness compacts.** Claude Code summarizes the older part of the conversation into a compact recap and continues with that plus the recent turns. The work isn't lost, but it's now a *summary* — which is why durable facts belong in `CLAUDE.md` or committed files, not just in chat.\n\nThe practical model: treat context like a **desk, not a filing cabinet.** Things on the desk are usable now; everything else has to be fetched back on (a tool call, a file read) before the model can use it — and fetching it costs space. Good agent design keeps the desk clear: focused tool results, subagents that return short summaries instead of raw dumps, and important state written somewhere persistent.\n\nThis is also why a **SessionStart** context injection (the orientation banner) is deliberately kept small — it rides in *every* session's window, so it's a recurring tax on the budget.",
      "diagram": "flowchart TD\n  subgraph W[Context window · finite token budget]\n    S[System prompt + tool schemas]\n    P[Project memory · CLAUDE.md / AGENTS.md]\n    H[Conversation history]\n    F[Pulled-in files + tool results]\n  end\n  W --> M[Model reasons over ONLY what's in the window]\n  H --> FULL{Near the limit?}\n  FULL -- yes --> C[Harness compacts older turns to a summary]\n  C --> W\n  FULL -- no --> M\n  class S,P,H,F,M fact\n  class C built",
      "diagram_mini": "flowchart LR\n  C[context window] --> L{full?}\n  L -- yes --> S[compact to summary]\n  L -- no --> R[model reasons]\n  class C,R fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/context-window.svg",
      "svg_mini": "knowledge/concepts/visuals/context-window.mini.svg"
    },
    {
      "id": "subagents",
      "title": "Subagents & orchestration",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 4,
      "summary": "A lead agent can spawn focused subagents, each with its own clean context window, and get back a short summary — not the raw work. It's how big tasks stay inside finite context.",
      "see_also": [
        "context-window",
        "agent-harness-loop",
        "tool-use"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "Claude Code changes the subagent dispatch model, the Task/Agent tool shape, or how subagent results return to the parent.",
      "sources": [
        {
          "label": "Subagents — Claude Code",
          "url": "https://code.claude.com/docs/en/sub-agents"
        },
        {
          "label": "Building agents with the Agent SDK",
          "url": "https://code.claude.com/docs/en/agent-sdk"
        }
      ],
      "body_md": "A single agent doing a sprawling task hits two walls: its **context window fills** with the noise of exploration, and a long linear chain has no parallelism. **Subagents** solve both. The lead agent (the *orchestrator*) hands a self-contained slice of work to a **subagent** — a fresh agent with its **own clean context window**, its own tools, and a focused brief. The subagent does the legwork and returns a **short summary** to the lead. The raw files it read and dead-ends it explored never touch the lead's context.\n\nThis is the **orchestrator–worker** pattern, and the load-bearing idea is *information hiding*: the lead pays only for the conclusion, not the investigation. A research subagent might read forty files and reply with a two-paragraph answer — forty files' worth of tokens spent in a context the lead never has to carry.\n\nWhat it's good at, and where it bites:\n\n- **Fan-out.** Independent slices run in parallel — three subagents searching three areas at once, returning three summaries. Big wall-clock win when the slices don't depend on each other.\n- **Focus.** A narrowly-scoped subagent with the right tools beats one giant agent juggling everything, the same way a specialist beats a generalist.\n- **The summary is the seam — and the risk.** The lead sees *only* what the subagent reports back. A subagent that summarizes badly, or drops a detail the lead needed, silently loses that information. Briefs should say what to return, not just what to do.\n- **Subagents don't share memory with each other.** Each is isolated by design, so coordination happens through the lead, not sideways between workers.\n\nRavenClaude's Team Lead is exactly this pattern with named specialists; the command-review tribunal's parallel reviewer seats are another instance — several focused agents, one aggregated verdict.",
      "diagram": "flowchart TD\n  U[Your goal] --> L[Lead agent · orchestrator]\n  L --> A[Subagent A<br/>own context · own tools]\n  L --> B[Subagent B<br/>own context · own tools]\n  L --> C[Subagent C<br/>own context · own tools]\n  A --> SA[short summary]\n  B --> SB[short summary]\n  C --> SC[short summary]\n  SA --> L2[Lead integrates summaries]\n  SB --> L2\n  SC --> L2\n  L2 --> OUT[Result · lead never carried the raw work]\n  class L,L2,A,B,C fact\n  class OUT built",
      "diagram_mini": "flowchart LR\n  L[lead] --> S[subagents<br/>clean context] --> R[short summaries] --> L\n  class L,S,R fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/subagents.svg",
      "svg_mini": "knowledge/concepts/visuals/subagents.mini.svg"
    },
    {
      "id": "mcp",
      "title": "MCP: tools & data over a protocol",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 5,
      "summary": "The Model Context Protocol is a standard way to plug external tools and data into any agent. Write a server once; every MCP-aware client can use it — no per-app glue.",
      "see_also": [
        "tool-use",
        "agent-harness-loop"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "The MCP spec changes its primitives (tools/resources/prompts), transport set, or Claude Code changes how MCP servers are configured.",
      "sources": [
        {
          "label": "Model Context Protocol — specification",
          "url": "https://modelcontextprotocol.io"
        },
        {
          "label": "Connect to MCP servers — Claude Code",
          "url": "https://code.claude.com/docs/en/mcp"
        }
      ],
      "body_md": "Tool use lets a model call a tool — but who *supplies* the tools? If every integration (GitHub, a database, a ticketing system) had to be hand-wired into every agent, you'd rebuild the same connectors over and over. The **Model Context Protocol (MCP)** is the open standard that fixes this. It's often described as *\"a USB-C port for AI\"*: a single, uniform way for an agent to plug into outside tools and data.\n\nThe shape is **client ↔ server.** Your agent host (Claude Code, an IDE, a desktop app) is an **MCP client**. An **MCP server** is a small program that exposes capabilities over the protocol. Write the server once, and *any* MCP-aware client can use it — the glue is the protocol, not bespoke per-app code. Servers offer three kinds of primitive:\n\n- **Tools** — actions the model can call (the GitHub tools this very session uses are an MCP server).\n- **Resources** — data the agent can read (files, records, query results).\n- **Prompts** — reusable prompt templates the server provides.\n\nTwo things worth knowing:\n\n- **Servers run as separate processes** and connect over a transport (local stdio, or remote HTTP/SSE). A server may be on your machine or across the network — the client talks to both the same way.\n- **MCP tools are still just tool use.** Once connected, an MCP tool appears to the model exactly like any built-in tool: same request/result loop, same permission gating. MCP standardizes *where tools come from*, not *how the model calls them*. (In Claude Code a server can show as \"still connecting\" at startup — its tools are name-only until the schema loads, which is a connection state, not an absent capability.)",
      "diagram": "flowchart TD\n  H[Agent host · MCP client<br/>Claude Code · IDE · app] -->|protocol| S1[MCP server: GitHub]\n  H -->|protocol| S2[MCP server: database]\n  H -->|protocol| S3[MCP server: your own]\n  S1 --> P[Tools · Resources · Prompts]\n  S2 --> P\n  S3 --> P\n  P --> M[Model calls them like any tool<br/>same request/result loop]\n  class H,M fact\n  class S1,S2,S3 built",
      "diagram_mini": "flowchart LR\n  C[agent · client] -->|MCP| S[servers] --> T[tools · data]\n  class C,T fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/mcp.svg",
      "svg_mini": "knowledge/concepts/visuals/mcp.mini.svg"
    },
    {
      "id": "model-selection",
      "title": "Choosing a model",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 6,
      "summary": "Bigger isn't always better. Match the model to the job — a fast, cheap model for routine steps, a frontier model for hard reasoning — and use the strong one to supervise the cheap ones.",
      "see_also": [
        "agent-harness-loop",
        "subagents"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-05",
      "refresh_when": "The Claude model line-up (Opus / Sonnet / Haiku tiers) or the default-model guidance for agents changes.",
      "sources": [
        {
          "label": "Models overview — Claude API",
          "url": "https://docs.claude.com/en/docs/about-claude/models"
        },
        {
          "label": "Choosing a model — Claude Code",
          "url": "https://code.claude.com/docs/en/model-config"
        }
      ],
      "body_md": "An agent isn't locked to one model, and the strongest model isn't always the right one. Picking a model is a **cost / latency / capability** trade, and the three tiers exist precisely so you can match the model to the work:\n\n- **Frontier (e.g. Opus)** — deepest reasoning, hardest planning, gnarliest debugging. Slower and most expensive per token. Reach for it when the task genuinely needs it.\n- **Balanced (e.g. Sonnet)** — the everyday workhorse: strong reasoning at much lower cost and latency. The sensible default for most agent work.\n- **Fast (e.g. Haiku)** — quick, cheap, great for classification, routing, summarizing, and high-volume routine steps where frontier-grade reasoning is overkill.\n\nTwo ideas make this practical:\n\n- **Mix models within one task.** A capable orchestrator can plan and supervise while dispatching the routine legwork to a faster, cheaper model — *strong model supervises, cheap models execute.* Subagents are the natural place to drop a tier: the lead reasons in Sonnet/Opus, the workers grind in Haiku.\n- **Diversity is a safety property, not just a cost one.** When multiple agents *review* the same thing, running them all on the **same** model means one model's blind spot can pass the whole panel — the \"anti-correlated hallucination\" failure. Deliberately running ≥2 *different* backbones on a review panel catches what a monoculture would miss. (RavenClaude's command-review tribunal enforces exactly this.)\n\nThe wrong instinct is \"always use the biggest model.\" The right instinct is \"use the smallest model that reliably clears this specific bar,\" and let a stronger model check the cheaper ones' work.",
      "diagram": "flowchart TD\n  T[Task slice] --> Q{How hard?}\n  Q -- routine: classify / route / summarize --> FAST[Fast model · Haiku-class]\n  Q -- everyday reasoning + edits --> BAL[Balanced · Sonnet-class]\n  Q -- hardest reasoning + planning --> FRONT[Frontier · Opus-class]\n  FRONT --> SUP[Strong model supervises<br/>cheaper models execute]\n  REV[Review panel] --> DIV[Run ≥2 DIFFERENT models<br/>· avoid a blind-spot monoculture]\n  class FAST,BAL,FRONT,Q fact\n  class SUP,DIV built",
      "diagram_mini": "flowchart LR\n  J{job} -- routine --> F[fast]\n  J -- everyday --> B[balanced]\n  J -- hardest --> O[frontier]\n  class J,F,B,O fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/model-selection.svg",
      "svg_mini": "knowledge/concepts/visuals/model-selection.mini.svg"
    },
    {
      "id": "source-control-basics",
      "title": "Source control basics",
      "category": "Foundations",
      "kind": "platform-fact",
      "order": 7,
      "summary": "Git is a time-machine for your files. RavenClaude does most of the git work for you — this page is the vocabulary so you can follow along.",
      "see_also": [
        "getting-started",
        "layout-hook"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-04",
      "refresh_when": "Git or GitHub change a load-bearing default (branch name, PR flow), or the marketplace's PR conventions or branch-archive contract change.",
      "sources": [
        {
          "label": "AGENTS.md — PR conventions",
          "url": "AGENTS.md"
        },
        {
          "label": "AGENTS.md — House rules (branch-archive)",
          "url": "AGENTS.md"
        },
        {
          "label": "branch-archive skill",
          "url": "plugins/ravenclaude-core/skills/branch-archive/SKILL.md"
        },
        {
          "label": "Pro Git — About Version Control",
          "url": "https://git-scm.com/book/en/v2/Getting-Started-About-Version-Control"
        }
      ],
      "body_md": "**Source control** (the tool is called **git**, the website is **GitHub**) is a time-machine for every file in a project: every change is saved as a labeled snapshot, and many people can work on the same project in parallel without overwriting each other. The load-bearing fact for you: **RavenClaude does almost all of the git work for you** — this page is the vocabulary so you understand what Claude Code is doing on your behalf, and can step in when something goes sideways.\n\n**Four ideas cover almost everything you'll see:**\n\n- **Commit** — one saved snapshot of your work, with a short message explaining _why_ (not what — the diff already shows what).\n- **Branch** — a parallel timeline for a piece of work-in-progress, kept separate from the project's official timeline (the **main** branch).\n- **Pull request (PR)** — a proposal that the work on a branch be merged back into main; it gets reviewed (by a person, by CI checks, sometimes by Claude) before anything moves.\n- **Merge** — combining a branch's commits into main once the PR is approved.\n\n**The minimum vocabulary** you'll see in commands and logs:\n\n- `git status` — what's changed since the last snapshot\n- `git add <file>` — stage a file for the next snapshot\n- `git commit -m \"why\"` — take the snapshot\n- `git push` — upload local commits to GitHub\n- `git pull` — download new commits from GitHub\n- `git log` — read the history\n\n**Two patterns you'll actually encounter in this repo:**\n\n1. **Docs commit straight to main, no PR.** Pure documentation under `docs/` (plans, designs, research notes, the rolling session log) commits direct to main because it can't break a consumer's `/plugin marketplace update`. Faster planning loop.\n2. **Code, hooks, manifests, and CI go through a PR.** Claude Code creates the branch, makes the commits, opens the PR, and tells you the URL. You review in GitHub and click **Merge**.\n\n**The safety nets** mean you can experiment without breaking things:\n\n- The `guard-destructive.sh` hook blocks the truly destructive commands — **force-push to main**, `git reset --hard`, `git clean -f`, and `git branch -D` on a protected branch. It denies them before they run.\n- The sanctioned way to retire a finished branch is `scripts/archive-branch.sh` — it **tags the branch tip** (so the work is recoverable forever on the tag), pushes the tag to GitHub, writes an audit log, then deletes the local branch. Work is never actually lost.\n\nYou will rarely type any of these commands yourself. The reason to learn the vocabulary is so that when Claude says _\"I opened PR #238 on branch `feat/rc-hardener-followups`\"_ or _\"the guard denied a force-push\"_, you know exactly what happened and what your next click is.",
      "diagram": "flowchart TD\n  W[Your working files] -->|git add| S[Staging area]\n  S -->|git commit| C[Commit on a branch]\n  C -->|git push| GH[(GitHub: branch)]\n  GH -->|open PR| PR[Pull request · reviewed]\n  PR -->|merge| MAIN[(main branch · official timeline)]\n  D[Docs change under docs/] -->|commit + push| MAIN\n  X[git push --force to main<br/>git reset --hard<br/>rm -rf the repo] -.->|guard-destructive.sh| BLOCK[Blocked — safety net]\n  R[Retire a finished branch] -->|archive-branch.sh| TAG[Tag pushed · branch deleted · recoverable forever]\n  class C,PR,MAIN,TAG fact\n  class BLOCK,GH built",
      "diagram_mini": "flowchart LR\n  E[edit] --> C[commit] --> B[branch] --> P[PR] --> M[main]\n  class C,P,M fact",
      "steps": [
        {
          "caption": "Edit your working files — your changes in progress.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-1.svg"
        },
        {
          "caption": "git commit: save a labeled snapshot with a short 'why' message.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-2.svg"
        },
        {
          "caption": "Branch: keep work in progress on its own timeline, separate from main.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-3.svg"
        },
        {
          "caption": "git push: upload the branch's commits to GitHub.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-4.svg"
        },
        {
          "caption": "Open a PR: propose merging the branch; reviewed by people, CI, sometimes Claude.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-5.svg"
        },
        {
          "caption": "Merge into main once approved. RavenClaude does most of this for you.",
          "diagram": "flowchart LR\n  N1[Edit] --> N2[Commit] --> N3[Branch] --> N4[Push] --> N5[PR] --> N6[Merge]\n  class N6 built",
          "svg": "knowledge/concepts/visuals/source-control-basics.step-6.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/source-control-basics.svg",
      "svg_mini": "knowledge/concepts/visuals/source-control-basics.mini.svg"
    },
    {
      "id": "getting-started",
      "title": "Getting started",
      "category": "Getting started",
      "kind": "ravenclaude-built",
      "order": 5,
      "summary": "Add the marketplace, install ravenclaude-core, run /dashboard. Needs jq + python3; under Copilot CLI it's `ravenclaude install`.",
      "see_also": [
        "comfort-posture",
        "capability-banner"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The install steps, requirements, or slash-command surface change.",
      "sources": [
        {
          "label": "ravenclaude-core README",
          "url": "plugins/ravenclaude-core/README.md"
        },
        {
          "label": "AGENTS.md — Setup commands",
          "url": "AGENTS.md"
        }
      ],
      "body_md": "RavenClaude is a Claude Code **plugin marketplace**. To use the core plugin: add the marketplace, install the plugin, reload. `jq` and `python3` are required for the CI workflows and the layout-enforcement hook (both ship in the devcontainer).\n\n```text\n/plugin marketplace add mcorbett51090/RavenClaude\n/plugin install ravenclaude-core@ravenclaude\n/reload-plugins\n```\n\nAfter install, run **`/dashboard`** to launch the comfort-posture editor (point-and-click permission rules + command-review toggles, with one-click Save & apply). Domain plugins (`power-platform`, `finance`, …) build on core, so install it first. Running under **GitHub Copilot CLI** instead? Use `bash scripts/ravenclaude install` — and from then on, updating is just `git pull`.",
      "diagram": "flowchart TD\n  A[/plugin marketplace add …/] --> B[/plugin install ravenclaude-core/]\n  B --> C[/reload-plugins/]\n  C --> D[Run /dashboard]\n  D --> E[Set comfort posture · opt into command review]\n  E --> F[Start working — capability banner orients each session]\n  class A,B,C,D,E,F built",
      "diagram_mini": "flowchart LR\n  I[install] --> D[\"/dashboard\"] --> P[set posture]\n  class I,D,P built",
      "steps": [
        {
          "caption": "Add the marketplace: /plugin marketplace add mcorbett51090/RavenClaude.",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-1.svg"
        },
        {
          "caption": "Install the core plugin: /plugin install ravenclaude-core@ravenclaude (needs jq + python3).",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-2.svg"
        },
        {
          "caption": "Reload plugins so the agents, skills, and hooks register.",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-3.svg"
        },
        {
          "caption": "Run /dashboard to open the point-and-click comfort-posture editor.",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-4.svg"
        },
        {
          "caption": "Set your deny/ask/allow posture and opt into command review.",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-5.svg"
        },
        {
          "caption": "Start working — the capability banner orients every session.",
          "diagram": "flowchart LR\n  N1[Add] --> N2[Install] --> N3[Reload] --> N4[Dashboard] --> N5[Posture] --> N6[Work]\n  class N6 built",
          "svg": "knowledge/concepts/visuals/getting-started.step-6.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/getting-started.svg",
      "svg_mini": "knowledge/concepts/visuals/getting-started.mini.svg"
    },
    {
      "id": "permission-layers",
      "title": "Permission layers & precedence",
      "category": "Platform model",
      "kind": "platform-fact",
      "order": 10,
      "summary": "Four settings files merge — a deny in ANY layer wins, and you can't override it down from a later layer.",
      "see_also": [
        "hook-lifecycle",
        "command-review-tribunal"
      ],
      "widget": "permission-resolver",
      "try_it": {
        "label": "Set your own layers on the Settings tab",
        "href": "#/settings"
      },
      "node_links": {},
      "last_verified": "2026-05-25",
      "refresh_when": "Anthropic adds/removes a settings layer, or changes the cross-layer merge rule.",
      "sources": [
        {
          "label": "Configure permissions",
          "url": "https://code.claude.com/docs/en/permissions"
        },
        {
          "label": "Claude Code settings",
          "url": "https://code.claude.com/docs/en/settings"
        }
      ],
      "body_md": "Permission rules live under `permissions.allow`, `permissions.ask`, and `permissions.deny` in any of the settings files. Each rule is either a bare tool name (`Bash`, `WebSearch`) or a tool plus a specifier (`Bash(git status:*)`, `Read(/etc/**)`).\n\n**Within one file**, rules evaluate `deny → ask → allow` — the first match wins, so deny always beats ask and ask always beats allow.\n\n**Across files**, the layers **merge** rather than override: a deny in *any* layer blocks the action regardless of allow rules elsewhere. You **cannot override down** — if your user-level settings deny `Bash(rm *)`, no project-level allow re-enables it. That's the safe behavior, but it surprises people who expect a later layer to win.\n\nThe most-surprising rule: a **bare-tool deny** (`deny: [\"Bash\"]`) removes the tool from Claude's context entirely — Claude never sees it. A **scoped deny** (`Bash(rm *)`) keeps the tool and blocks only matching calls.",
      "diagram": "flowchart TD\n  CALL[Tool call] --> MERGE[Merge rules across all layers]\n  MERGE --> D{Any layer<br/>DENY match?}\n  D -- yes --> BLOCK[Blocked — cannot be overridden]\n  D -- no --> A{Any layer<br/>ASK match?}\n  A -- yes --> PROMPT[Prompt the user]\n  A -- no --> AL{Any layer<br/>ALLOW match?}\n  AL -- yes --> RUN[Run without asking]\n  AL -- no --> PROMPT\n  class BLOCK built\n  class D,A,AL fact",
      "diagram_mini": "flowchart LR\n  C[Call] --> D{deny<br/>anywhere?}\n  D -- yes --> X[Blocked]\n  D -- no --> OK[allow / ask]\n  class X built",
      "steps": [
        {
          "caption": "Rules live in four settings files under permissions.allow / ask / deny.",
          "diagram": "flowchart LR\n  N1[Rules in 4 files] --> N2[Merge layers] --> N3[Deny wins] --> N4[No override down] --> N5[Bare vs scoped]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/permission-layers.step-1.svg"
        },
        {
          "caption": "Across files the layers MERGE rather than override.",
          "diagram": "flowchart LR\n  N1[Rules in 4 files] --> N2[Merge layers] --> N3[Deny wins] --> N4[No override down] --> N5[Bare vs scoped]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/permission-layers.step-2.svg"
        },
        {
          "caption": "A deny in ANY layer wins — and you can't override it down from a later layer.",
          "diagram": "flowchart LR\n  N1[Rules in 4 files] --> N2[Merge layers] --> N3[Deny wins] --> N4[No override down] --> N5[Bare vs scoped]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/permission-layers.step-3.svg"
        },
        {
          "caption": "Within one file the order is deny then ask then allow; first match wins.",
          "diagram": "flowchart LR\n  N1[Rules in 4 files] --> N2[Merge layers] --> N3[Deny wins] --> N4[No override down] --> N5[Bare vs scoped]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/permission-layers.step-4.svg"
        },
        {
          "caption": "A bare-tool deny hides the tool entirely; a scoped deny blocks only matching calls.",
          "diagram": "flowchart LR\n  N1[Rules in 4 files] --> N2[Merge layers] --> N3[Deny wins] --> N4[No override down] --> N5[Bare vs scoped]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/permission-layers.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/permission-layers.svg",
      "svg_mini": "knowledge/concepts/visuals/permission-layers.mini.svg"
    },
    {
      "id": "permission-modes",
      "title": "Permission modes",
      "category": "Platform model",
      "kind": "platform-fact",
      "order": 15,
      "summary": "Six modes from default to bypassPermissions — but bypass still prompts on rm -rf /, and auto silently drops broad allow rules.",
      "see_also": [
        "permission-layers",
        "hook-lifecycle"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-25",
      "refresh_when": "Anthropic adds/renames a permission mode or changes auto-mode's drop-broad-rules behavior.",
      "sources": [
        {
          "label": "Choose a permission mode",
          "url": "https://code.claude.com/docs/en/permission-modes"
        },
        {
          "label": "Configure permissions",
          "url": "https://code.claude.com/docs/en/permissions"
        }
      ],
      "body_md": "The permission **mode** sets the baseline posture on top of your allow/ask/deny rules. There are six: `default` (prompt when uncertain), `acceptEdits` (auto-approve common filesystem Bash inside the cwd), `plan` (read/think only — no writes), `auto` (research-preview classifier-driven autonomy), `dontAsk` (auto-deny anything not explicitly allowed — handy for CI), and `bypassPermissions` (skips most checks).\n\nTwo surprises bite. **`bypassPermissions` is not a total kill-switch:** `rm -rf /` and `rm -rf ~` *still* prompt as a circuit breaker, and a `PreToolUse` hook `deny` still blocks. **`auto` mode silently drops broad allow rules** — `Bash(*)`, wildcarded interpreters, `npm run *`, and all `Agent` allows are dropped while it's active (they restore when you leave), and `defaultMode: \"auto\"` is *ignored* in project settings — it must live in user-level `~/.claude/settings.json`.",
      "diagram": "flowchart TD\n  M[Permission mode] --> DEF[default · prompt on uncertain]\n  M --> AE[acceptEdits · auto-approve cwd file ops]\n  M --> AUTO[auto · classifier autonomy<br/>drops broad allow rules]\n  M --> BP[bypassPermissions · skips most checks]\n  BP --> CB{rm -rf / or ~ ?}\n  CB -- yes --> STOP[still prompts · circuit breaker]\n  CB -- no --> RUN[runs]\n  class M,DEF,AE,AUTO,BP fact\n  class STOP built",
      "diagram_mini": "flowchart LR\n  D[default] --> A[acceptEdits] --> AU[auto] --> B[bypass]\n  B --> S[rm -rf / still blocks]\n  class S built",
      "steps": [
        {
          "caption": "The mode sets the baseline posture on top of your allow/ask/deny rules.",
          "diagram": "flowchart LR\n  N1[Mode baseline] --> N2[Default modes] --> N3[Auto drops allows] --> N4[DontAsk] --> N5[Bypass still blocks]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/permission-modes.step-1.svg"
        },
        {
          "caption": "default prompts on uncertain; acceptEdits auto-approves cwd file ops; plan is read-only.",
          "diagram": "flowchart LR\n  N1[Mode baseline] --> N2[Default modes] --> N3[Auto drops allows] --> N4[DontAsk] --> N5[Bypass still blocks]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/permission-modes.step-2.svg"
        },
        {
          "caption": "auto is classifier autonomy — but it silently DROPS broad allow rules like Bash star.",
          "diagram": "flowchart LR\n  N1[Mode baseline] --> N2[Default modes] --> N3[Auto drops allows] --> N4[DontAsk] --> N5[Bypass still blocks]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/permission-modes.step-3.svg"
        },
        {
          "caption": "dontAsk auto-denies anything not explicitly allowed (handy for CI).",
          "diagram": "flowchart LR\n  N1[Mode baseline] --> N2[Default modes] --> N3[Auto drops allows] --> N4[DontAsk] --> N5[Bypass still blocks]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/permission-modes.step-4.svg"
        },
        {
          "caption": "bypassPermissions skips most checks — but rm -rf / still prompts and a hook deny still blocks.",
          "diagram": "flowchart LR\n  N1[Mode baseline] --> N2[Default modes] --> N3[Auto drops allows] --> N4[DontAsk] --> N5[Bypass still blocks]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/permission-modes.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/permission-modes.svg",
      "svg_mini": "knowledge/concepts/visuals/permission-modes.mini.svg"
    },
    {
      "id": "hook-lifecycle",
      "title": "Hooks: verdicts & exit codes",
      "category": "Platform model",
      "kind": "platform-fact",
      "order": 20,
      "summary": "Only exit 2 blocks a tool call; a hook deny beats bypass mode, but a hook allow can't override a settings deny.",
      "see_also": [
        "permission-layers",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-25",
      "refresh_when": "Anthropic changes hook exit-code semantics, the hookSpecificOutput schema, or timeout behavior.",
      "sources": [
        {
          "label": "Hooks reference",
          "url": "https://code.claude.com/docs/en/hooks"
        },
        {
          "label": "Hooks guide",
          "url": "https://code.claude.com/docs/en/hooks-guide"
        }
      ],
      "body_md": "A `PreToolUse` hook reads the pending tool call as JSON on stdin and decides its fate. **Exit codes are the load-bearing, easy-to-get-wrong detail:** only **exit 2** blocks (and the hook's stderr is fed back to the model); **exit 0** allows; and **exit 1 or any other code is a *non-blocking* error — the tool still runs.** The trap is that `exit 1` is the conventional Unix \"failure\", so a policy hook that fails with `exit 1` *looks* like it blocked but doesn't.\n\nFor richer control, a hook can instead print a `hookSpecificOutput.permissionDecision` JSON on **exit 0**: `allow`, `deny`, `ask`, or `defer` (headless-only). When several hooks and rules apply, priority is **`deny` > `defer` > `ask` > `allow`**.\n\nTwo asymmetries make this safe: a hook **`deny` beats permission-mode bypass** (it blocks even under `bypassPermissions`), but a hook **`allow` does NOT override a settings `deny`** — hooks can *tighten* but never *loosen*. Note hooks **fail open**: on timeout or crash the tool proceeds, so a hook that must fail closed has to emit its own `deny` before its deadline.",
      "diagram": "flowchart TD\n  H[PreToolUse hook] --> EX{exit code?}\n  EX -- \"exit 2\" --> BLOCK[Blocked · stderr fed to model]\n  EX -- \"exit 1 / other\" --> NB[Non-blocking error · tool still RUNS]\n  EX -- \"exit 0\" --> J{JSON permissionDecision?}\n  J -- deny --> DEN[Denied · beats bypass mode]\n  J -- ask --> ASK[User prompted]\n  J -- \"allow / none\" --> ALL[Prompt skipped · settings deny still wins]\n  class H,EX,J,NB,ASK,ALL fact\n  class BLOCK,DEN built",
      "diagram_mini": "flowchart LR\n  E{exit?} -- \"2\" --> B[blocks]\n  E -- \"0\" --> J[JSON verdict]\n  E -- \"1\" --> R[runs anyway]\n  class B built",
      "steps": [
        {
          "caption": "A PreToolUse hook reads the pending tool call as JSON on stdin.",
          "diagram": "flowchart LR\n  N1[Read stdin] --> N2[Exit 2 blocks] --> N3[Exit 1 runs] --> N4[Exit 0 verdict] --> N5[Tighten only]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/hook-lifecycle.step-1.svg"
        },
        {
          "caption": "Exit 2 blocks the call — and the hook's stderr is fed back to the model.",
          "diagram": "flowchart LR\n  N1[Read stdin] --> N2[Exit 2 blocks] --> N3[Exit 1 runs] --> N4[Exit 0 verdict] --> N5[Tighten only]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/hook-lifecycle.step-2.svg"
        },
        {
          "caption": "Exit 1 (or any other code) is a non-blocking error: the tool still RUNS. The trap.",
          "diagram": "flowchart LR\n  N1[Read stdin] --> N2[Exit 2 blocks] --> N3[Exit 1 runs] --> N4[Exit 0 verdict] --> N5[Tighten only]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/hook-lifecycle.step-3.svg"
        },
        {
          "caption": "Exit 0 with a permissionDecision JSON: allow / deny / ask (priority deny > defer > ask > allow).",
          "diagram": "flowchart LR\n  N1[Read stdin] --> N2[Exit 2 blocks] --> N3[Exit 1 runs] --> N4[Exit 0 verdict] --> N5[Tighten only]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/hook-lifecycle.step-4.svg"
        },
        {
          "caption": "Hooks only tighten: a deny beats bypass mode, but an allow can't override a settings deny.",
          "diagram": "flowchart LR\n  N1[Read stdin] --> N2[Exit 2 blocks] --> N3[Exit 1 runs] --> N4[Exit 0 verdict] --> N5[Tighten only]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/hook-lifecycle.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/hook-lifecycle.svg",
      "svg_mini": "knowledge/concepts/visuals/hook-lifecycle.mini.svg"
    },
    {
      "id": "session-start-context",
      "title": "SessionStart context injection",
      "category": "Platform model",
      "kind": "platform-fact",
      "order": 22,
      "summary": "SessionStart hooks inject additionalContext into every session — additive only; they can't block or delay startup and are capped near 10k chars.",
      "see_also": [
        "hook-lifecycle",
        "capability-banner"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {
        "H": "hook-lifecycle"
      },
      "last_verified": "2026-05-26",
      "refresh_when": "Anthropic changes the SessionStart additionalContext field, its size cap, or matcher set.",
      "sources": [
        {
          "label": "Hooks reference",
          "url": "https://code.claude.com/docs/en/hooks"
        }
      ],
      "body_md": "`PreToolUse` hooks gate tool calls; **`SessionStart` hooks can't gate anything.** Their job is to add text to the session via a different field — `hookSpecificOutput.additionalContext` — and nothing more. The output is read **only on exit 0**; a non-zero exit is a non-blocking error and the session still starts. A SessionStart hook can never block or delay a session; its output is purely additive.\n\nRules that bite: `additionalContext` is **capped near ~10,000 characters** (it's injected every session, so it's a recurring token cost — keep it tight); **multiple SessionStart hooks run in parallel and their outputs are concatenated**; the optional `matcher` is `startup` / `resume` / `clear` / `compact`; and like other hooks it **fails open** on timeout. This is the mechanism RavenClaude's capability banner rides on.",
      "diagram": "flowchart TD\n  S[Session starts] --> H[SessionStart hooks run in parallel]\n  H --> EX{exit 0?}\n  EX -- no --> SKIP[Non-blocking error · session starts anyway]\n  EX -- yes --> ADD[additionalContext concatenated into context]\n  ADD --> CAP[~10k char cap · injected every session]\n  CAP --> GO[Session proceeds · never blocked]\n  SKIP --> GO\n  class S,H,EX,SKIP,GO fact\n  class ADD,CAP built",
      "diagram_mini": "flowchart LR\n  S[SessionStart] --> A[additionalContext]\n  A --> C[added to context<br/>· can't block]\n  class A built",
      "steps": [
        {
          "caption": "A session starts; SessionStart hooks run in parallel.",
          "diagram": "flowchart LR\n  N1[Session starts] --> N2[Hooks parallel] --> N3[Exit 0 only] --> N4[Context added] --> N5[10k cap]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/session-start-context.step-1.svg"
        },
        {
          "caption": "They can't gate anything — output is read only on exit 0.",
          "diagram": "flowchart LR\n  N1[Session starts] --> N2[Hooks parallel] --> N3[Exit 0 only] --> N4[Context added] --> N5[10k cap]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/session-start-context.step-2.svg"
        },
        {
          "caption": "A non-zero exit is a non-blocking error; the session starts anyway.",
          "diagram": "flowchart LR\n  N1[Session starts] --> N2[Hooks parallel] --> N3[Exit 0 only] --> N4[Context added] --> N5[10k cap]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/session-start-context.step-3.svg"
        },
        {
          "caption": "Each hook's additionalContext is concatenated into the session context.",
          "diagram": "flowchart LR\n  N1[Session starts] --> N2[Hooks parallel] --> N3[Exit 0 only] --> N4[Context added] --> N5[10k cap]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/session-start-context.step-4.svg"
        },
        {
          "caption": "Capped near 10k chars (injected every session); fails open on timeout.",
          "diagram": "flowchart LR\n  N1[Session starts] --> N2[Hooks parallel] --> N3[Exit 0 only] --> N4[Context added] --> N5[10k cap]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/session-start-context.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/session-start-context.svg",
      "svg_mini": "knowledge/concepts/visuals/session-start-context.mini.svg"
    },
    {
      "id": "cloudflare-what-runs-your-site",
      "title": "What actually runs your site on Cloudflare",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 15,
      "summary": "Static files are served without running any code; a Worker is code that runs per request. A Pages Function IS a Worker. Workers are not Node — that is what breaks libraries.",
      "see_also": [
        "cloudflare-where-data-lives",
        "cloudflare-who-gets-in"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Cloudflare changes the Pages-vs-Workers feature split (the migration guide's two comparison tables), announces a Pages deprecation or migration mandate, changes the free/paid CPU-time or script-size limits, or moves a node: module out of the non-functional-stub list.",
      "sources": [
        {
          "label": "Cloudflare Docs — Static assets on Workers",
          "url": "https://developers.cloudflare.com/workers/static-assets/"
        },
        {
          "label": "Cloudflare Docs — Static assets: billing and limitations",
          "url": "https://developers.cloudflare.com/workers/static-assets/billing-and-limitations/"
        },
        {
          "label": "Cloudflare Docs — Pages Functions",
          "url": "https://developers.cloudflare.com/pages/functions/"
        },
        {
          "label": "Cloudflare Docs — Migrate from Pages to Workers",
          "url": "https://developers.cloudflare.com/workers/static-assets/migration-guides/migrate-from-pages/"
        },
        {
          "label": "Cloudflare Docs — How Workers works",
          "url": "https://developers.cloudflare.com/workers/reference/how-workers-works/"
        },
        {
          "label": "Cloudflare Docs — Node.js compatibility",
          "url": "https://developers.cloudflare.com/workers/runtime-apis/nodejs/"
        },
        {
          "label": "Cloudflare Docs — Workers limits",
          "url": "https://developers.cloudflare.com/workers/platform/limits/"
        }
      ],
      "body_md": "**The question to answer first: does anything have to _run_ when a visitor asks for this URL?**\n\n- **No** — the HTML, CSS, JS and images were built ahead of time. They are **static assets**. Cloudflare serves them straight off its network, and per the docs: _\"If a requested URL matches a file in the static assets directory, that file will be served — without invoking Worker code.\"_ Those requests cost nothing: _\"Requests to static assets are free and unlimited.\"_\n- **Yes** — a session must be checked, a database read, an API called, a page rendered from live data. That is a **Worker**: your code, executed once per request, before a response exists.\n\n**Pages and Workers are not two different runtimes.** A **Pages Function _is_ a Worker** — the docs define Functions as \"executing code on the Cloudflare network with Cloudflare Workers.\" And Workers serve static assets natively: `wrangler` uploads your build output and \"deploys both your Worker code and your static assets in a single operation.\" So one Workers project can be the entire site — the static files _and_ the handful of routes that need code.\n\n**Which one, then?** Cloudflare's own migration guide does **not** deprecate Pages, and it does not tell you to move. It publishes a feature split, and that split is the real decision:\n\n| Only on Workers | Only on Pages |\n| --- | --- |\n| Durable Objects, Cron Triggers, Queue consumers, Email Workers | File-based routing with no framework adapter |\n| Workers Logs, Logpush, Tail Workers, source maps | Early Hints (a workaround exists on Workers) |\n| Gradual deployments, remote development, the Vite plugin | Custom domains outside your Cloudflare zones |\n| Non-root routes, rate limiting, image resizing | Serving assets on a path without a workaround |\n\nPractical read: **start on Workers unless you specifically want Pages' file-based routing or need a custom domain on a zone you don't run.** Everything a growing app reaches for later — a durable coordination object, a cron job, a queue consumer, real logs — is on the Workers side of that table.\n\n**What \"edge\" actually means.** Not a metaphor: your code is deployed to _\"a growing global network of thousands of machines distributed across hundreds of locations,\"_ and _\"each of these machines hosts an instance of the Workers runtime.\"_ A request is handled by whichever machine took the connection. The consequence people miss: your **compute** is everywhere, but your **data** usually isn't — a Worker in Sydney hitting a database in Virginia is still a trip to Virginia.\n\n**Cold starts.** Workers don't run a container per tenant; they run V8 **isolates**, and paying the runtime's startup cost once per machine _\"eliminates the cold starts of the virtual machine model.\"_ An isolate _\"can start around a hundred times faster than a Node process on a container or virtual machine.\"_ Stop designing around warm-up — there is nothing to keep warm.\n\n**Workers is not Node, and this is what breaks your libraries.** There is no Node runtime underneath. You opt into Node APIs with the `nodejs_compat` compatibility flag and a compatibility date of **2024-09-23 or later**; with it, much of the standard library really works — `node:crypto`, `Buffer`, streams, `path`, `http`/`https`, `net`, `AsyncLocalStorage`, `zlib`. What still breaks:\n\n- **Non-functional stubs** — `node:worker_threads`, `node:cluster`, `node:vm`, `node:http2` import fine and then do nothing. A library that reaches for them fails at runtime, not at build time.\n- **Anything assuming a long-lived process** — a background thread, an in-process job queue, a connection pool that outlives the request, a native addon.\n- **No flag, no Node.** Without `nodejs_compat` set, `node:` imports don't resolve at all.\n\n**The limits that actually bite.** **CPU time**, not wall-clock, is the ceiling: 10 ms per request on Free, 30 s by default on Paid (raisable to 5 min). Waiting on a database or an API isn't CPU time, and an HTTP request has \"no limit\" on duration while the client stays connected. Memory is **128 MB** per isolate. A script is capped at **3 MB** compressed on Free, **10 MB** on Paid — which is what a bundled headless-CMS SDK runs into.",
      "diagram": "flowchart TD\n  R[Request for a URL] --> M{Does a built file<br/>match this path?}\n  M -- yes --> A[Static asset served<br/>no code runs · free and unlimited]\n  M -- no --> W[Your Worker runs<br/>a Pages Function IS a Worker]\n  W --> N{Needs a Node<br/>runtime feature?}\n  N -- worker_threads · cluster · vm · http2 --> X[Non-functional stub · breaks at runtime]\n  N -- crypto · Buffer · streams · path --> OK[Works with nodejs_compat<br/>compat date 2024-09-23+]\n  W --> L[Budget: 128 MB memory<br/>CPU 10 ms free · 30 s paid]\n  class M,N fact\n  class A,W,X,OK,L built",
      "diagram_mini": "flowchart LR\n  U[request] --> F{file exists?}\n  F -- yes --> S[static · free]\n  F -- no --> C[Worker code runs]\n  class F fact\n  class S,C built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/cloudflare-what-runs-your-site.svg",
      "svg_mini": "knowledge/concepts/visuals/cloudflare-what-runs-your-site.mini.svg"
    },
    {
      "id": "cloudflare-where-data-lives",
      "title": "Where Cloudflare keeps your data",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 16,
      "summary": "Five stores, one question each: rows you query = D1, files = R2 (egress is free), read-mostly config = KV (up to 60s stale), coordination = Durable Objects, work to hand off = Queues.",
      "see_also": [
        "cloudflare-what-runs-your-site",
        "cloudflare-who-gets-in"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "R2 starts charging egress or changes the $0.015/GB-month or Class A/B prices, D1 raises the 10 GB per-database cap or stops processing queries one at a time, KV's stated 60-second propagation window changes, or Queues changes its free allocation.",
      "sources": [
        {
          "label": "Cloudflare Docs — D1 overview",
          "url": "https://developers.cloudflare.com/d1/"
        },
        {
          "label": "Cloudflare Docs — D1 limits",
          "url": "https://developers.cloudflare.com/d1/platform/limits/"
        },
        {
          "label": "Cloudflare Docs — R2 pricing",
          "url": "https://developers.cloudflare.com/r2/pricing/"
        },
        {
          "label": "Cloudflare Docs — How KV works",
          "url": "https://developers.cloudflare.com/kv/concepts/how-kv-works/"
        },
        {
          "label": "Cloudflare Docs — Durable Objects overview",
          "url": "https://developers.cloudflare.com/durable-objects/"
        },
        {
          "label": "Cloudflare Docs — Durable Objects in-memory state",
          "url": "https://developers.cloudflare.com/durable-objects/reference/in-memory-state/"
        },
        {
          "label": "Cloudflare Docs — Queues pricing",
          "url": "https://developers.cloudflare.com/queues/platform/pricing/"
        }
      ],
      "body_md": "Five stores, and the choice is almost never close once you name the shape of the data. Ask the question in the left column; the answer picks the product.\n\n| What you have | Store | The fact that decides it |\n| --- | --- | --- |\n| Rows you want to query and join | **D1** | SQLite semantics; 10 GB per database (Paid), 500 MB (Free) |\n| Files: uploads, images, backups, exports | **R2** | **Egress is free** — the whole reason to leave S3 |\n| Read-mostly config, flags, cached responses | **KV** | Writes can take **60 s or more** to show up elsewhere |\n| Many clients that must agree on one state | **Durable Objects** | One active instance per ID handles every request for it |\n| Work that shouldn't happen in the request | **Queues** | Guaranteed delivery, batching, retries, dead-letter queue |\n\n### D1 — rows you query\n\n_\"D1 is Cloudflare's managed, serverless database with SQLite's SQL semantics.\"_ Real SQL, real joins, real constraints, queried from a Worker binding or over HTTP. Limits worth knowing before you design: **10 GB per database** on Workers Paid (500 MB Free), **1 TB total** per account (5 GB Free), **50,000 databases** (10 on Free), 30-second query duration, 2 MB max row.\n\nThe load-bearing one is not a size: _\"Each individual D1 database is inherently single-threaded, and processes queries one at a time.\"_ That is why there is no published rows-per-second figure — throughput is just one over your query duration. A slow query doesn't only hurt its own caller; it holds up everything behind it. The intended answer to a write-heavy workload is **more databases**, not a bigger one — Cloudflare specifically notes you can \"create thousands of databases at no extra cost for isolation,\" so a per-tenant or per-shard database is the sanctioned pattern.\n\n### R2 — files, and the egress story\n\nObject storage, S3-API-compatible, and the differentiator is one line: **\"Egress (data transfer to Internet)\" is \"Free.\"** No per-GB bandwidth bill for serving what you stored. That single fact is what makes user-uploaded media, public downloads, dataset hosting and video segments viable at a price S3 can't match, and it is the reason to migrate even when nothing else about R2 is better for you.\n\nYou pay for storage and operations instead: **$0.015/GB-month** standard ($0.01 for Infrequent Access), **Class A $4.50/million** (writes and lists — `PutObject`, `ListObjects`, `CopyObject`, multipart), **Class B $0.36/million** (reads — `GetObject`, `HeadObject`). Deletes are free. The free tier is 10 GB-month plus 1M Class A and 10M Class B operations per month. The practical shape: **writes cost 12× what reads cost**, so batch uploads and stop listing a bucket on every page load.\n\n### KV — fast reads, and the trap\n\nKV _\"achieves high performance by being eventually-consistent.\"_ A write is usually visible immediately where you made it, but _\"changes may take up to 60 seconds or more to be visible in other global network locations.\"_ Reads are additionally cached with a `cacheTtl` defaulting to 60 seconds, and negative lookups are cached too — so a key you just created can keep reading as missing.\n\n**Never put anything needing read-after-write in KV.** Not a user's just-saved profile, not a session that a redirect immediately reads, not a counter, not a lock. Cloudflare says it plainly: KV is wrong for workloads needing _\"atomic operations or where values must be read and written in a single transaction,\"_ and wrong for write-heavy keys updated _\"tens or hundreds of times per second.\"_ It is right for feature flags, routing tables, config, and cached API responses — things written rarely and read everywhere.\n\n### Durable Objects — one place where truth lives\n\n_\"Each Durable Object has a globally-unique name, which allows you to send requests to a specific object from anywhere in the world\"_ and — the property everything else rests on — _\"each Durable Object has one active instance at any particular time. All requests sent to that Durable Object are handled by that same instance.\"_ That is the coordination primitive: give a chat room, a document, a game match, or a rate-limit bucket its own ID and every client converges on one authority instead of racing.\n\nIts storage is _\"durable, transactional, and strongly consistent\"_ (up to 10 GB per object), and the SQLite storage backend exposes both SQL and key-value APIs. The mistake to avoid: in-memory variables are a cache, not state — _\"because in-memory state is not preserved across eviction or hibernation, persist anything important to storage.\"_\n\n### Queues — take it out of the request path\n\n_\"Send and receive messages with guaranteed delivery,\"_ with batching, delays, retries and a dead-letter queue for repeated failures. Reach for it when the user shouldn't wait: send the email, resize the image, sync the CRM, rebuild the index. No egress charges. Free plan gets 10,000 operations/day; Paid is $0.40/million with 1M included, where **an operation is each 64 KB written, read, or deleted** — so a 200 KB message is four operations on each hop, not one.",
      "diagram": "flowchart TD\n  Q{What shape is it?} -- rows you query --> D1[D1 · SQLite<br/>one query at a time per DB<br/>10 GB · shard by tenant]\n  Q -- a file --> R2[R2 · object storage<br/>EGRESS FREE · $0.015 per GB-month]\n  Q -- config read everywhere --> KV[KV · eventually consistent<br/>up to 60 s to propagate]\n  Q -- clients must agree --> DO[Durable Objects<br/>one instance per ID · strongly consistent]\n  Q -- work to defer --> QU[Queues · guaranteed delivery<br/>batch · retry · dead-letter]\n  KV --> W[Needs read-after-write?<br/>then NOT KV · use D1 or a Durable Object]\n  class Q fact\n  class D1,R2,KV,DO,QU,W built",
      "diagram_mini": "flowchart LR\n  D[rows] --> D1[D1]\n  F[files] --> R2[R2 · free egress]\n  C[config] --> KV[KV · 60s stale]\n  S[shared state] --> DO[Durable Object]\n  class D,F,C,S fact\n  class D1,R2,KV,DO built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/cloudflare-where-data-lives.svg",
      "svg_mini": "knowledge/concepts/visuals/cloudflare-where-data-lives.mini.svg"
    },
    {
      "id": "cloudflare-who-gets-in",
      "title": "Who Cloudflare lets in",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 17,
      "summary": "Three products, three different questions: Access asks who you are, Turnstile asks whether a visitor is a human, the WAF asks whether a request is malicious. They stack; none substitutes.",
      "see_also": [
        "cloudflare-what-runs-your-site",
        "cloudflare-where-data-lives"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Turnstile changes the 300-second token lifetime, single-use replay rule, or starts supporting wildcards in the hostname field; Access changes its deny-by-default posture or policy building blocks; or Cloudflare moves WAF Managed Rules onto or off the Free plan.",
      "sources": [
        {
          "label": "Cloudflare Docs — Access policies",
          "url": "https://developers.cloudflare.com/cloudflare-one/policies/access/"
        },
        {
          "label": "Cloudflare Docs — Cloudflare One / Zero Trust",
          "url": "https://developers.cloudflare.com/cloudflare-one/"
        },
        {
          "label": "Cloudflare Docs — Turnstile: get started",
          "url": "https://developers.cloudflare.com/turnstile/get-started/"
        },
        {
          "label": "Cloudflare Docs — Turnstile server-side validation",
          "url": "https://developers.cloudflare.com/turnstile/get-started/server-side-validation/"
        },
        {
          "label": "Cloudflare Docs — Turnstile hostname management",
          "url": "https://developers.cloudflare.com/turnstile/concepts/hostname-management/"
        },
        {
          "label": "Cloudflare Docs — Cloudflare WAF",
          "url": "https://developers.cloudflare.com/waf/"
        }
      ],
      "body_md": "These three get confused constantly, and the confusion is expensive: people put a CAPTCHA in front of an admin panel that needed a login, or expect a firewall rule to stop credential stuffing from a real browser. Each answers a **different question**, and each has a different unit it judges.\n\n| Product | The question it answers | What it judges |\n| --- | --- | --- |\n| **Access** (Cloudflare One / Zero Trust) | *Who are you, and are you allowed in?* | A **person**, against an identity provider |\n| **Turnstile** | *Is this visitor a human?* | A **visitor** on a page, at one moment |\n| **WAF** | *Is this request trying to hurt me?* | A single **HTTP request** |\n\n### Access — identity in front of a whole application\n\n_\"Cloudflare Access determines who can reach your application by applying the Access policies you configure.\"_ Unauthenticated visitors are stopped before the app is reached at all — **\"Access is deny by default.\"** You are not writing auth code; you are putting a policy-enforced door in front of an existing app (self-hosted, SaaS, or non-HTTP).\n\nA policy is four parts: an **action** (Allow, Block, Bypass, or Service Auth), **rule types** that combine criteria, **selectors** — the attribute being checked, such as email domain, country, or device posture — and the **values** to match. Identity is validated against an IdP: the canonical example is allowing anyone _\"with an `@example.com` email address, as validated against an IdP.\"_ Note the docs' own warning: Cloudflare _\"does not recommend using Bypass to grant direct permanent access to your internal applications.\"_\n\nUse it for the staging site, the admin panel, the internal dashboard, the metrics endpoint. Do **not** reach for Turnstile there — a bot check does not know who someone is.\n\n### Turnstile — proving a visitor is a human, without a CAPTCHA\n\n_\"Cloudflare's smart CAPTCHA alternative.\"_ It runs _\"a series of small non-interactive JavaScript challenges to gather signals about the visitor or browser environment\"_ instead of making people label traffic lights, and it is a drop-in replacement for reCAPTCHA and hCaptcha. Critically, **your site does not have to be on Cloudflare**: _\"Turnstile can be embedded into any website without sending traffic through Cloudflare.\"_\n\nTwo keys, and mixing them up is the classic failure:\n\n- **Sitekey — public.** _\"Public key used to invoke the Turnstile widget on your site.\"_ It ships in your HTML. That is fine and by design.\n- **Secret key — private.** _\"Private key used for server-side token validation,\"_ with the docs' own rule: _\"Never expose secret keys in client-side code.\"_\n\n**The widget alone proves nothing.** A rendered widget just hands the browser a token; anyone can POST your form without one. The check is the server-side call to `POST https://challenges.cloudflare.com/turnstile/v0/siteverify` with `secret` and `response`, and acting on `success`. Two properties shape your code: the token is **valid for 300 seconds (5 minutes)**, and it is **single-use** — _\"a replayed token will be rejected.\"_ So verify once, at submit, on the server, and never cache or re-check a token.\n\n**Hostname behaviour catches people out.** A hostname you add covers itself *and everything under it*: _\"the widget will work on that exact hostname and all of its subdomains.\"_ But you cannot express that with a glob — _\"wildcard characters (such as `*`) are not supported in the hostname field.\"_ So add `example.com` and `app.example.com` and `staging.example.com` are already covered; typing `*.example.com` is not a pattern, it is a broken hostname.\n\n### WAF — blocking malicious requests\n\nThe WAF _\"checks incoming web and API requests and filters undesired traffic based on sets of rules called rulesets,\"_ matching on request properties such as _\"IP address, URL path, headers, and body content.\"_ Three rule families do the work: **Custom rules** you write, **Managed Rules** Cloudflare maintains and updates for zero-days, and **Rate limiting rules** that cap requests matching an expression.\n\nPlan matters here, so check before you design around it: Free gets custom rules, **one** rate limiting rule, IP access rules and sampled security events. **Managed Rules require Pro or above**; advanced rate limiting and account-level configuration are Enterprise.\n\n### They stack — and the order is the point\n\nA request can pass the WAF (it isn't a SQL injection), fail Turnstile (it's a scripted signup), and never reach Access (nobody logged in). Putting Turnstile on a login form does not authenticate anyone; putting Access on a public marketing site locks out your customers; a WAF rule cannot tell a legitimate user from a stolen password. Pick by the question you actually have.",
      "diagram": "flowchart TD\n  V[Incoming request] --> W{WAF · is this request malicious?<br/>IP · path · headers · body}\n  W -- matched a rule --> B1[Blocked or rate-limited]\n  W -- clean --> A{Access · who is this person?<br/>validated against your IdP}\n  A -- no identity --> B2[Deny by default · sent to login]\n  A -- allowed, or app is public --> T{Turnstile · is this visitor human?<br/>on the form that matters}\n  T -- token verified server-side --> OK[Request handled]\n  T -- no token · replayed · expired after 300 s --> B3[Rejected]\n  class W,A,T fact\n  class B1,B2,B3,OK built",
      "diagram_mini": "flowchart LR\n  R[request] --> W[WAF · malicious?]\n  W --> A[Access · who?]\n  A --> T[Turnstile · human?]\n  class W,A,T fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/cloudflare-who-gets-in.svg",
      "svg_mini": "knowledge/concepts/visuals/cloudflare-who-gets-in.mini.svg"
    },
    {
      "id": "cloudflare-how-users-find-you",
      "title": "How the internet finds your site",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 18,
      "summary": "A registrar holds the name, a DNS host answers the lookups, a CDN sits in front of the server. Knowing which is which is what tells you who to change, and where, to move a domain.",
      "see_also": [
        "cloudflare-deployment-traps",
        "build-time-vs-runtime-config",
        "source-control-basics"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Cloudflare changes which record types can be proxied, what Universal SSL covers, or the default no-cache-for-HTML behaviour — or the registrar/DNS-host split stops being how domains work.",
      "sources": [
        {
          "label": "Cloudflare DNS — proxy status (proxied vs DNS-only)",
          "url": "https://developers.cloudflare.com/dns/proxy-status/"
        },
        {
          "label": "Cloudflare DNS — DNS record types",
          "url": "https://developers.cloudflare.com/dns/manage-dns-records/reference/dns-record-types/"
        },
        {
          "label": "Cloudflare DNS — full setup, change your nameservers",
          "url": "https://developers.cloudflare.com/dns/zone-setups/full-setup/setup/"
        },
        {
          "label": "Cloudflare SSL — Universal SSL",
          "url": "https://developers.cloudflare.com/ssl/edge-certificates/universal-ssl/"
        },
        {
          "label": "Cloudflare Cache — default cache behavior",
          "url": "https://developers.cloudflare.com/cache/concepts/default-cache-behavior/"
        }
      ],
      "body_md": "You own a domain and you want it to point somewhere new — a new host, a new site, a new mail provider — without breaking the other two. The whole question is **who do I change, and where**. It is only hard because three separate jobs are usually sold by one company, so people merge them into a single mental \"my domain.\"\n\n**Three roles. Learn to say which one you mean.**\n\n- **Registrar** — who you rent the *name* from (GoDaddy, Namecheap, Cloudflare Registrar). It holds exactly one thing that matters day to day: the list of **nameservers**. Nothing about your site's content lives here.\n- **DNS host** — the service that *answers the questions*: \"what IP is example.com?\", \"where does its mail go?\". Your records live here. Cloudflare's own setup guide draws the line: the registrar is where you \"registered and own your domain\"; the DNS provider is \"the service managing your DNS records.\"\n- **CDN / proxy** — a network that sits *in front of* your server: terminates TLS, caches, filters. Entirely optional. Cloudflare can be all three at once, which is exactly why the three get merged.\n\n**Nameservers are the switch at the top.** An `NS` record \"indicate[s] which server should be used for authoritative DNS.\" You set it **at the registrar**, and it decides which company gets asked everything else. Change it and every record moves at once. Cloudflare tells you to \"wait up to 24 hours while your registrar updates your nameservers\" — that lag is why the nameserver change is the one you plan, and the record change is the one you do casually.\n\n**The four records you will actually touch**, all at the DNS host:\n\n- **A / AAAA** — \"map a domain name to one or multiple IPv4 or IPv6 address(es).\" The literal \"the site lives at this machine\" record.\n- **CNAME** — \"map[s] a domain name to another (canonical) domain name.\" Use it when your host gives you a hostname (`my-app.pages.dev`) instead of an IP.\n- **MX** — \"required to deliver email to a mail server.\" **Email is a separate system from the website.** Moving your site does not touch MX; deleting MX by accident is how a site move takes the mail down with it.\n- **TXT** — \"let you enter text into the DNS system.\" Domain-ownership proofs and email authentication (SPF / DKIM / DMARC) live here.\n\n**Proxied vs DNS-only is a real fork, not a display setting.** Proxied means \"Cloudflare sits between your visitors and your server — optimizing, caching, and protecting traffic along the way\": visitors resolve to Cloudflare's anycast IPs, never your origin. DNS-only records \"respond with your server's actual IP address and do not route HTTP/HTTPS traffic through its network.\" Only address-resolving records qualify — \"Other record types (such as MX or TXT) are always DNS-only.\" The part that bites: DDoS protection, caching, and \"WAF rules, caching, and redirect rules\" apply **only to proxied traffic**. A record you flipped to DNS-only while debugging is a record with no cache, no WAF, and your origin IP published.\n\n**SSL is automatic, and it is a property of the edge.** Cloudflare \"issues — and renews — free, unshared, publicly trusted SSL certificates to all domains added to and activated on Cloudflare.\" On a full setup that covers the root domain and first-level subdomains; deeper names need more certificate. Because the edge presents it, the automatic certificate is for **proxied** hostnames — turn the proxy off and you are back to whatever certificate your origin holds.\n\n**Caching defaults surprise people in one specific direction.** \"The Cloudflare CDN does not cache HTML or JSON by default.\" It caches by file extension — CSS, JS, images, fonts, PDFs. So your *assets* are cached at the edge and your *pages* are not, unless you asked. Cloudflare skips caching when `Cache-Control` is `private`, `no-store`, `no-cache`, or `max-age=0`, and caches when it is `public` with `max-age` greater than 0. Practical consequence: \"I deployed and still see the old page\" is usually your **browser**, not the CDN.\n\n**So: who do I change, and where.**\n\n| What you want | Where you do it |\n| --- | --- |\n| Move to a different DNS host | `NS` — at the **registrar** |\n| Point the site at a new server or host | `A` / `AAAA` / `CNAME` — at the **DNS host** |\n| Move email | `MX` — at the DNS host; unrelated to the website |\n| Turn the CDN on or off for one hostname | proxy status on that record — at the DNS host |\n| Prove ownership to a third party | `TXT` — at the DNS host |\n| Move to a different registrar | transfer — at the **registrar**; records are unaffected if the nameservers do not change |\n\nThe sentence worth keeping: **the registrar decides who answers, the DNS host answers, and the proxy decides what happens after the answer.** Most \"move my site\" work is one row of A/CNAME edits at the DNS host — people reach for the nameservers, and the 24-hour clock, when they never needed to.",
      "diagram": "flowchart TD\n  U[Visitor types example.com] --> Q{Who is authoritative?}\n  Q --> REG[Registrar · owns the name<br/>holds the NS pointer]\n  REG -->|NS points here| DNS[DNS host · answers lookups<br/>A · AAAA · CNAME · MX · TXT]\n  DNS -->|A or CNAME · proxied| CDN[CDN edge · anycast IP<br/>auto TLS · cache · WAF]\n  DNS -->|A or CNAME · DNS-only| ORG[Your server · real IP published<br/>no cache · no WAF]\n  CDN --> ORG2[Your server · origin IP hidden]\n  DNS -->|MX · always DNS-only| MAIL[Mail provider · separate system]\n  class REG,DNS,CDN fact\n  class ORG,ORG2,MAIL built",
      "diagram_mini": "flowchart LR\n  R[registrar<br/>NS] --> D[DNS host<br/>records] --> P[proxy<br/>TLS · cache] --> S[server]\n  class R,D,P fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/cloudflare-how-users-find-you.svg",
      "svg_mini": "knowledge/concepts/visuals/cloudflare-how-users-find-you.mini.svg"
    },
    {
      "id": "subscription-billing-model",
      "title": "How subscription billing actually works",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 21,
      "summary": "Trial → first charge → renewal. Webhooks are at-least-once and unordered, so provisioning needs an event-ID ledger and a per-state guard — and a $0 invoice is not a payment.",
      "see_also": [
        "silent-green-defects",
        "oauth-scope-cost"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Stripe changes its webhook delivery guarantee (at-least-once / unordered), changes the invoice.paid + status:active provisioning rule, stops creating a zero-amount invoice for trial subscriptions, or renames the lifecycle events.",
      "sources": [
        {
          "label": "Stripe — Receive Stripe events in your webhook endpoint (duplicates, ordering)",
          "url": "https://docs.stripe.com/webhooks"
        },
        {
          "label": "Stripe — How subscriptions work (lifecycle and statuses)",
          "url": "https://docs.stripe.com/billing/subscriptions/overview"
        },
        {
          "label": "Stripe — Subscription webhook events",
          "url": "https://docs.stripe.com/billing/subscriptions/webhooks"
        },
        {
          "label": "Stripe — Use free trial periods on subscriptions",
          "url": "https://docs.stripe.com/billing/subscriptions/trials/free-trials"
        },
        {
          "label": "Stripe — Fulfill orders (webhooks are required)",
          "url": "https://docs.stripe.com/checkout/fulfillment"
        }
      ],
      "body_md": "The failure you will actually meet is this: a customer paid, their browser died on the redirect, and they have no account. Or: they got billed once and provisioned three times. Or: someone on a free trial got the paid tier for free, forever, because a zero-amount invoice looked like a payment.\n\nNone of those are edge cases. They are the default behaviour of a subscription integration that was written as though the money path runs through the browser. It doesn't. It runs through webhooks, and webhooks have properties you have to design around.\n\n## The lifecycle, in the order it happens\n\n**Creation.** Stripe creates a Subscription. If payment is due immediately, it also creates an Invoice and a PaymentIntent, and the subscription starts as `incomplete` — it becomes `active` only after the customer pays that first invoice.\n\n**Trial.** With a trial, the initial status is `trialing`, and Stripe *\"automatically transitions to `active` when the trial ends and payment succeeds.\"* You get a heads-up first: `customer.subscription.trial_will_end` is *\"Sent 3 days before the trial period ends.\"*\n\n**First real charge.** *\"When the trial ends, if the subscription `status` isn't `paused`, we generate an invoice and send an `invoice.created` event notification. Approximately 1 hour later, we attempt to charge that invoice.\"* On success you get `invoice.paid`, described as: *\"Sent when the invoice is successfully paid. You can provision access to your product when you receive this event and the subscription `status` is `active`.\"*\n\n**Renewal.** The subscription keeps generating an invoice every billing period, and the same `invoice.paid` / `invoice.payment_failed` pair keeps arriving. Renewal is not a special case in your code — it is the same handler as the first charge. If your first-charge path is a one-shot script, renewals will quietly not work.\n\n**Failure.** `invoice.payment_failed` fires; the subscription lands in `past_due` or `unpaid` depending on your dashboard settings. Downgrade on that signal, not on a timer.\n\n## Why the money path must be webhooks, not the success page\n\nStripe puts this in a callout headed **\"Webhooks are required\"**:\n\n> *\"You can't rely on triggering fulfillment only from your Checkout landing page, because your customers aren't guaranteed to visit that page. For example, someone can pay successfully in Checkout and then lose their connection to the internet before your landing page loads.\"*\n\nThe browser is a *courtesy* channel: it tells the customer something happened. The webhook is the *authoritative* channel: it tells your server something happened, and it retries. Grant entitlements from the webhook only. It is fine for the success page to show a spinner and poll your own database — it is not fine for the success page to be the thing that writes the row.\n\n## At-least-once means you will get duplicates\n\n> *\"Webhook endpoints might occasionally receive the same event more than once.\"*\n\nAnd Stripe is blunt that this is your problem to solve: *\"Because of how this integration and the internet work, your `fulfill_checkout` function might be called multiple times, possibly concurrently, for the same Checkout Session.\"* Note **concurrently** — two deliveries can be in flight at once, so a read-then-write check (\"have I seen this?\" … \"no\" … \"ok, provision\") races itself and provisions twice.\n\nStripe's recommended defence is an **idempotency ledger**: *\"You can guard against duplicated event receipts by logging the event IDs you've processed, and then not processing already-logged events.\"*\n\nBuild it **INSERT-first**, not check-first:\n\n1. `INSERT` the event id into a table with a **unique constraint** on that id.\n2. If the insert raises a uniqueness violation, this is a duplicate — **return 200 and do nothing**. Not an error; a no-op.\n3. Only if the insert succeeded do you run the side effect.\n\nThe database's unique index is the lock. That is what makes it safe against concurrent deliveries, which an `if not exists` check is not. Return `200` on duplicates: a non-2xx makes Stripe retry, and retrying a duplicate forever is a self-inflicted outage.\n\nStripe also notes that in some cases *two separate Event objects* describe the same thing, and suggests keying on *\"the ID of the object in `data.object` along with the `event.type`\"* — worth doing if you see double-provisioning that the event-id ledger doesn't catch.\n\n## Out-of-order means a replay must be a safe no-op\n\n> *\"Stripe doesn't guarantee the delivery of events in the order that they're generated.\"*\n> *\"Make sure that your event destination isn't dependent on receiving events in a specific order.\"*\n\nA cancellation can land before the renewal that preceded it. So the event-id ledger is not enough on its own — it stops the *same* event running twice, but not an *older* event overwriting a newer state.\n\nAdd a **per-(subscription, state) guard**. Keep the subscription's current state in your own table, and make each handler a conditional transition: only move `subscription X` into state `active` if it isn't already there and the event isn't older than what you've recorded. Written that way, a replayed or late event finds the world already in the state it wanted and does nothing. That is the property you want: **every handler is safe to run at any time, in any order, any number of times.**\n\n## The trap: a $0 invoice is not a payment\n\nFor a trial subscription, Stripe's free-trial documentation says plainly:\n\n> *\"When creating a subscription with a trial period, you don't need to add a payment method. An immediate invoice is still created, but the amount is 0 and the Invoice Line descriptions include 'Free trial' verbiage.\"*\n\nSo a zero-amount invoice exists, and it settles, and an invoice-settled event carrying **no money at all** can reach your handler. A handler that reads \"invoice\" and grants the paid tier gives away the product.\n\nThis is exactly why Stripe's provisioning rule is **two-part**, and both parts matter: provision *\"when you receive this event **and** the subscription `status` is `active`.\"* During a free trial the status is `trialing`, not `active` — the status check is what stops the $0 invoice from being mistaken for a payment. Belt and braces: check the amount is greater than zero **and** the subscription status before you grant anything paid.\n\n*(Verified this session: the $0 trial invoice is created, and the two-part provisioning rule is Stripe's own. The docs I checked do not state in one place that `invoice.paid` is emitted for that zero-amount invoice — so treat it as though it is. Coding for the conservative case costs nothing; assuming the event never arrives costs you the product.)*\n\nTrials should be provisioned from the trial itself (`trialing` is a real, grantable state with its own entitlements), never inferred from a settled invoice.",
      "diagram": "flowchart TD\n  W[Stripe webhook arrives] --> SIG[Verify the signature]\n  SIG --> LED{INSERT event id<br/>unique constraint}\n  LED -- \"duplicate · unique violation\" --> NOOP[200 OK · do nothing<br/>at-least-once handled]\n  LED -- \"new\" --> KIND{Which event?}\n  KIND -- invoice.paid --> AMT{amount not zero<br/>AND status active?}\n  AMT -- no --> TRIAL[Zero-amount / trialing<br/>NOT a payment · no paid grant]\n  AMT -- yes --> GUARD{Per subscription+state guard<br/>already in this state?}\n  GUARD -- yes --> NOOP2[No-op · replay or out-of-order]\n  GUARD -- no --> PROV[Provision · first charge OR renewal]\n  KIND -- invoice.payment_failed --> DOWN[past_due / unpaid · downgrade]\n  KIND -- trial_will_end --> NOTIFY[Warn · ensure payment method]\n  B[Browser hits success page] -.-> POLL[Read-only: poll your own DB]\n  POLL -.-> X[Never grants anything]\n  class NOOP,NOOP2,PROV,TRIAL,DOWN,NOTIFY,POLL,X built\n  class LED,KIND,AMT,GUARD fact",
      "diagram_mini": "flowchart LR\n  E[event] --> L{seen id?}\n  L -- yes --> N[no-op 200]\n  L -- no --> P[provision if paid + active]\n  class N,P built",
      "steps": [
        {
          "caption": "Trial → first charge → renewal. Renewal is the SAME handler, not a special case.",
          "diagram": "flowchart LR\n  N1[Lifecycle] --> N2[Webhooks, not the browser] --> N3[Event-id ledger] --> N4[Per-state guard] --> N5[Zero is not a payment]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/subscription-billing-model.step-1.svg"
        },
        {
          "caption": "The browser may never reach the success page. Grant entitlements from the webhook only.",
          "diagram": "flowchart LR\n  N1[Lifecycle] --> N2[Webhooks, not the browser] --> N3[Event-id ledger] --> N4[Per-state guard] --> N5[Zero is not a payment]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/subscription-billing-model.step-2.svg"
        },
        {
          "caption": "Delivery is at-least-once and concurrent: INSERT the event id first, no-op on the unique violation.",
          "diagram": "flowchart LR\n  N1[Lifecycle] --> N2[Webhooks, not the browser] --> N3[Event-id ledger] --> N4[Per-state guard] --> N5[Zero is not a payment]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/subscription-billing-model.step-3.svg"
        },
        {
          "caption": "Order is not guaranteed: a per-(subscription, state) guard makes a late or replayed event a safe no-op.",
          "diagram": "flowchart LR\n  N1[Lifecycle] --> N2[Webhooks, not the browser] --> N3[Event-id ledger] --> N4[Per-state guard] --> N5[Zero is not a payment]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/subscription-billing-model.step-4.svg"
        },
        {
          "caption": "A trial creates a zero-amount invoice. Require amount > 0 AND status active before granting the paid tier.",
          "diagram": "flowchart LR\n  N1[Lifecycle] --> N2[Webhooks, not the browser] --> N3[Event-id ledger] --> N4[Per-state guard] --> N5[Zero is not a payment]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/subscription-billing-model.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/subscription-billing-model.svg",
      "svg_mini": "knowledge/concepts/visuals/subscription-billing-model.mini.svg"
    },
    {
      "id": "oauth-scope-cost",
      "title": "What an OAuth scope actually costs you",
      "category": "Shipping to production",
      "kind": "platform-fact",
      "order": 22,
      "summary": "Google grades OAuth scopes recommended, sensitive, or restricted. Restricted adds verification plus a 12-monthly third-party security assessment — check the scope before you design the feature.",
      "see_also": [
        "permission-layers",
        "web-access-guardrail",
        "containment-posture"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Google changes its scope categories, moves a Drive scope between categories, changes the drive.file grant wording, or drops/replaces the CASA third-party security assessment or its 12-month recertification.",
      "sources": [
        {
          "label": "Google — Restricted scope verification (CASA security assessment)",
          "url": "https://developers.google.com/identity/protocols/oauth2/production-readiness/restricted-scope-verification"
        },
        {
          "label": "Google — Choose Google Drive API scopes",
          "url": "https://developers.google.com/workspace/drive/api/guides/api-specific-auth"
        },
        {
          "label": "Google — Sensitive scope verification",
          "url": "https://developers.google.com/identity/protocols/oauth2/production-readiness/sensitive-scope-verification"
        }
      ],
      "body_md": "You sketch a feature: *\"we'll make a folder in the customer's Google Drive, they drop files in, and we read them.\"* One sentence, one afternoon of work. It is, in fact, a multi-week compliance project with a recurring annual bill — and nothing in the code tells you that. The scope does.\n\n**Check the scope before you design the feature.** The scope is not a checkbox you fill in at the end; it is the thing that decides whether the feature is cheap, expensive, or effectively off the table for a small team.\n\n## Google's three tiers\n\nGoogle sorts OAuth scopes into three buckets, and the bucket sets the price:\n\n- **Recommended / non-sensitive** — narrow, per-item access. No Google review. Ship today.\n- **Sensitive** — broader access to user data. *\"Sensitive scopes require review by Google before any Google Account can grant access.\"* Weeks of back-and-forth, but no outside auditor.\n- **Restricted** — the highest-risk data. Verification **plus** an independent security assessment.\n\nThe restricted tier is where the surprise lives. Google's own words: *\"Every app that requests access to Google users' restricted data and has the ability to access data from or through a third-party server must go through a security assessment\"* — standardised on the App Defense Alliance's **CASA** framework. And it does not end: *\"apps must be reverified for compliance and complete a security assessment at least every 12 months after your assessor's Letter of Assessment (LOA) approval date.\"* Google warns that restricted-scope verification alone *\"can potentially take several weeks to complete.\"*\n\nRead the trigger carefully: it fires on **the ability to access restricted data from or through a server**. A purely client-side toy might dodge it. Anything with a backend — which is to say, anything real — does not.\n\n## The Drive example, concretely\n\n`drive.file` is **non-sensitive**. Google describes what it grants exactly:\n\n> *\"Create new Drive files, or modify existing files, that you open with an app or that the user shares with an app while using the Google Picker API or the app's file picker.\"*\n\nThat wording is per-file, and it names exactly two ways a file enters your grant: **your app created it**, or **the user handed it to you through a picker**. Nothing else.\n\nSo run the sketch through it. Your app creates the folder — fine, the app created that. The user then drags in twelve invoices. Your app did not create those files and the user never picked them, so they are simply not in the grant. There is no \"the folder is mine, therefore its contents are mine\" rule. The only way to enumerate whatever a user happens to put in a folder is a scope like `drive` or `drive.readonly` — and both are **restricted**. Your afternoon feature just bought you verification, a CASA assessment, and an annual re-audit forever.\n\nThe cheap redesign is usually right there: keep `drive.file` and put a Google Picker in the flow, so the user *hands you* the files instead of you going to find them. Same user outcome, one tier down, no auditor.\n\n## Two honesty notes\n\n**Providers differ, and I only checked one.** Google's tiering, the CASA assessment, and the 12-month recertification were verified against Google's own documentation for this concept. Microsoft, Slack, GitHub, Dropbox and the rest each have their own review regimes, their own definitions of \"sensitive\", and their own audit requirements — some lighter, some heavier. Do not carry Google's rules across; go read the provider you are actually integrating with. Nothing here is verified for any provider other than **Google**.\n\n**This is engineering guidance, not legal or compliance advice.** The costs, timelines and audit terms are Google's published requirements as of the verification date, not a quote for your app.",
      "diagram": "flowchart TD\n  F[Feature idea] --> Q[Ask FIRST: which scope does this need?]\n  Q --> C{Google's scope tier}\n  C -- recommended / non-sensitive --> R[Ship it · no Google review]\n  C -- sensitive --> S[Google review · weeks · no outside auditor]\n  C -- restricted --> T{Server touches<br/>the data?}\n  T -- no --> V[Verification only]\n  T -- yes --> A[Verification + CASA security assessment<br/>+ re-audit every 12 months]\n  A --> RD[Redesign: Picker + drive.file<br/>drops you back to non-sensitive]\n  RD --> R\n  class R,S,V,A,RD built\n  class C,T fact",
      "diagram_mini": "flowchart LR\n  S[Scope] --> T{tier?}\n  T -- restricted --> A[audit yearly]\n  T -- per-file --> OK[ship]\n  class A,OK built",
      "steps": [
        {
          "caption": "Start from the feature, not the code: \"we create a folder and read what they put in it.\"",
          "diagram": "flowchart LR\n  N1[Feature idea] --> N2[Which scope?] --> N3[Which tier?] --> N4[Restricted = audit] --> N5[Redesign to per-file]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/oauth-scope-cost.step-1.svg"
        },
        {
          "caption": "Name the scope BEFORE the design is fixed — the scope is the cost driver, not the code.",
          "diagram": "flowchart LR\n  N1[Feature idea] --> N2[Which scope?] --> N3[Which tier?] --> N4[Restricted = audit] --> N5[Redesign to per-file]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/oauth-scope-cost.step-2.svg"
        },
        {
          "caption": "Look up its tier: recommended / non-sensitive, sensitive, or restricted. The tier is the price.",
          "diagram": "flowchart LR\n  N1[Feature idea] --> N2[Which scope?] --> N3[Which tier?] --> N4[Restricted = audit] --> N5[Redesign to per-file]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/oauth-scope-cost.step-3.svg"
        },
        {
          "caption": "Restricted + a server that can reach the data = CASA assessment, re-done every 12 months.",
          "diagram": "flowchart LR\n  N1[Feature idea] --> N2[Which scope?] --> N3[Which tier?] --> N4[Restricted = audit] --> N5[Redesign to per-file]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/oauth-scope-cost.step-4.svg"
        },
        {
          "caption": "Redesign so the user HANDS you each file (Google Picker + drive.file) — same outcome, no auditor.",
          "diagram": "flowchart LR\n  N1[Feature idea] --> N2[Which scope?] --> N3[Which tier?] --> N4[Restricted = audit] --> N5[Redesign to per-file]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/oauth-scope-cost.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/oauth-scope-cost.svg",
      "svg_mini": "knowledge/concepts/visuals/oauth-scope-cost.mini.svg"
    },
    {
      "id": "cloudflare-deployment-traps",
      "title": "Cloudflare deployment traps that cost a day",
      "category": "Shipping lessons",
      "kind": "ravenclaude-built",
      "order": 23,
      "summary": "Six Cloudflare Pages behaviours that each cost real debugging time. They share one shape: every failure is silent and plausible, so you diagnose a wrong thing confidently for hours.",
      "see_also": [
        "cloudflare-how-users-find-you",
        "build-time-vs-runtime-config",
        "claim-grounding"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Cloudflare Pages changes when bindings and secrets attach, adds named environments beyond production/preview, or starts documenting Functions propagation and the POST content-type rejection.",
      "sources": [
        {
          "label": "Cloudflare Pages — Functions bindings ('Redeploy your project for the binding to take effect')",
          "url": "https://developers.cloudflare.com/pages/functions/bindings/"
        },
        {
          "label": "Cloudflare Pages — Wrangler configuration (production and preview are the only two environments)",
          "url": "https://developers.cloudflare.com/pages/functions/wrangler-configuration/"
        },
        {
          "label": "Wrangler commands — pages secret put",
          "url": "https://developers.cloudflare.com/workers/wrangler/commands/pages/"
        },
        {
          "label": "Wrangler — environments and the --env flag",
          "url": "https://developers.cloudflare.com/workers/wrangler/environments/"
        },
        {
          "label": "Cloudflare SSL — certificate and hostname priority (SNI selection)",
          "url": "https://developers.cloudflare.com/ssl/reference/certificate-and-hostname-priority/"
        },
        {
          "label": "Cloudflare D1 — local development (preview_database_id)",
          "url": "https://developers.cloudflare.com/d1/configuration/local-development/"
        }
      ],
      "body_md": "Each of these was paid for with real debugging time on a live Cloudflare Pages project. None of them throws. That is the point — read the last section before you decide these are trivia.\n\n**1 — Secrets and D1 bindings attach at DEPLOY time.** Setting a secret or adding a binding changes nothing about what is currently running. Cloudflare says it plainly, once per binding type: *\"Redeploy your project for the binding to take effect.\"* So the loop that feels obvious — set the secret, hit the endpoint, still broken, set it again — is testing the **old** value every time, and you will conclude the secret is wrong when it was right on the first try. Set it, **deploy**, then test.\n\n**2 — Functions propagate after static assets.** On a fresh deploy the pages render perfectly while every `/api/*` route still 404s, for roughly 30–60 seconds. You will spend that window convinced you broke routing. Deploy, wait, *then* diagnose — a 404 inside the propagation window is not evidence of anything. `[unverified — field-observed; the Pages known-issues page documents no propagation lag, so treat the exact window as an observation, not a contract]`\n\n**3 — Cloudflare routes by TLS SNI, so a forged `Host:` header proves nothing.** Certificate and zone selection happen during the TLS handshake, on the SNI hostname: *\"Certificates and settings that match the SNI hostname exactly take precedence\"*, and *\"If no SNI is presented, Cloudflare uses certificate based on the IP address.\"* The HTTP `Host` header arrives **after** that choice is already made. This invalidates a whole category of test people write — `curl -H \"Host: my-app.example.com\" https://some-other-host/` does not put you on that zone's Worker, it puts you on whatever zone the SNI selected, holding a header nobody routed on. If you want to test a hostname, resolve to it or use `--resolve`; do not fake the header.\n\n**4 — Pages rejects a POST with no `content-type` before your Function runs.** It comes back as its own 403, treated as a cross-site form submission — and because the rejection is upstream of your code, **your handler's logs show nothing at all**. That silence reads exactly like \"my route isn't wired up,\" which is the wrong hunt. Always send an explicit `content-type` on POST, including from `fetch`, scripts and health checks. `[unverified — field-observed; not documented in the Pages known-issues page]`\n\n**5 — `--env preview` on `wrangler pages secret put` is undocumented but load-bearing.** The Pages command reference lists only `[KEY]` and `--project-name` for `pages secret put`; `--env` is a *global* Wrangler flag, and omitting it means Wrangler \"uses the top-level configuration.\" Pages, meanwhile, has exactly two environments — *\"`production` and `preview` are the only two options available via `[env.<ENVIRONMENT>]`.\"* Net effect: the secret lands in one environment and you assume both. Preview then runs with a missing or stale value while production is fine, and the difference is invisible until a preview deploy behaves nothing like the branch it came from. Set it once per environment, explicitly.\n\n**6 — `preview_database_id` does not bind a preview deployment.** It is a *local development* key — D1's docs describe it as \"a user-defined ID for your local test database.\" It is not how a deployed Pages preview picks a database, and nothing errors when you use it that way; the preview simply keeps whatever it had, which is usually **production**. That is a preview branch writing to the real database while everyone believes it is isolated. The documented Pages shape is an explicit environment override:\n\n```toml\n[[env.preview.d1_databases]]\nbinding = \"DB\"\ndatabase_name = \"my-app-preview\"\ndatabase_id = \"<PREVIEW_DATABASE_ID>\"\n```\n\n**The unifying lesson, which is the actually portable part.** Every one of these fails **silently and plausibly**: no exception, no warning, and a symptom that points confidently at something else — a wrong secret, a broken route, an unwired handler, an isolated preview. That is why they cost a day rather than a minute. The defence is not memorising six Cloudflare facts; it is the habit of asking, before you start bisecting: *what would this look like if my mental model of when config attaches were wrong?* Deploy before you test a secret. Wait before you diagnose a 404. Don't trust a header the platform didn't route on. Don't trust a preview you never proved was pointed somewhere else.",
      "diagram": "flowchart TD\n  S[You change something] --> W{When does it take effect?}\n  W -->|secret or binding| D[Next DEPLOY · not now]\n  W -->|Functions| P[~30-60s AFTER static assets]\n  D --> T1[Trap · you test the OLD value]\n  P --> T2[Trap · /api 404s on a good deploy]\n  R[You write a test] --> H{What does the platform route on?}\n  H -->|TLS SNI| T3[Trap · forged Host header proves nothing]\n  H -->|no content-type on POST| T4[Trap · 403 before your code · no logs]\n  C[You set config] --> E{Which environment?}\n  E -->|--env omitted| T5[Trap · secret in one env only]\n  E -->|preview_database_id| T6[Trap · preview bound to PRODUCTION db]\n  T1 --> L[All six fail SILENTLY and PLAUSIBLY]\n  T2 --> L\n  T3 --> L\n  T4 --> L\n  T5 --> L\n  T6 --> L\n  class T1,T2,T3,T4,T5,T6 built\n  class L fact",
      "diagram_mini": "flowchart LR\n  C[change config] --> D[deploy] --> T[then test]\n  X[test before deploy] -.-> W[wrong conclusion]\n  class D,T fact\n  class W built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/cloudflare-deployment-traps.svg",
      "svg_mini": "knowledge/concepts/visuals/cloudflare-deployment-traps.mini.svg"
    },
    {
      "id": "build-time-vs-runtime-config",
      "title": "Build-time config vs runtime config",
      "category": "Shipping lessons",
      "kind": "ravenclaude-built",
      "order": 24,
      "summary": "A PUBLIC_ variable is baked into the JS bundle during the build. Set one as a runtime secret and it never reaches the browser — the code compiles, deploys, and silently ships an empty value.",
      "see_also": [
        "cloudflare-deployment-traps",
        "cloudflare-how-users-find-you",
        "claim-grounding"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "Vite, Astro or Next change how public-prefixed variables are inlined, or a framework adds a real runtime channel for client config.",
      "sources": [
        {
          "label": "Vite — env variables and modes ('statically replaced at build time')",
          "url": "https://vite.dev/guide/env-and-mode"
        },
        {
          "label": "Astro — environment variables (PUBLIC_ prefix)",
          "url": "https://docs.astro.build/en/guides/environment-variables/"
        },
        {
          "label": "Next.js — environment variables (NEXT_PUBLIC_ inlining)",
          "url": "https://nextjs.org/docs/app/guides/environment-variables"
        },
        {
          "label": "Cloudflare Pages — Functions bindings (redeploy for a binding to take effect)",
          "url": "https://developers.cloudflare.com/pages/functions/bindings/"
        },
        {
          "label": "Cloudflare Turnstile — client-side rendering (data-sitekey)",
          "url": "https://developers.cloudflare.com/turnstile/get-started/client-side-rendering/"
        }
      ],
      "body_md": "Every configuration value you set lands in one of two places, and they are not interchangeable:\n\n- **Build-time config** is *text substitution during the build*. The bundler finds `import.meta.env.PUBLIC_X` in your source and replaces it with a literal string. After that there is no variable — there is a string sitting in a `.js` file on a CDN.\n- **Runtime config** is *read while the request is being served*. Server code asks the environment for a value each time. Nothing is baked; change it and redeploy, and the next request sees the new value.\n\nVite states the mechanism directly: variables prefixed `VITE_` \"will be exposed in client-side source code after Vite bundling\", and are \"statically replaced at build time to make tree-shaking effective.\" Astro renames the prefix to `PUBLIC_` and inherits the same behaviour — \"only environment variables prefixed with `PUBLIC_` are available in client-side code\". Next.js uses `NEXT_PUBLIC_` and spells out the consequence better than anyone: *\"After being built, your app will no longer respond to changes to these environment variables… all `NEXT_PUBLIC_` variables will be frozen with the value evaluated at build time.\"*\n\n**So a `PUBLIC_` variable set as a runtime secret is not \"set late.\" It is not set at all.** The build ran without it, substituted an empty string, and shipped that. Your platform's secret store now holds a value that nothing will ever read, and the dashboard shows it present — which is why this survives review.\n\n**The failure it actually caused.** A Cloudflare Turnstile **sitekey** was set as a runtime secret. The sitekey is a client-side value by design — it goes in the markup as `data-sitekey` for the widget to render. With it inlined as empty, the widget never rendered, so the browser produced no token. The server half was configured correctly and kept doing its job: it demanded a token and rejected the request without one. Result: **every checkout failed with a captcha error**, on a build that compiled clean, deployed green, and logged nothing unusual. Both halves were individually \"working.\" The split between them was the bug.\n\n**The test for telling which kind a variable is — two questions, in order.**\n\n1. **Does the browser need to read it?** If yes, it must be build-time (or fetched from an API at runtime — but then it is *that endpoint's* runtime config, not the bundle's). If no, keep it runtime and server-only.\n2. **Would you be relaxed about it appearing in View Source?** Build-time public values are published — Vite says it outright: `VITE_*` variables \"should *not* contain sensitive information such as API keys… bundled into your source code at build time.\" If the honest answer is \"no,\" it cannot be a public variable, and the design needs a server endpoint instead of a prefix.\n\nThe mechanical confirmation takes ten seconds: **build, then grep the output bundle for the value.** Present in `dist/` ⇒ build-time, and public forever. Absent ⇒ it never reached the client, whatever the dashboard says. Do this once per public variable before you argue with the platform.\n\n**Two corollaries worth internalising.**\n\n- **Changing a build-time value requires a rebuild, not a restart.** The same trap as a Cloudflare Pages binding, where the docs say \"Redeploy your project for the binding to take effect\" — except here even a redeploy of the *same artifact* is not enough; the bundle must be built again with the value present.\n- **Public/secret pairs must live on opposite sides.** A sitekey/secret-key pair, a publishable/secret API key pair — the public half is build-time and shipped, the secret half is runtime and server-only. Putting both in the same place is wrong in one direction or the other every time.",
      "diagram": "flowchart TD\n  V[A config value] --> Q{Does the browser read it?}\n  Q -->|yes| B[BUILD-TIME · PUBLIC_ / VITE_ / NEXT_PUBLIC_]\n  Q -->|no| R[RUNTIME · server-only secret or binding]\n  B --> INL[Inlined as a literal during the build]\n  INL --> SHIP[Published in the JS bundle · public forever]\n  R --> READ[Read per request from the environment]\n  READ --> SAFE[Never sent to the browser]\n  X[Public value set as a RUNTIME secret] --> EMPTY[Build ran without it<br/>empty string inlined]\n  EMPTY --> FAIL[Widget never renders · server still demands a token<br/>every checkout fails · nothing errors]\n  class B,R fact\n  class X,EMPTY,FAIL built",
      "diagram_mini": "flowchart LR\n  P[PUBLIC_ var] --> BLD[build · inlined] --> JS[in the bundle]\n  S[secret] --> RT[runtime · server only]\n  class BLD,RT fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/build-time-vs-runtime-config.svg",
      "svg_mini": "knowledge/concepts/visuals/build-time-vs-runtime-config.mini.svg"
    },
    {
      "id": "comfort-posture",
      "title": "Comfort-posture dashboard",
      "category": "Security",
      "kind": "ravenclaude-built",
      "order": 25,
      "summary": "A point-and-click editor that writes Claude Code permission rules per category and layer — and keeps design check-ins independent of permission level.",
      "see_also": [
        "permission-layers",
        "command-review-tribunal"
      ],
      "widget": "permission-resolver",
      "try_it": {
        "label": "Open the Settings tab",
        "href": "#/settings"
      },
      "node_links": {
        "R": "permission-layers"
      },
      "last_verified": "2026-05-26",
      "refresh_when": "The comfort-posture category set, the layer model, or the design_checkins decoupling changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "apply-comfort-posture translator",
          "url": "plugins/ravenclaude-core/scripts/apply-comfort-posture.py"
        }
      ],
      "body_md": "**Comfort posture** is RavenClaude's friendly front-end over the raw permission model. Instead of hand-editing `settings.json`, you set a level — **deny / ask / allow** — per *category* of action (file reads, code execution, remote mutations, …) and per *layer* (user / local / project). The dashboard's Settings tab serializes that to `.ravenclaude/comfort-posture.yaml`, and `apply-comfort-posture.py` translates it into the actual Claude Code permission rules — so the layer-precedence rules still govern what finally wins.\n\nThe load-bearing subtlety: **permission level ≠ design judgment.** Setting a category to `allow` only removes the click-to-approve on tool calls — it does **not** tell Claude to stop surfacing architectural decisions. That behavior is a *separate* flag, `design_checkins` (on by default), so relaxing permissions to move faster never silently mutes design check-ins. The two are deliberately decoupled.",
      "diagram": "flowchart TD\n  U[You set deny/ask/allow<br/>per category · per layer] --> Y[(comfort-posture.yaml)]\n  Y --> T[apply-comfort-posture.py]\n  T --> R[Claude Code permission rules]\n  U --> DC{design_checkins}\n  DC -- \"on (default)\" --> PAUSE[Surface design decisions<br/>at any permission level]\n  DC -- off --> FAST[Proceed · report after]\n  class Y,T,R,PAUSE,FAST built\n  class DC fact",
      "diagram_mini": "flowchart LR\n  L[deny/ask/allow<br/>per category] --> Y[(posture.yaml)] --> R[permission rules]\n  class Y,R built",
      "steps": [
        {
          "caption": "You set deny/ask/allow per category and per layer in the dashboard.",
          "diagram": "flowchart LR\n  N1[Set per category] --> N2[Posture yaml] --> N3[Translate] --> N4[Permission rules] --> N5[Design checkins]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/comfort-posture.step-1.svg"
        },
        {
          "caption": "It serializes to .ravenclaude/comfort-posture.yaml.",
          "diagram": "flowchart LR\n  N1[Set per category] --> N2[Posture yaml] --> N3[Translate] --> N4[Permission rules] --> N5[Design checkins]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/comfort-posture.step-2.svg"
        },
        {
          "caption": "apply-comfort-posture.py translates it into real Claude Code permission rules.",
          "diagram": "flowchart LR\n  N1[Set per category] --> N2[Posture yaml] --> N3[Translate] --> N4[Permission rules] --> N5[Design checkins]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/comfort-posture.step-3.svg"
        },
        {
          "caption": "Layer-precedence still governs what finally wins.",
          "diagram": "flowchart LR\n  N1[Set per category] --> N2[Posture yaml] --> N3[Translate] --> N4[Permission rules] --> N5[Design checkins]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/comfort-posture.step-4.svg"
        },
        {
          "caption": "design_checkins is separate: 'allow' never silences design check-ins (on by default).",
          "diagram": "flowchart LR\n  N1[Set per category] --> N2[Posture yaml] --> N3[Translate] --> N4[Permission rules] --> N5[Design checkins]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/comfort-posture.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/comfort-posture.svg",
      "svg_mini": "knowledge/concepts/visuals/comfort-posture.mini.svg"
    },
    {
      "id": "command-review-tribunal",
      "title": "Command-review tribunal (the Thing)",
      "category": "Security",
      "kind": "ravenclaude-built",
      "order": 30,
      "summary": "An opt-in panel of reviewer seats that votes ALLOW/EDIT/DENY on shell commands instead of interrupting you.",
      "see_also": [
        "comfort-posture",
        "permission-layers",
        "hook-lifecycle"
      ],
      "widget": null,
      "try_it": {
        "label": "Run a command through it in Test a command",
        "href": "#/simulator"
      },
      "node_links": {
        "B": "comfort-posture",
        "E": "model-diversity",
        "L": "saga-log"
      },
      "last_verified": "2026-05-26",
      "refresh_when": "The tribunal's tier model, seat roster, or gate_floor semantics change.",
      "sources": [
        {
          "label": "thing skill (operating reference)",
          "url": "plugins/ravenclaude-core/skills/thing/SKILL.md"
        },
        {
          "label": "Tribunal design",
          "url": "docs/tribunal-review-feature-design.md"
        }
      ],
      "body_md": "**Command review** — codename *the Thing* — is an opt-in panel of reviewer agents that adjudicates shell commands instead of stopping to ask you. It sits **on top of** comfort-posture: posture sets the policy (allow/ask/deny per category); the tribunal is the adjudicator you switch on for a category so a verdict lands in seconds.\n\nRouting is **tiered**. Every command resolves to `low → medium → high → extreme` (its category base tier, bumped by a deterministic high/critical concern). A clean `low` read runs **no panel at all** (zero cost); seat count and the confidence bar escalate with the tier. Up to three seats run in parallel — **Forseti** (security), **Mímir** (code), **Heimdall** (injection) — with **Thor** (architect) convened only on a split.\n\nThe **`gate_floor`** knob (default `high`) is the lowest tier whose *confident ALLOW* is surfaced to you as an `ask`. DENY still blocks and EDIT still rewrites autonomously, so the tribunal pre-filters the dangerous and the fixable before either reaches you. Two hard overrides ignore the knob: **reads are never surfaced**, and **irreversible high-blast allows always are**. An abstaining panel always fails **closed**. It can never relax the `security_deny` floor.",
      "diagram": "flowchart TD\n  A[Bash PreToolUse] --> B{category toggled on?}\n  B -- no --> Z[normal flow]\n  B -- yes --> C[classify: category · tier · routing]\n  C -- unarguable critical --> DENY[DENY · no LLM]\n  C -- clean low read --> ALLOW[allow · no panel]\n  C -- tier ≥ medium --> E[convene seats in parallel]\n  E -- split / low confidence --> T[convene Thor]\n  T --> AGG[aggregate]\n  E --> AGG\n  AGG -- deny / critical --> DENY\n  AGG -- edit safe --> EDIT[allow + rewrite]\n  AGG -- allow --> GATE{tier ≥ gate_floor<br/>or high-blast?}\n  GATE -- yes --> ASK[ask · you confirm]\n  GATE -- no --> ALLOW\n  DENY & ALLOW & EDIT & ASK --> L[(Sága log)]\n  class DENY,ALLOW,EDIT,ASK,E,T built\n  class GATE fact",
      "diagram_mini": "flowchart LR\n  C[Command] --> P[Panel<br/>1–3 seats]\n  P --> V{Verdict}\n  V --> A[allow / edit / deny / ask]\n  class P,A built",
      "steps": [
        {
          "caption": "A Bash call hits PreToolUse; if its category is toggled on, the Thing convenes.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-1.svg"
        },
        {
          "caption": "Classify the command: category then tier (low/medium/high/extreme) then routing.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-2.svg"
        },
        {
          "caption": "tier at least medium convenes up to 3 seats in parallel (Forseti, Mimir, Heimdall); Thor on a split.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-3.svg"
        },
        {
          "caption": "Aggregate the votes: deny blocks, edit rewrites, allow continues.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-4.svg"
        },
        {
          "caption": "gate_floor (default high): a confident ALLOW at or above it is surfaced to you as an ask.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-5.svg"
        },
        {
          "caption": "Every verdict writes one entry to the Saga audit log.",
          "diagram": "flowchart LR\n  N1[Bash call] --> N2[Classify tier] --> N3[Panel seats] --> N4[Aggregate] --> N5[Gate floor] --> N6[Saga log]\n  class N6 built",
          "svg": "knowledge/concepts/visuals/command-review-tribunal.step-6.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/command-review-tribunal.svg",
      "svg_mini": "knowledge/concepts/visuals/command-review-tribunal.mini.svg"
    },
    {
      "id": "saga-log",
      "title": "The Sága audit log",
      "category": "Security",
      "kind": "ravenclaude-built",
      "order": 32,
      "summary": "Every command-review verdict writes one JSON entry — command, category, tier, per-seat votes, concerns, final verdict — under .ravenclaude/runs/thing/.",
      "see_also": [
        "command-review-tribunal",
        "model-diversity"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The Sága log entry shape or its on-disk location changes.",
      "sources": [
        {
          "label": "thing skill (operating reference)",
          "url": "plugins/ravenclaude-core/skills/thing/SKILL.md"
        }
      ],
      "body_md": "Every tribunal verdict — allow, edit, deny, or ask — writes exactly **one JSON entry** to `.ravenclaude/runs/thing/<id>.json`, the **Sága log**. The entry records the command, its category, the resolved tier, each seat's verdict, the concerns cited, the final verdict (and the revised command on an EDIT), and the duration.\n\nThis is the observability substrate: it turns an otherwise opaque \"the panel decided\" into an auditable trail you can read after the fact to tune the panel, the `gate_floor`, or the bypass patterns. It's **gitignored by default** — the log is local operational data, not something to commit.",
      "diagram": "flowchart TD\n  V[Tribunal renders a verdict] --> E[Write one JSON entry]\n  E --> F[command · category · tier<br/>per-seat votes · concerns<br/>final verdict · duration]\n  F --> D[(.ravenclaude/runs/thing/&lt;id&gt;.json)]\n  D --> U[Audit · tune panel / gate_floor / bypass]\n  class V,E,F,D,U built",
      "diagram_mini": "flowchart LR\n  V[Verdict] --> J[(Sága JSON entry)]\n  J --> A[auditable trail]\n  class V,J,A built",
      "steps": [
        {
          "caption": "The tribunal renders a verdict: allow / edit / deny / ask.",
          "diagram": "flowchart LR\n  N1[Verdict] --> N2[One JSON entry] --> N3[Records fields] --> N4[Gitignored] --> N5[Audit and tune]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/saga-log.step-1.svg"
        },
        {
          "caption": "It writes exactly one JSON entry under .ravenclaude/runs/thing/.",
          "diagram": "flowchart LR\n  N1[Verdict] --> N2[One JSON entry] --> N3[Records fields] --> N4[Gitignored] --> N5[Audit and tune]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/saga-log.step-2.svg"
        },
        {
          "caption": "The entry records command, category, tier, per-seat votes, concerns, verdict, duration.",
          "diagram": "flowchart LR\n  N1[Verdict] --> N2[One JSON entry] --> N3[Records fields] --> N4[Gitignored] --> N5[Audit and tune]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/saga-log.step-3.svg"
        },
        {
          "caption": "Gitignored by default — local operational data, not committed.",
          "diagram": "flowchart LR\n  N1[Verdict] --> N2[One JSON entry] --> N3[Records fields] --> N4[Gitignored] --> N5[Audit and tune]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/saga-log.step-4.svg"
        },
        {
          "caption": "It's the audit trail you read to tune the panel, gate_floor, or bypass patterns.",
          "diagram": "flowchart LR\n  N1[Verdict] --> N2[One JSON entry] --> N3[Records fields] --> N4[Gitignored] --> N5[Audit and tune]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/saga-log.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/saga-log.svg",
      "svg_mini": "knowledge/concepts/visuals/saga-log.mini.svg"
    },
    {
      "id": "model-diversity",
      "title": "Model diversity on the panel",
      "category": "Security",
      "kind": "ravenclaude-built",
      "order": 34,
      "summary": "When ≥2 reviewer seats convene, the tribunal guarantees ≥2 distinct model backbones run — so one model's blind spot can't pass the whole panel.",
      "see_also": [
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The model-diversity guarantee or its auto-reassignment behavior changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Tribunal assessment & improvement plan",
          "url": "docs/tribunal-assessment-and-improvement-plan.md"
        }
      ],
      "body_md": "A panel of reviewers is only as strong as its diversity. If every seat runs on the same model, a single model's blind spot — a shared hallucination or a class of injection it doesn't catch — can pass the **whole** panel unnoticed. That's the **anti-correlated-hallucination** failure mode.\n\nSo the engine enforces a **model-diversity rule**: whenever **two or more seats convene, at least two distinct model backbones run**. If a `panel:` config override happens to collapse the seats onto one model, the engine **auto-reassigns one seat to a different, equal-or-stronger model** rather than letting a monoculture review the command. It's proven by Gate 22.",
      "diagram": "flowchart TD\n  P{≥2 seats convened?} -- no --> ONE[Single seat · its own model]\n  P -- yes --> CHK{≥2 distinct models?}\n  CHK -- yes --> OK[Panel runs as configured]\n  CHK -- no --> RE[Auto-reassign one seat to a<br/>different equal-or-stronger model]\n  RE --> OK\n  class P,CHK fact\n  class ONE,OK,RE built",
      "diagram_mini": "flowchart LR\n  S[≥2 seats] --> D{≥2 models?}\n  D -- no --> R[reassign one]\n  D -- yes --> OK[run]\n  class R,OK built",
      "steps": [
        {
          "caption": "Whenever two or more seats convene...",
          "diagram": "flowchart LR\n  N1[Two plus seats] --> N2[Two plus models] --> N3[Run configured] --> N4[Reassign one] --> N5[No monoculture]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/model-diversity.step-1.svg"
        },
        {
          "caption": "Are two or more distinct model backbones running?",
          "diagram": "flowchart LR\n  N1[Two plus seats] --> N2[Two plus models] --> N3[Run configured] --> N4[Reassign one] --> N5[No monoculture]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/model-diversity.step-2.svg"
        },
        {
          "caption": "Yes and the panel runs as configured.",
          "diagram": "flowchart LR\n  N1[Two plus seats] --> N2[Two plus models] --> N3[Run configured] --> N4[Reassign one] --> N5[No monoculture]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/model-diversity.step-3.svg"
        },
        {
          "caption": "No (a config collapsed them) and one seat is auto-reassigned to a different, stronger model.",
          "diagram": "flowchart LR\n  N1[Two plus seats] --> N2[Two plus models] --> N3[Run configured] --> N4[Reassign one] --> N5[No monoculture]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/model-diversity.step-4.svg"
        },
        {
          "caption": "So one model's blind spot can't pass the whole panel. Proven by Gate 22.",
          "diagram": "flowchart LR\n  N1[Two plus seats] --> N2[Two plus models] --> N3[Run configured] --> N4[Reassign one] --> N5[No monoculture]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/model-diversity.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/model-diversity.svg",
      "svg_mini": "knowledge/concepts/visuals/model-diversity.mini.svg"
    },
    {
      "id": "decision-review",
      "title": "Decision-review tribunal",
      "category": "Security",
      "kind": "ravenclaude-built",
      "order": 36,
      "summary": "A sibling tribunal votes yes/no/defer on yes-or-no DECISIONS before they reach you — binding only if you opt in, and high-blast calls always defer.",
      "see_also": [
        "command-review-tribunal",
        "model-diversity"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {
        "PANEL": "command-review-tribunal"
      },
      "last_verified": "2026-05-26",
      "refresh_when": "The decision-review modes, the defer rules, or the high-blast carve-out change.",
      "sources": [
        {
          "label": "Post-PR decision review",
          "url": "docs/post-pr-decision-review.md"
        },
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "The command-review tribunal adjudicates *shell commands*; the **decision-review** tribunal adjudicates *yes/no decisions* — the kind an agent would otherwise interrupt you with. The `decision-review` skill convenes the same seats on a yes/no question (engine: `thing-decide.py`) and returns **`yes` / `no` / `defer`**. A binding yes/no is acted on without pausing you; a `defer` asks you. The panel defers genuine preferences, low-confidence or split calls, and anything high-blast.\n\nTwo guardrails: **high-blast / irreversible decisions never auto-resolve** — force-push, deletes, prod actions, the `security_deny` family always `defer` to you, regardless of mode. And the mode knob `decision_review: off | advisory | binding` is **off by default**, so nothing is auto-decided unless you opt in. (The seats run via `claude -p`; without it the panel abstains and fails safe to `defer`.) After each PR, a retrospective routes the PR's decisions through the same panel and logs the verdicts.",
      "diagram": "flowchart TD\n  Q[A yes/no decision] --> HB{high-blast /<br/>irreversible?}\n  HB -- yes --> DEFER[defer · always ask you]\n  HB -- no --> MODE{decision_review mode}\n  MODE -- \"off (default)\" --> ASK[ask you]\n  MODE -- \"binding\" --> PANEL[convene seats · thing-decide.py]\n  PANEL --> V{yes / no / defer}\n  V -- yes/no --> ACT[act without pausing you]\n  V -- defer --> DEFER\n  class Q,HB,MODE fact\n  class DEFER,ASK,PANEL,V,ACT built",
      "diagram_mini": "flowchart LR\n  Q[yes/no decision] --> P[panel]\n  P --> V[yes / no / defer]\n  class P,V built",
      "steps": [
        {
          "caption": "A yes/no decision the agent would otherwise interrupt you with.",
          "diagram": "flowchart LR\n  N1[Yes or no] --> N2[High blast defers] --> N3[Mode gate] --> N4[Panel votes] --> N5[Act or defer]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/decision-review.step-1.svg"
        },
        {
          "caption": "High-blast or irreversible? It always defers to you (force-push, deletes, prod).",
          "diagram": "flowchart LR\n  N1[Yes or no] --> N2[High blast defers] --> N3[Mode gate] --> N4[Panel votes] --> N5[Act or defer]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/decision-review.step-2.svg"
        },
        {
          "caption": "Mode off (default) asks you; mode binding convenes the seats (thing-decide.py).",
          "diagram": "flowchart LR\n  N1[Yes or no] --> N2[High blast defers] --> N3[Mode gate] --> N4[Panel votes] --> N5[Act or defer]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/decision-review.step-3.svg"
        },
        {
          "caption": "The panel returns yes / no / defer.",
          "diagram": "flowchart LR\n  N1[Yes or no] --> N2[High blast defers] --> N3[Mode gate] --> N4[Panel votes] --> N5[Act or defer]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/decision-review.step-4.svg"
        },
        {
          "caption": "yes/no acts without pausing you; defer asks you.",
          "diagram": "flowchart LR\n  N1[Yes or no] --> N2[High blast defers] --> N3[Mode gate] --> N4[Panel votes] --> N5[Act or defer]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/decision-review.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/decision-review.svg",
      "svg_mini": "knowledge/concepts/visuals/decision-review.mini.svg"
    },
    {
      "id": "runaway-brake",
      "title": "Runaway brake — the depth guard",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 30,
      "summary": "A model-free PreToolUse brake that pauses the agent when it thrashes (too many identical calls in a row) or blows a total-call ceiling — with a read-only carve-out.",
      "see_also": [
        "dod-gate",
        "task-scope-gate",
        "containment-posture",
        "command-review-tribunal",
        "comfort-posture"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The max_consecutive/max_total defaults, the read-only carve-out allowlist, or the per-session counter path change.",
      "sources": [
        {
          "label": "runaway-brake.sh hook",
          "url": "plugins/ravenclaude-core/hooks/runaway-brake.sh"
        },
        {
          "label": "ravenclaude-core constitution — Auto-mode guardrails",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "The **runaway brake** is the **depth guard**: it stops an agent from disappearing down a rabbit hole — looping on a fabricated error, or grinding through hundreds of calls without converging. It is a `PreToolUse` hook that runs *before every tool call*, counts the calls per session, and **pauses** (a deny) when one of two limits trips. It exists because Claude Code's native `auto`-mode brake (the 3-consecutive / 20-total block) is Anthropic-API-only, so it can't protect the model-agnostic GitHub Copilot CLI surface (Claude / ChatGPT / Grok). This hook is the portable, **model-free** equivalent — there is no LLM in the loop, just a counter.\n\nTwo limits, both tunable in `.ravenclaude/comfort-posture.yaml`. **`max_consecutive`** (default 8) trips when the agent makes the *same* tool call — byte-identical name and input — that many times in a row; that repetition is the \"stuck in a loop\" signal. **`max_total`** (default 1200) is a generous per-session ceiling on *all* calls, the backstop against a slow grind that never repeats exactly. State lives in a per-session counter file under `.ravenclaude/runs/thing/runaway/`, so a brand-new session always starts fresh. The whole hook is **opt-in and fail-safe**: with no posture file it exits immediately (one `stat`, zero cost), and `runaway: off` disables it entirely.\n\nThe load-bearing subtlety is the **read-only carve-out**. A legitimate startup burst of read-only calls — repeated `git log`, `ls`, `cat`, `grep` — is not a runaway, so it must not trip the consecutive-loop counter. A call counts as read-only when the tool is `Read`/`Grep`/`Glob`/`NotebookRead`, *or* it's a `Bash` command matching a **strict, anchored, fail-closed** allowlist of obviously-non-mutating programs. A read-only call is **transparent** to the loop counter (it neither increments nor resets the consecutive count) but **still increments the total** — so the session ceiling keeps bounding *every* call regardless of type. Any doubt fails closed: a shell metacharacter that could chain to a mutating command (`&&`, `;`, `|`, `>`, `` ` ``) or any mutating token anywhere in the string forces the call to count as a normal (non-read-only) call.",
      "diagram": "flowchart TD\n  A[PreToolUse: a tool call] --> B{posture present<br/>and runaway not off?}\n  B -- no --> Z[allow · zero cost]\n  B -- yes --> C{read-only call?<br/>strict allowlist}\n  C -- yes --> T[total++ only<br/>consec untouched]\n  C -- no --> D[total++; same as last?<br/>consec++, else reset]\n  T --> G{consec ≥ max OR<br/>total ≥ ceiling?}\n  D --> G\n  G -- yes --> DENY[PAUSE · deny · explain]\n  G -- no --> ALLOW[allow]\n  class T,D,DENY,ALLOW built\n  class B,C,G fact",
      "diagram_mini": "flowchart LR\n  C[tool call] --> K{thrash or<br/>ceiling?}\n  K -- yes --> P[pause]\n  K -- no --> A[allow]\n  class P,A built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/runaway-brake.svg",
      "svg_mini": "knowledge/concepts/visuals/runaway-brake.mini.svg"
    },
    {
      "id": "dod-gate",
      "title": "Definition-of-done gate",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 31,
      "summary": "A Stop-hook gate that runs your configured test/build/lint command when the agent tries to finish and blocks the stop until it passes — self-limited so it can't deadlock.",
      "see_also": [
        "runaway-brake",
        "task-scope-gate",
        "containment-posture",
        "command-review-tribunal",
        "comfort-posture"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The Stop-block mechanism, the max_blocks force-allow cap, the source-changed filter, or the first-run trust gate change.",
      "sources": [
        {
          "label": "dod-gate.sh hook",
          "url": "plugins/ravenclaude-core/hooks/dod-gate.sh"
        },
        {
          "label": "ravenclaude-core constitution — Auto-mode guardrails",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "The **definition-of-done gate** closes the gap that command review can't see: it gates work **correctness**, not command safety. The Thing tribunal asks \"is this command dangerous?\"; this gate asks \"is the work actually finished, or does it just *look* finished?\" It is a `Stop` hook — it fires when the agent tries to end its turn — and if a `definition_of_done.cmd` is configured in `.ravenclaude/comfort-posture.yaml`, it runs that command (your tests, build, lint) and **blocks the stop until the command exits zero**. That turns \"looks done\" into \"is done\" without the human having to be the verification loop.\n\nThe gate is **scoped and self-limiting** so it never becomes an obstacle. It only fires when *source files actually changed* this session (it inspects `git status --porcelain` for code extensions), so a docs-only or read-only session ends cleanly. When the command fails, the gate feeds the tail of the failing output back to the agent so it can fix the problem and try again. To guarantee it can't deadlock, it blocks at most **`max_blocks`** (default 8) consecutive times, then **force-allows** the stop with a warning — Claude Code force-overrides Stop after 8 anyway, but Copilot CLI gives no such guarantee, so the cap is built in. With no `definition_of_done.cmd` set, the gate is inert and the advisory `remind-tests.sh` nudge handles the reminder instead.\n\nOne safety detail: the configured command is shell-executed, and it comes from a YAML field a malicious pull request could edit. So before the *first* run per session for a given command value, the gate refuses to execute and surfaces the literal command plus a one-line `touch` step to authorize it (or set `definition_of_done.trusted: true` to skip the prompt permanently). Rotating the command or starting a new session re-triggers the check. Like the other guardrails it is **opt-in and deterministic** — the verdict is just a command's exit code, no model in the loop — so it ports unchanged to Copilot through the adapter's `stop` mode.",
      "diagram": "flowchart TD\n  A[Stop: agent tries to finish] --> B{source changed<br/>and DoD cmd set?}\n  B -- no --> Z[allow stop]\n  B -- yes --> F{first run of this cmd?<br/>trust gate}\n  F -- not authorized --> ASK[refuse · show cmd · ask to authorize]\n  F -- authorized --> R[run the DoD command]\n  R -- exit 0 --> PASS[allow stop · work is done]\n  R -- fails --> C{blocks < max_blocks?}\n  C -- yes --> BLOCK[block stop · feed failure back]\n  C -- no --> FORCE[force-allow + warning]\n  class ASK,R,PASS,BLOCK,FORCE built\n  class B,F,C fact",
      "diagram_mini": "flowchart LR\n  S[Stop] --> R[run tests/lint]\n  R -- pass --> A[allow]\n  R -- fail --> B[block · keep working]\n  class A,B,R built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/dod-gate.svg",
      "svg_mini": "knowledge/concepts/visuals/dod-gate.mini.svg"
    },
    {
      "id": "silent-green-defects",
      "title": "Silent-green defects",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 31,
      "summary": "A silent-green defect is a check that passes loudest exactly when it should fail — a skipped suite, a grep over a deleted file, a stale build dir. Ask what it does when its subject is ABSENT.",
      "see_also": [
        "dod-gate",
        "audit-gates",
        "task-scope-gate",
        "runaway-brake"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-07-21",
      "refresh_when": "The audit-gates must-fail-half convention changes, or the definition-of-done gate stops being the Stop-event verification surface.",
      "sources": [
        {
          "label": "CI gate audit — every gate must fail on a known-bad fixture",
          "url": "docs/best-practices/ci-gate-audit.md"
        },
        {
          "label": "audit-gates.sh — the meta-test that proves each gate has teeth",
          "url": "scripts/audit-gates.sh"
        },
        {
          "label": "dod-gate.sh — the definition-of-done Stop gate",
          "url": "plugins/ravenclaude-core/hooks/dod-gate.sh"
        }
      ],
      "body_md": "A **silent-green defect** is a check that **passes loudest exactly when it should fail**. Not a flaky test, not a check that misses an edge case — a check whose *failure mode is success*. The build is green, the suite is green, CI is green, and the thing being guarded is gone.\n\nThis is the worst class of bug in a codebase full of automation, because every signal you would normally use to find a bug is the thing that is lying to you. You cannot test your way out of it by running the tests harder. It survives review because the diff looks correct in isolation, and it survives CI because CI is the victim.\n\n## Six real ones, all from a single project\n\n1. **A test suite that skips itself when its build artifact is missing.** The suite opened with a `skipIf()` guarding against a missing `dist/` directory. A skipped suite reports **green**. So every local run without a prior build reported success while asserting *nothing at all* — and the one environment that did build (CI) was the only place the assertions ever ran.\n2. **A grep-based gate over a file that no longer exists.** The gate ran a pattern over a source file and failed if the match count was wrong. Someone deleted the file. Zero matches. The gate **passed** — precisely, and only, because the thing it was guarding had been deleted.\n3. **Assertions run against a stale build directory.** The tests read the last successful build's output, not the current source. They described markup that had been removed weeks earlier. Every assertion passed, honestly and confidently, about a page that no longer existed.\n4. **A guard with no `else`.** `if (condition) { handle(); }` — and the unhandled case just falls through. No error, no log, no branch taken. The function returns as though it did its job.\n5. **`LIMIT 1` with no `ORDER BY`.** The database is free to return any row it likes. In testing, against a small table, it always returns the one you expect. In production, against a large one, it doesn't — occasionally, and unreproducibly.\n6. **Copy that promises behaviour no code implements.** A file-picker button, a nice upload affordance, a \"your file has been received\" message — with nothing behind it that uploads anything. Nothing failed, because nothing ran.\n\nDifferent-looking bugs, one shape.\n\n## The diagnostic that finds all six\n\nAsk one question of every check you own:\n\n> **What does this check do when the thing it checks is ABSENT?**\n\nAbsent means: the file was deleted, the build never ran, the fixture didn't load, the config key isn't set, the branch was never taken, the row set came back empty, the feature flag is off. If the honest answer is *\"it passes\"*, you have a silent-green defect — today, whether or not anything is broken yet.\n\nRun it over each of the six and it lands every time: the skipped suite (no build → pass), the grep (no file → pass), the stale directory (no fresh build → pass), the missing `else` (no branch → pass), the `LIMIT 1` (no ordering → whatever, and \"whatever\" looks like a pass), the copy (no handler → pass).\n\n## The fix pattern: assert existence first, fail closed\n\nTwo moves, in this order.\n\n**Assert the subject exists before you assert anything about it.** A check must first prove it has something to check: the artifact is present, the file is where the gate expects it, the build is newer than the source, the query returned a row, the branch was taken. Make that a *hard* assertion, not a skip. \"I couldn't find it\" must be a failure, never an absence of failure.\n\n**Fail closed.** When the check can't run — missing dependency, missing input, unhandled case — the outcome is a **failure**, not a pass. A tool that isn't installed makes the gate red, or at minimum shouts *\"this is not a pass.\"* A guard with no matching branch raises. `LIMIT 1` gets an `ORDER BY` so \"the row\" is a defined thing rather than a coincidence.\n\n**And prove the check has teeth.** This is what a *must-fail half* is for: run every gate against a known-bad fixture and require it to go **red**, alongside the known-good fixture that requires it to go green. A gate that has only ever been observed passing has never been observed working. Both halves, or you're trusting a colour.",
      "diagram": "flowchart TD\n  C[A check runs] --> Q{Is the thing it checks<br/>present at all?}\n  Q -- \"absent · missing file, no build,<br/>empty result, branch not taken\" --> S{What does it report?}\n  S -- \"PASS · skip, zero matches,<br/>stale data, silent fallthrough\" --> BAD[SILENT-GREEN DEFECT<br/>green when it should be red]\n  S -- \"FAIL · loud, explicit\" --> GOOD[Fails closed · trustworthy]\n  Q -- present --> N[Normal check · pass or fail on merit]\n  BAD --> F1[Fix 1 · assert existence FIRST]\n  F1 --> F2[Fix 2 · fail closed when it can't run]\n  F2 --> F3[Fix 3 · must-fail half proves teeth]\n  F3 --> GOOD\n  class BAD,GOOD,N,F1,F2,F3 built\n  class Q,S fact",
      "diagram_mini": "flowchart LR\n  A[thing absent] --> C{check says?}\n  C -- pass --> B[silent green]\n  C -- fail --> G[fails closed]\n  class B,G built",
      "steps": [
        {
          "caption": "A silent-green defect is a check that passes loudest exactly when it should fail.",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-1.svg"
        },
        {
          "caption": "Six from one project: skipped suite, grep over a deleted file, stale build dir, no else, LIMIT 1, empty copy.",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-2.svg"
        },
        {
          "caption": "The one diagnostic: what does this check do when the thing it checks is ABSENT?",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-3.svg"
        },
        {
          "caption": "Fix 1 — assert the subject exists before asserting anything about it. A skip is not a pass.",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-4.svg"
        },
        {
          "caption": "Fix 2 — when the check can't run, that is a failure. Never a quiet pass.",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-5.svg"
        },
        {
          "caption": "Fix 3 — a must-fail half: the gate must go RED on a known-bad fixture, or it has never been observed working.",
          "diagram": "flowchart LR\n  N1[The shape] --> N2[Six real cases] --> N3[Ask: what if ABSENT?] --> N4[Assert existence first] --> N5[Fail closed] --> N6[Prove teeth]\n  class N6 built",
          "svg": "knowledge/concepts/visuals/silent-green-defects.step-6.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/silent-green-defects.svg",
      "svg_mini": "knowledge/concepts/visuals/silent-green-defects.mini.svg"
    },
    {
      "id": "task-scope-gate",
      "title": "Task-scope gate — the breadth guard",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 32,
      "summary": "An optional per-task write blast-radius gate: a write to a path matching no in_scope glob in .ravenclaude/task-scope.json is denied — independent of, and composable with, the repo-layout policy.",
      "see_also": [
        "runaway-brake",
        "dod-gate",
        "containment-posture",
        "layout-hook",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The task-scope.json shape (in_scope/spec), the deny ordering vs the layout check, or the fail-safe rules change.",
      "sources": [
        {
          "label": "enforce-layout.sh hook",
          "url": "plugins/ravenclaude-core/hooks/enforce-layout.sh"
        },
        {
          "label": "ravenclaude-core constitution — Auto-mode guardrails",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "The **task-scope gate** is the **breadth guard**: where the runaway brake bounds how *deep* an agent goes and the definition-of-done gate bounds *correctness*, this bounds how *wide* it spreads. It answers \"for the task at hand, which files is the agent allowed to touch?\" You declare the current task's write blast radius in `.ravenclaude/task-scope.json` — `{\"in_scope\": [globs], \"spec\": \"SPEC.md\"}` — and any `Write`/`Edit`/`MultiEdit` to a path matching **none** of the `in_scope` globs is denied, with the spec named in the hint. You copy the file in per task and delete it when the task is done.\n\nIt rides on the **same** `PreToolUse` hook that enforces the repo layout (`enforce-layout.sh`), so it ships with **zero new wiring** — that hook was already registered on writes under both Claude Code and Copilot. But the two policies are **independent and composable**. The repo-layout policy (`.repo-layout.json`) is about repo *structure* — where any file may live, the marketplace's permanent discipline. Task-scope is about *this task's* radius — a temporary, much tighter fence. Either, both, or neither may be present; a write must satisfy whichever policies *are* configured. The task-scope check deliberately runs **first**, so it is never short-circuited by the layout policy's forbid-only early-exit.\n\nThe gate is **fail-safe** in every direction: an absent file, an empty `in_scope` list, or unparseable JSON is a silent no-op (the write is allowed). Glob matching uses bash pattern semantics — a single `*` matches across slashes — and a path containing `..` is refused up front as a path-traversal scrub. Every deny emits a structured line to the hook-event substrate (the same log the Heimdall and Víðarr dashboard tabs read), so a blocked write is observable after the fact, not just a wall-message.",
      "diagram": "flowchart TD\n  A[PreToolUse: Write/Edit/MultiEdit] --> B{task-scope.json present<br/>with a non-empty in_scope?}\n  B -- no --> L[fall through to repo-layout check]\n  B -- yes --> M{path matches<br/>an in_scope glob?}\n  M -- yes --> L\n  M -- no --> DENY[deny · cite the spec · suggest adding the glob]\n  L --> OK[allow if layout permits]\n  class DENY,OK,L built\n  class B,M fact",
      "diagram_mini": "flowchart LR\n  W[write path] --> S{in task scope?}\n  S -- no --> D[deny]\n  S -- yes --> A[allow]\n  class D,A built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/task-scope-gate.svg",
      "svg_mini": "knowledge/concepts/visuals/task-scope-gate.mini.svg"
    },
    {
      "id": "web-access-guardrail",
      "title": "Website access guardrail",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 33,
      "summary": "A committed allow/deny list of websites the agent honors, plus a four-option prompt (just once / this session / permanently / deny) the first time it needs an unlisted domain.",
      "see_also": [
        "runaway-brake",
        "dod-gate",
        "task-scope-gate",
        "containment-posture",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The four-option menu, the web-access.yaml allow/deny shape, the per-session allow file, or the subdomain-match rule change.",
      "sources": [
        {
          "label": "guard-web-access.sh hook",
          "url": "plugins/ravenclaude-core/hooks/guard-web-access.sh"
        },
        {
          "label": "ravenclaude-core constitution — Website access",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "The **website-access guardrail** governs which sites an agent may fetch. It has two halves: a committed **allow/deny list** the agent honors, and a **four-option choice** it surfaces the first time it needs a domain that isn't on either list. The lists live in `.ravenclaude/web-access.yaml` as plain YAML (`allow: [domains]`, `deny: [domains]`), and a rule matches the domain **and all its subdomains**. Because they're a plain file, they work both for Claude Code (enforced by the hook) and for any *other* CLI tool that clones the repo and reads the same file — it's a cross-tool interop layer, not a Claude-only setting.\n\nThe deterministic backstop is the `guard-web-access.sh` `PreToolUse(WebFetch)` hook. A **whitelisted** domain auto-allows with no prompt; a **blacklisted** domain is **blocked** (and emits a deny event to the substrate); an **unlisted** domain falls through to the agent's normal per-domain prompt. It's fail-safe: absent config or a missing parser makes it a no-op (ask as normal). One refinement: even a whitelisted domain gets a *first-use* \"allow this and subsequent fetches this session?\" ask, so a hostile edit to the YAML can't silently turn on exfiltration — set `web_access.trusted: true` to skip that once you've reviewed the list. The hook can't replace Claude Code's built-in permission dialog (no hook can), but it is the deterministic floor beneath it.\n\nThe behavioral half is the **four-option menu**. When the agent is about to fetch an unlisted domain it hasn't already cleared this session, it surfaces exactly four choices and then records the answer in the right place. **Just once** fetches now and writes nothing. **This session** appends the domain to the per-session file `.ravenclaude/runs/<session>/web-allow.txt` (the hook auto-allows it for the rest of the session, then it's cleared). **Permanently** appends to the `allow:` whitelist (persists, and propagates to other tools). **Deny** appends to the `deny:` blacklist (blocked from now on). So a *deny* lands on the blacklist and a *permanent* allow lands on the whitelist, exactly as named.",
      "diagram": "flowchart TD\n  A[WebFetch a domain] --> B{on a list?}\n  B -- deny list --> BLOCK[block · deny event]\n  B -- allow list --> ALLOW[auto-allow<br/>first-use ask once]\n  B -- unlisted --> Q[four-option prompt]\n  Q --> O1[Just once → fetch, write nothing]\n  Q --> O2[This session → session allow file]\n  Q --> O3[Permanently → allow: whitelist]\n  Q --> O4[Deny → deny: blacklist]\n  class BLOCK,ALLOW,O1,O2,O3,O4 built\n  class B fact",
      "diagram_mini": "flowchart LR\n  U[unlisted domain] --> M[4 options]\n  M --> R[once / session /<br/>permanent / deny]\n  class M,R built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/web-access-guardrail.svg",
      "svg_mini": "knowledge/concepts/visuals/web-access-guardrail.mini.svg"
    },
    {
      "id": "containment-posture",
      "title": "Containment posture — the OS boundary",
      "category": "Guardrails",
      "kind": "ravenclaude-built",
      "order": 34,
      "summary": "The OS/container/worktree boundary that bounds subprocesses an agent spawns — the layer the tribunal and the tool-layer guards structurally cannot provide.",
      "see_also": [
        "runaway-brake",
        "dod-gate",
        "task-scope-gate",
        "web-access-guardrail",
        "command-review-tribunal",
        "comfort-posture"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The container/worktree containment model, the seeded credential-store denies, or the Claude-only OS-sandbox caveat change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Containment posture",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "claude-code-permissions knowledge file",
          "url": "plugins/ravenclaude-core/knowledge/claude-code-permissions.md"
        }
      ],
      "body_md": "Every other guardrail in this cluster — the runaway brake, the definition-of-done gate, the task-scope gate, and command review itself — is a **model-layer** guard: it gates the *agent's own tools*. That leaves one gap none of them can close. A `deny` on `Read(~/.ssh/**)` stops the agent's `Read` tool, but it does **not** stop a script the agent writes and then runs — the subprocess inherits the shell's access, not the agent's permission rules. Only the **operating system** can hold that line, because the OS enforces it whether or not the command was correctly labeled, mislabeled, or flipped by an injection.\n\nThat is **containment posture**: the boundary lives *below* the model. The sanctioned, model-agnostic containment is **the devcontainer this marketplace scaffolds plus a git worktree** for risky or parallel runs. The container is the real, OS-enforced blast radius, and it behaves identically under Claude Code, GitHub Copilot CLI, or any other host — that portability is exactly why it's the recommended posture. On top of it, the balanced comfort-posture seed adds **tool-layer denies** for host credential stores outside the repo (`~/.ssh`, `~/.aws`, `~/.config/gcloud`, `~/.azure`, `~/.kube/config`, `~/.docker/config.json`) so the agent's own Read tool also refuses them — but those are tool-layer, *not* OS isolation, so they don't close the subprocess gap on their own.\n\nThe honest caveat completes the picture. Claude Code *can* add an OS sandbox (Seatbelt/bubblewrap, `denyRead`/`denyWrite`) that genuinely contains subprocesses — but there's no evidence Copilot CLI honors it, so under Copilot the container/worktree is the containment, **not** the sandbox. RavenClaude deliberately does not write a Claude-only sandbox config and present it as portable. The takeaway for working safely: when a run is risky, put it inside a container or a worktree so the *floor* of what it can touch is set by the OS, and treat the model-layer guards as the layers above that floor — they reduce mistakes, but the container is what survives one.",
      "diagram": "flowchart TD\n  A[Agent] -->|tool call| T{model-layer guards<br/>tribunal · denies}\n  T -- gated --> OK[bounded tool call]\n  A -->|writes & runs a script| S[subprocess]\n  S -. not gated by model layer .-> X[(host files · creds)]\n  C[Container / worktree boundary<br/>OS-enforced] --> S\n  C --> X\n  class OK,C built\n  class T fact",
      "diagram_mini": "flowchart LR\n  M[model-layer guards] --> G[gate tools]\n  C[OS container/worktree] --> S[bound subprocesses]\n  class G,S built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/containment-posture.svg",
      "svg_mini": "knowledge/concepts/visuals/containment-posture.mini.svg"
    },
    {
      "id": "event-substrate",
      "title": "The event substrate",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 40,
      "summary": "Two append-only JSONL logs — hook deny/warn verdicts and posture diffs — that make guardrail and posture activity observable after the fact. The read foundation the Norse tabs consume.",
      "see_also": [
        "heimdall",
        "vidarr",
        "norns",
        "run-state-monitor",
        "saga-log",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The JSONL line shape, the emitting hooks, or the two log paths change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Structured event substrate",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "_emit-event.sh (the shared emitter)",
          "url": "plugins/ravenclaude-core/hooks/_emit-event.sh"
        }
      ],
      "body_md": "Guardrail verdicts and posture changes used to vanish — they went to stderr or stdout and were gone the moment the turn ended. The **event substrate** fixes that with two **append-only JSONL logs** (one JSON object per line) that record what happened so a panel can read it *after the fact*. It is deliberately built **first**, as the shared emission convention, so every reader downstream consumes one format instead of each inventing its own.\n\nThe **hook-event log** (`hook-events.jsonl`, written per session under `.ravenclaude/runs/<session>/`) gets one line per **deny or warn verdict** from the shared sourced helper `_emit-event.sh`. Three hooks emit into it: the layout/scope hook (`enforce-layout.sh`), the destructive-command guard (`guard-destructive.sh`), and the recursive-spawn guard (`guard-recursive-spawn.sh`). The pure formatter is intentionally **not** wired — it has no verdict, so emitting per format would just flood the log. The **posture-event log** (`posture-events.jsonl`) gets one line per posture change from the comfort-posture translator, recording the added/removed permission rules as a diff. An identical reapply writes nothing, so the session-start reapply never floods it.\n\nBoth logs are **fail-safe and additive**: a telemetry write can never break the guardrail or posture apply that produced it. They are git-ignored (per-consumer). This is the read-side foundation the four observability tabs sit on — Heimdall reads the hook half, Víðarr interleaves both, Norns reads the scenario stream, and the push monitor tails the hook log live.",
      "diagram": "flowchart TD\n  H[Guardrail hooks<br/>deny / warn] --> E[_emit-event.sh]\n  E --> HJ[(hook-events.jsonl)]\n  P[Posture change] --> PE[posture diff]\n  PE --> PJ[(posture-events.jsonl)]\n  HJ --> R[Heimdall · Víðarr · monitor]\n  PJ --> R\n  class E,HJ,PE,PJ,R built\n  class H,P fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/event-substrate.svg",
      "svg_mini": null
    },
    {
      "id": "heimdall",
      "title": "Heimdall — perimeter alerts",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 41,
      "summary": "The perimeter-alarm tab: recent hook denials, CI runs, and version drift at a glance, topped by the tiered Gjallarhorn banner. A read-only mirror — it writes nothing.",
      "see_also": [
        "event-substrate",
        "vidarr",
        "norns",
        "nidhoggr",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "Heimdall's cards, the Gjallarhorn tiering, or the /__heimdall reader change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Heimdall",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "generate-dashboards.py (the Heimdall tab + reader)",
          "url": "scripts/generate-dashboards.py"
        }
      ],
      "body_md": "**Heimdall** is the watchman at the rainbow bridge — and the dashboard's perimeter-alarm tab answers his question in one glance: *what tripped, when, and why?* It is the **first reader** of the event substrate, and it is a **read-only mirror** — it writes nothing, to no log; it surfaces only what the hooks and manifests already emitted.\n\nFour cards. **Recent hook denials** reads the session hook-event logs (last 30 days) over a served `/__heimdall` endpoint, groups by hook, and tier-classifies each event. **Recent CI runs** fetches the GitHub Actions API client-side, with three honest states (rows when public, rate-limited on `403`, \"needs a token\" on a private `404`) — the empty state never masquerades as \"CI green.\" **Plugin version drift** compares each plugin's manifest version to the catalog, inlined at build time so it works on a static host too. And the **Gjallarhorn banner** is the horn itself: a tiered alarm derived from the hook-event tiers — **red** for an irrecoverable deny (force-push, `rm -rf`, publish), **amber** for any other deny (layout/scope), **grey** for a warn — with screen-reader urgency matched to the tier. It hides entirely when every source is clean.\n\nBecause the denial card needs file-system access, it is **served-mode only**; on a static host it shows an honest \"open the served dashboard\" prompt rather than a fake all-clear. Heimdall answers the operational \"what just tripped?\"; its sibling Víðarr answers the audit \"how did my posture change over time?\"",
      "diagram": "flowchart TD\n  S[(event substrate<br/>hook-events.jsonl)] --> H[Heimdall reader]\n  M[plugin manifests] --> H\n  CI[GitHub Actions API] --> H\n  H --> C[Denials · CI · drift cards]\n  H --> G[Gjallarhorn banner<br/>red / amber / grey]\n  class H,C,G built\n  class S,M,CI fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/heimdall.svg",
      "svg_mini": null
    },
    {
      "id": "vidarr",
      "title": "Víðarr — security log",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 42,
      "summary": "The security/posture audit tab: posture changes and security denials interleaved newest-first, filterable by time range and event type. Read-only — no edit or dismiss.",
      "see_also": [
        "event-substrate",
        "heimdall",
        "norns",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The Víðarr sources, the security-denial predicate, or the filters change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Víðarr",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "generate-dashboards.py (the Víðarr tab + reader)",
          "url": "scripts/generate-dashboards.py"
        }
      ],
      "body_md": "**Víðarr** is the silent, enduring god — and his tab is the **second reader** of the event substrate, reading the posture-event half. Where Heimdall answers the operational \"what guardrail tripped just now?\", Víðarr answers the audit question: *how did my security posture change over time, and what security-relevant denials happened?* It is a **read-only, filterable, chronological** audit log — no edit, no dismiss, no acknowledge.\n\nIt interleaves two sources into one newest-first table (columns: when / type / category / summary / source). **Posture changes** are every line of `posture-events.jsonl`, summarized as the permission-rule diff counts (e.g. \"+1 deny, +15 override\"). **Security-relevant hook denials** are the hook-event log filtered to **deny verdicts only** — warns are advisory and deliberately excluded (they live in Heimdall's grey tier, not the security audit). A single predicate is the one place that decides what counts as security-relevant.\n\nTwo filter controls narrow the view: a **time-range** select (24h / 7d / 30d / all, which re-fetches) and **event-type chips** (All / Posture changes / Security denials, applied client-side). Like Heimdall, both sources are per-consumer and git-ignored, so the data is **served-only** — a static host degrades to an honest empty state. When the perimeter has been quiet, the table says so plainly rather than pretending.",
      "diagram": "flowchart TD\n  PJ[(posture-events.jsonl)] --> V[Víðarr reader]\n  HJ[(hook-events.jsonl<br/>deny only)] --> V\n  V --> T[Chronological audit table]\n  T --> F[Filter: time range · event type]\n  class V,T,F built\n  class PJ,HJ fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/vidarr.svg",
      "svg_mini": null
    },
    {
      "id": "norns",
      "title": "Norns — plugin lineage",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 43,
      "summary": "The Urðr / Verðandi / Skuld past-present-future view of a plugin: lessons and history, current version and counts, and proposed roadmap — all read live, nothing inlined.",
      "see_also": [
        "event-substrate",
        "heimdall",
        "vidarr",
        "saga-log",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The three Norn columns, their sources, or the live-read contract change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Norns",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "generate-dashboards.py (the Norns tab + reader)",
          "url": "scripts/generate-dashboards.py"
        }
      ],
      "body_md": "The **Norns** weave fate from what was, what is, and what shall be — and their tab is the **third and final reader** of the event substrate, completing the loop by reading the scenario-event stream. It shows a plugin's life as a **read-only three-column past/present/future view**.\n\n**Urðr** (past) — \"Lessons & history\" — gathers the most recent scenario surfaces from the event log, any decision-log entries, and the last several commits. **Verðandi** (present) — \"Current\" — shows the live version, the active hook and rule counts, and the last release date. **Skuld** (future) — \"Proposed\" — shows the next version and roadmap plus open proposals naming the plugin; today most plugins declare no next version, so Skuld renders a gentle gated empty state until one does.\n\nThe load-bearing design choice: unlike Heimdall and Víðarr (which inline a small static slice), Norns inlines **nothing** at build time — *all* of its data is read live by a served endpoint. That is deliberate and non-negotiable, because the git-log and scenario data vary between a full local clone and CI's shallow checkout, and the dashboard is freshness-gated by exact byte match. Every source is guarded so a missing file or a git failure yields an empty section, never an error — and on a static host the columns degrade to an honest \"open the served dashboard\" prompt.",
      "diagram": "flowchart LR\n  G[git log · scenarios] --> U[Urðr<br/>past]\n  M[version · counts] --> V[Verðandi<br/>present]\n  R[next_version · roadmap] --> S[Skuld<br/>future]\n  U --> N[Lineage tab]\n  V --> N\n  S --> N\n  class U,V,S,N built\n  class G,M,R fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/norns.svg",
      "svg_mini": null
    },
    {
      "id": "nidhoggr",
      "title": "Níðhöggr — debt watch",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 44,
      "summary": "A low-noise maintenance card inside the Heimdall tab: stale plugins, uncovered hooks, superseded decisions, and TODO/FIXME in commit subjects — the slow debt that never trips an alarm.",
      "see_also": [
        "heimdall",
        "event-substrate",
        "vidarr",
        "norns",
        "audit-gates"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "Níðhöggr's four debt signals, its served reader, or its promote-to-tab threshold change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Níðhöggr 'Debt watch'",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "generate-dashboards.py (the Níðhöggr card + reader)",
          "url": "scripts/generate-dashboards.py"
        }
      ],
      "body_md": "**Níðhöggr** is the dragon gnawing at the roots of the world-tree — and the \"Debt watch\" card surfaces exactly that: the slow, quiet decay that no perimeter alarm ever catches. It lives as a **card inside the Heimdall tab** (not its own tab), and it carries both labels: \"Debt watch\" primary, \"Níðhöggr\" parenthetical.\n\nFour low-noise signals. **Stale plugins** — any plugin not version-bumped in 120+ days. **Uncovered hooks** — hooks referenced by neither a CI workflow nor the gate-audit harness; cross-checking *both* is what cuts the false positives down to the genuinely undercovered set. **Superseded decisions** — decision-log entries marked as superseded (none today). And **TODO/FIXME in commit subjects** — debt the team literally wrote into the history.\n\nIt reads **live** through a served endpoint rather than inlining at build time, because two of its signals are git-derived (commit dates, log history) and vary by clone depth — which would otherwise break the exact-match dashboard freshness gate, the same trap Norns navigates. Every source is guarded so a git failure yields an empty signal, never an error. It is a small card today; the plan is to promote it to a full tab only if the marketplace grows past roughly five plugins or the debt signals exceed about twenty entries.",
      "diagram": "flowchart TD\n  A[stale plugins] --> N[Níðhöggr reader]\n  B[uncovered hooks] --> N\n  C[superseded decisions] --> D[Debt watch card]\n  E[TODO/FIXME commits] --> N\n  N --> D\n  class N,D built\n  class A,B,C,E fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/nidhoggr.svg",
      "svg_mini": null
    },
    {
      "id": "mimir",
      "title": "Mímir — session state",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 45,
      "summary": "The Session tab: what Claude Code knows about this session — theme, model, permission mode, recent sessions — read from on-disk state, with honest in-process-only fields.",
      "see_also": [
        "event-substrate",
        "heimdall",
        "vidarr",
        "norns",
        "command-review-tribunal"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "Mímir's cards, the on-disk reachability map, or the in-process-only field list change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Mímir Session-state dashboard tab",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "skills/mimir/SKILL.md (the reader contract)",
          "url": "plugins/ravenclaude-core/skills/mimir/SKILL.md"
        }
      ],
      "body_md": "**Mímir** is the wise head at the well of knowledge — and his tab answers one question: *what does Claude Code actually know about **this** session?* It surfaces what's reachable from on-disk session state, replacing the need to remember `/status`, `/usage`, or `/theme`. Its defining discipline is **honesty about reachability** — it never fakes a value it can't read.\n\nFive cards, hydrated from a served endpoint when the tab opens. **Settings** — theme, configured and last-used model, and permission mode, plus an honest in-process pill for reasoning effort (which is runtime-only and rendered as an explainer, never a fake dash). **Current session** — matched by working directory and busy status. **Activity summary** — drawn from the stats cache, carrying a mandatory \"as of\" date because the cache can be up to a day stale. **Recent project sessions** — the newest few session logs, each with its id prefix, event count, output-token sum, and git branch. And **In-process only** — the explicit list of fields that simply don't exist on disk (the effort dial, plan tier, live status cache), each with a per-field explainer, so the agent never claims dashboard parity for something it can't read.\n\nA hard safety rule runs underneath: user-typed message content is **scrubbed** at the JSON boundary and never surfaced — only structural counts and metadata. Every dynamic byte is read live from the endpoint (nothing inlined at build time, the same freshness-gate reasoning as Norns). On a static host each card shows an honest \"open the served dashboard\" prompt while still rendering the layout so you see what's available once you switch.",
      "diagram": "flowchart TD\n  D[~/.claude session state] --> R[Mímir reader]\n  R --> S[Settings · session · activity]\n  R --> X[In-process-only<br/>honest empty states]\n  R --> SC[scrub user content]\n  class R,S,X,SC built\n  class D fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/mimir.svg",
      "svg_mini": null
    },
    {
      "id": "run-state-monitor",
      "title": "The run-state monitor",
      "category": "Observability",
      "kind": "ravenclaude-built",
      "order": 46,
      "summary": "The reactive PUSH monitor: streams guardrail signals as native notifications during a spawn-team run, so the agent reacts to a deny as it lands. Read-only, derived-labels-only, Claude-Code-only.",
      "see_also": [
        "event-substrate",
        "heimdall",
        "vidarr",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The monitor's emit whitelist, its on-skill-invoke scoping, or its fail-safe tailing change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Reactive run-state monitor",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "monitors/watch-run-state.sh",
          "url": "plugins/ravenclaude-core/monitors/watch-run-state.sh"
        }
      ],
      "body_md": "Heimdall, Víðarr, and Norns are **pull** surfaces — you open a tab and read what already happened. The **run-state monitor** is the **push** complement: it streams the same guardrail signals to Claude Code as **native notifications**, so the agent reacts to a deny or warn *as it lands* during a multi-agent run, without anyone asking it to go look. It is the marketplace's first member of a new component type, `monitors/`.\n\nIt is scoped **`on-skill-invoke:spawn-team`**, not `when: always` — it starts the first time the team-dispatch skill runs (exactly when multiple agents are live and guardrails matter most) and stays up for the rest of the session. That scoping is the **cost bound**: ordinary single-agent sessions never start it. It tails the newest session hook-event log and, for each new line, emits **one** notification built only from a whitelist of **derived labels** — verdict, hook name, tool, and rule. It **never** echoes the raw path or command field, because every monitor stdout line becomes a Claude notification, so the emit surface is an injection surface; the fixed label vocabulary is the defense, mirroring the capability-banner rule.\n\nTwo more invariants keep it safe and robust. It is **read-only** — it tails and summarizes, writing nothing and mutating no run state. And it is **fail-safe** against the empty-glob trap: rather than `tail -F` a bare glob (which would exit on no match and crash-loop), it resolves the single newest log itself and idle-polls to re-resolve when a log rotates. It is **Claude-Code-only** — plugin monitors are a Claude Code component with no Copilot equivalent, so under Copilot it simply doesn't load and the pull tabs remain the surface there.",
      "diagram": "flowchart TD\n  ST[spawn-team dispatched] --> W[watch-run-state.sh]\n  HJ[(hook-events.jsonl)] --> W\n  W --> WL[whitelist:<br/>verdict · hook · tool · rule]\n  WL --> NT[Claude notification]\n  class W,WL,NT built\n  class ST,HJ fact",
      "diagram_mini": null,
      "steps": [],
      "svg": "knowledge/concepts/visuals/run-state-monitor.svg",
      "svg_mini": null
    },
    {
      "id": "capability-banner",
      "title": "Capability-orientation banner",
      "category": "Orientation & capability",
      "kind": "ravenclaude-built",
      "order": 40,
      "summary": "A SessionStart hook injects what the project touches, the auth it holds (names only), and the effective permissions — so the agent never acts as if it has no access.",
      "see_also": [
        "session-start-context",
        "comfort-posture"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {
        "G": "permission-layers"
      },
      "last_verified": "2026-05-26",
      "refresh_when": "The capability-orientation hook's banner contents or its salience-not-enforcement framing change.",
      "sources": [
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Claude Code permissions (SessionStart)",
          "url": "plugins/ravenclaude-core/knowledge/claude-code-permissions.md"
        }
      ],
      "body_md": "The `capability-orientation.sh` SessionStart hook assembles a **capability banner** and injects it via `additionalContext` every session. It states the project's detected external surface, the auth it holds (env-var **names/presence only — never values; no network calls**), the effective `.claude/settings.json` permissions, and a presence/staleness summary of `environment-context.md`.\n\nWhy it exists: the behavioral instruction \"read the posture at session start\" is prose the model often skips. The hook makes the summary impossible to miss. Crucially, it is a **salience boost, not enforcement** — the real gate is the permission rules; the banner just stops the agent acting as if it has no access (the \"did you try X?\" round-trip on actions it's already authorized for). The banner is a *pointer*; `environment-context.md` stays the authoritative source for per-environment detail.",
      "diagram": "flowchart TD\n  S[Session starts] --> H[capability-orientation.sh]\n  H --> R1[Detect project surface]\n  H --> R2[Read auth env-var NAMES · no values · no network]\n  H --> R3[Read effective settings.json permissions]\n  R1 & R2 & R3 --> B[Banner via additionalContext]\n  B --> A[Agent is oriented — but NOT gated]\n  A --> G[(Real gate: permission rules)]\n  class S,H,R1,R2,R3 built\n  class B,A built\n  class G fact",
      "diagram_mini": "flowchart LR\n  S[SessionStart] --> B[surface + auth + perms]\n  B --> A[agent oriented<br/>· not enforcement]\n  class B,A built",
      "steps": [
        {
          "caption": "A session starts; the capability-orientation.sh SessionStart hook runs.",
          "diagram": "flowchart LR\n  N1[Session start] --> N2[Detect surface] --> N3[Auth names only] --> N4[Effective perms] --> N5[Banner orients]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/capability-banner.step-1.svg"
        },
        {
          "caption": "Detect the project's external surface.",
          "diagram": "flowchart LR\n  N1[Session start] --> N2[Detect surface] --> N3[Auth names only] --> N4[Effective perms] --> N5[Banner orients]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/capability-banner.step-2.svg"
        },
        {
          "caption": "Read auth env-var NAMES only — never values, no network calls.",
          "diagram": "flowchart LR\n  N1[Session start] --> N2[Detect surface] --> N3[Auth names only] --> N4[Effective perms] --> N5[Banner orients]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/capability-banner.step-3.svg"
        },
        {
          "caption": "Read the effective settings.json permissions and environment-context staleness.",
          "diagram": "flowchart LR\n  N1[Session start] --> N2[Detect surface] --> N3[Auth names only] --> N4[Effective perms] --> N5[Banner orients]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/capability-banner.step-4.svg"
        },
        {
          "caption": "Inject the banner via additionalContext — orientation, NOT enforcement.",
          "diagram": "flowchart LR\n  N1[Session start] --> N2[Detect surface] --> N3[Auth names only] --> N4[Effective perms] --> N5[Banner orients]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/capability-banner.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/capability-banner.svg",
      "svg_mini": "knowledge/concepts/visuals/capability-banner.mini.svg"
    },
    {
      "id": "environment-context",
      "title": "Environment context & discovery",
      "category": "Orientation & capability",
      "kind": "ravenclaude-built",
      "order": 42,
      "summary": "An optional file maps which environments your creds reach and what's pre-authorized — so the agent acts without asking where allowed, and stops on forbidden actions.",
      "see_also": [
        "capability-banner",
        "decision-trees"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The environment-context pre-action check or the discovery skill's contract changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "environment-context template",
          "url": "plugins/ravenclaude-core/templates/environment-context.md"
        }
      ],
      "body_md": "`.ravenclaude/environment-context.md` (at the **consumer's** project root, optional) records each environment (DEV / TEST / PROD / named), its role, its **pre-authorized action categories**, and a **forbidden** list. The Capability Grounding Protocol does a **pre-action check** against it: if the current environment pre-authorizes the action category, the agent **executes without asking** (no \"did you try X?\" round-trip); if the action is forbidden, it **stops** for explicit confirmation regardless of role; if the file is silent, it falls through to alternate-methods enumeration.\n\nWhen the file is absent, the agent offers the **environment-discovery** skill instead of asking you to fill in a template by hand — it probes installed CLIs read-only, decodes any acquired JWTs, and drafts the file for you to save/edit/skip. Discovery never runs without confirmation, is read-only by contract, and **never writes credentials** (those live in env vars / Key Vault; the file holds posture, not secrets).",
      "diagram": "flowchart TD\n  ACT[Agent about to act] --> F{environment-context.md present?}\n  F -- no --> ALT[Fall through to alternate-methods]\n  F -- yes --> CAT{Action pre-authorized<br/>for current env?}\n  CAT -- yes --> RUN[Execute · no prompt]\n  CAT -- forbidden --> STOP[Stop · require confirmation]\n  CAT -- \"not listed\" --> ALT\n  class ACT,F,CAT fact\n  class ALT,RUN,STOP built",
      "diagram_mini": "flowchart LR\n  A[Action] --> C{pre-authorized<br/>here?}\n  C -- yes --> R[run]\n  C -- forbidden --> S[stop]\n  class R,S built",
      "steps": [
        {
          "caption": "The agent is about to act.",
          "diagram": "flowchart LR\n  N1[About to act] --> N2[File present] --> N3[Pre authorized] --> N4[Forbidden stop] --> N5[Silent fallthrough]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/environment-context.step-1.svg"
        },
        {
          "caption": "Is environment-context.md present? If absent, fall through to alternate-methods.",
          "diagram": "flowchart LR\n  N1[About to act] --> N2[File present] --> N3[Pre authorized] --> N4[Forbidden stop] --> N5[Silent fallthrough]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/environment-context.step-2.svg"
        },
        {
          "caption": "Action pre-authorized for the current environment? Execute, no prompt.",
          "diagram": "flowchart LR\n  N1[About to act] --> N2[File present] --> N3[Pre authorized] --> N4[Forbidden stop] --> N5[Silent fallthrough]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/environment-context.step-3.svg"
        },
        {
          "caption": "Action forbidden? Stop for explicit confirmation, regardless of role.",
          "diagram": "flowchart LR\n  N1[About to act] --> N2[File present] --> N3[Pre authorized] --> N4[Forbidden stop] --> N5[Silent fallthrough]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/environment-context.step-4.svg"
        },
        {
          "caption": "File silent on it? Fall through to alternate-methods. Discovery can draft the file, read-only.",
          "diagram": "flowchart LR\n  N1[About to act] --> N2[File present] --> N3[Pre authorized] --> N4[Forbidden stop] --> N5[Silent fallthrough]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/environment-context.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/environment-context.svg",
      "svg_mini": "knowledge/concepts/visuals/environment-context.mini.svg"
    },
    {
      "id": "decision-trees",
      "title": "Decision trees in knowledge files",
      "category": "Orientation & capability",
      "kind": "ravenclaude-built",
      "order": 44,
      "summary": "Knowledge files carry Mermaid decision trees the agent traverses BEFORE picking a method — preventing wrong-branch-from-the-start, with a mandatory staleness date.",
      "see_also": [
        "environment-context"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The decision-tree convention or its last-verified staleness discipline changes.",
      "sources": [
        {
          "label": "Decision trees in knowledge files",
          "url": "docs/best-practices/decision-trees-in-knowledge-files.md"
        },
        {
          "label": "ravenclaude-core constitution",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "When a knowledge file contains a `## Decision Tree: <Domain>` section, the agent must **traverse the Mermaid graph top-to-bottom before selecting a method** — resolving each condition node against the user's actual context (not keyword-matching the request), defaulting to the **leaf with the smaller blast radius**, and escalating to a higher-blast leaf only after the smaller one demonstrably fails.\n\nThis closes the **wrong-branch-from-the-start** failure mode (the agent picks the wrong method on the first try because the branches weren't visible). It composes with alternate-methods enumeration (which handles \"method failed, try the next\") and the environment-context check (which handles \"I'm already authorized\"). Every diagram-bearing knowledge file carries a mandatory **`last-verified` date**, and the Researcher's sweep flags any tree older than 90 days — the same discipline these Learn concepts follow.",
      "diagram": "flowchart TD\n  SIT[Situation matches a tree's entry] --> TRAV[Traverse the graph top-to-bottom]\n  TRAV --> RES[Resolve each node against real context]\n  RES --> LEAF[Default to the smaller-blast leaf]\n  LEAF --> TRY[Try it]\n  TRY -- works --> DONE[Done]\n  TRY -- fails --> ESC[Escalate to higher-blast leaf]\n  class SIT,TRAV,RES,LEAF,TRY,ESC,DONE built",
      "diagram_mini": "flowchart LR\n  S[Situation] --> T[traverse tree]\n  T --> L[smaller-blast leaf first]\n  class T,L built",
      "steps": [
        {
          "caption": "A situation matches a knowledge file's Decision Tree section.",
          "diagram": "flowchart LR\n  N1[Situation] --> N2[Traverse top down] --> N3[Resolve context] --> N4[Smaller blast leaf] --> N5[Escalate on fail]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/decision-trees.step-1.svg"
        },
        {
          "caption": "Traverse the Mermaid graph top-to-bottom BEFORE picking a method.",
          "diagram": "flowchart LR\n  N1[Situation] --> N2[Traverse top down] --> N3[Resolve context] --> N4[Smaller blast leaf] --> N5[Escalate on fail]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/decision-trees.step-2.svg"
        },
        {
          "caption": "Resolve each node against the user's real context — not keyword-matching.",
          "diagram": "flowchart LR\n  N1[Situation] --> N2[Traverse top down] --> N3[Resolve context] --> N4[Smaller blast leaf] --> N5[Escalate on fail]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/decision-trees.step-3.svg"
        },
        {
          "caption": "Default to the leaf with the smaller blast radius, and try it.",
          "diagram": "flowchart LR\n  N1[Situation] --> N2[Traverse top down] --> N3[Resolve context] --> N4[Smaller blast leaf] --> N5[Escalate on fail]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/decision-trees.step-4.svg"
        },
        {
          "caption": "Works and done. Fails and you escalate to a higher-blast leaf. Every tree carries a last-verified date.",
          "diagram": "flowchart LR\n  N1[Situation] --> N2[Traverse top down] --> N3[Resolve context] --> N4[Smaller blast leaf] --> N5[Escalate on fail]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/decision-trees.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/decision-trees.svg",
      "svg_mini": "knowledge/concepts/visuals/decision-trees.mini.svg"
    },
    {
      "id": "capability-grounding-protocol",
      "title": "Capability Grounding Protocol",
      "category": "Agent disciplines",
      "kind": "ravenclaude-built",
      "order": 50,
      "summary": "Don't falsely say you're blocked: read the error, check what you're already authorized for, try at least two other paths, and only then report blocked — with what you tried.",
      "see_also": [
        "structured-output-protocol",
        "last-mile-completion",
        "claim-grounding",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The CGP clauses in ravenclaude-core/CLAUDE.md change — the alternate-methods rule, the read-the-error step, the environment-context check, verify-before-you-yield, or the mandatory-phrasing block.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Capability Grounding Protocol",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Alternate-methods grounding (root accuracy discipline)",
          "url": "AGENTS.md"
        }
      ],
      "body_md": "Every RavenClaude agent inherits one promise: **it will not tell you \"I can't\" when it actually can.** That sounds obvious, but the most common — and costliest — agent mistake is the *false negative*: hitting one wall and reporting \"this is impossible\" or \"can you authorize me?\" when a second path was sitting right there. The Capability Grounding Protocol (CGP) is the floor that stops this. It says a wrong route is **never** proof a capability is absent — a `command not found`, a `403`, or a tool whose menu hasn't loaded yet is evidence about **one** way of doing the thing, not about the thing itself.\n\nWhen an agent hits a wall, CGP makes it do four cheap things before the word \"blocked\" leaves its mouth. **First, read the actual error** — the status code *and* the message body, not just the headline — because the reason picks the fix (an expired token means \"log in and retry the same way\"; a missing permission means \"use a path that already has it\"). **Second, check what it's already allowed to do** — many sessions carry an environment-context note saying \"you're pre-authorized for X here,\" so the agent should just do X instead of asking. **Third, enumerate at least two other paths and try the easiest one** (a different API, a lower-level tool, a manual step with automation around it). Only after those does it report — and when it genuinely is blocked, it must use the **mandatory phrasing**: \"After trying A (outcome), B (outcome), I'm blocked on [specific reason]; the remaining options are [X, Y].\" That report tells you exactly what's left so you never have to ask \"did you try…?\"\n\nCGP also guards the *correction* moment. If you push back on a claim, the agent must **verify before it yields** — it can't just cave to be agreeable (that's how a confident-but-wrong answer survives the one moment that should catch it), and it can't dig in either. It re-checks, names the specific error if it was wrong, and adopts your correction once. CGP is the first leg of the agent-honesty triad — paired with [Claim Grounding](#/learn/claim-grounding) (don't *over*-claim certainty) and [Last-Mile Completion](#/learn/last-mile-completion) (finish everything you *can* do).",
      "diagram": "flowchart TD\n  W[Hit a wall] --> E[Read the error:<br/>code + body]\n  E --> A{Already<br/>authorized here?}\n  A -- yes --> DO[Just do it]\n  A -- no --> ALT[Enumerate 2+ paths<br/>try the easiest]\n  ALT -- worked --> DONE[Continue the task]\n  ALT -- all failed --> M[Report blocked with<br/>mandatory phrasing:<br/>what I tried + what's left]\n  class DO,DONE,M built\n  class E,ALT step",
      "diagram_mini": "flowchart LR\n  W[Wall] --> R[Read error] --> T[Try 2+ paths] --> M[Blocked? say what you tried]\n  class M built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/capability-grounding-protocol.svg",
      "svg_mini": "knowledge/concepts/visuals/capability-grounding-protocol.mini.svg"
    },
    {
      "id": "structured-output-protocol",
      "title": "Structured Output Protocol",
      "category": "Agent disciplines",
      "kind": "ravenclaude-built",
      "order": 51,
      "summary": "Every specialist ends a handoff with both human-readable prose and a machine-readable JSON block fenced by ---RESULT_START--- / ---RESULT_END--- so the Team Lead can route reliably.",
      "see_also": [
        "capability-grounding-protocol",
        "last-mile-completion",
        "claim-grounding",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The Structured Output Protocol section in ravenclaude-core/CLAUDE.md changes — the envelope delimiters, the required handoff fields, or which agents must emit it.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Structured Output Protocol",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "structured-output skill (the operating reference)",
          "url": "plugins/ravenclaude-core/skills/structured-output/SKILL.md"
        }
      ],
      "body_md": "When one agent finishes a slice of work and hands it back to the Team Lead, two things have to travel together: the **reasoning** (so a human can read and trust it) and a **clean summary a program can parse** (so the orchestrator can decide what happens next without re-reading prose). Pure prose is unparseable; pure JSON throws away the \"why.\" The Structured Output Protocol solves this by asking every specialist to emit **both** — prose first, then a small JSON block wrapped in a fixed envelope:\n\n```\n---RESULT_START---\n{ \"status\": \"complete\", \"summary\": \"...\", \"handoff_recommendation\": {...}, \"confidence\": 0.9 }\n---RESULT_END---\n```\n\nThose exact delimiters are the contract. The Team Lead reads only what's between them to drive routing — which specialist goes next, whether the work is `complete` / `partial` / `blocked`, and how confident the agent is. Because the markers are literal and unique, extraction is reliable even when the prose above them is long and freeform. Every handoff JSON carries the same load-bearing fields: a one-line summary of what was done, the recommended next specialist and why, any risks or open questions, and a numeric `confidence` (the float that rides agent-to-agent, the complement to the `[unverified]` markers humans see in chat — see [Claim Grounding](#/learn/claim-grounding)).\n\nThis is a **discipline, not a hook** — nothing mechanically forces the JSON to appear, so it lives in every agent's instructions and the Team Lead enforces it when it briefs them. It pairs naturally with [Last-Mile Completion](#/learn/last-mile-completion): when a `blocked` or `partial` status is honest, the protocol requires the agent to fill `risks` with the alternatives it ruled out and `next_actions` with the escalation path — exactly the [Capability Grounding](#/learn/capability-grounding-protocol) \"what I tried\" report, in machine-readable form. Casual chatter (\"read the file,\" \"tests ran\") is exempt; the envelope is only for handoffs that another agent or a human will act on.",
      "diagram": "flowchart TD\n  A[Specialist finishes a slice] --> P[Write human-readable<br/>reasoning + prose]\n  P --> J[Append JSON block:<br/>---RESULT_START--- ... ---RESULT_END---]\n  J --> TL[Team Lead reads the JSON]\n  TL --> R{status?}\n  R -- complete --> NEXT[Route to next specialist]\n  R -- partial / blocked --> ESC[Read risks + next_actions,<br/>re-route or escalate]\n  class P,J,NEXT,ESC built\n  class TL,R step",
      "diagram_mini": "flowchart LR\n  P[Prose] --> J[JSON envelope] --> TL[Team Lead routes]\n  class J,TL built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/structured-output-protocol.svg",
      "svg_mini": "knowledge/concepts/visuals/structured-output-protocol.mini.svg"
    },
    {
      "id": "last-mile-completion",
      "title": "Last-Mile Completion Protocol",
      "category": "Agent disciplines",
      "kind": "ravenclaude-built",
      "order": 52,
      "summary": "Do everything automatable, partial-do the rest, tee up and deep-link the human-only residue, and report a clean split of done vs your-turn so the human only confirms or clicks.",
      "see_also": [
        "capability-grounding-protocol",
        "structured-output-protocol",
        "claim-grounding",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The Last-Mile Completion Protocol section in ravenclaude-core/CLAUDE.md changes — the five rules, the done-vs-your-turn report shape, or the deep-link requirement.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Last-Mile Completion Protocol",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Capability Grounding Protocol (the floor it pairs with)",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "[Capability Grounding](#/learn/capability-grounding-protocol) is the *floor* — don't falsely claim you can't. Last-Mile Completion is the *ceiling* — once an agent has confirmed it *can* act, it carries the work as far toward done as its authority allows before handing anything back. The guiding rule is simple: **the human should do as little as possible — ideally only the part a machine genuinely can't do, reduced to a confirm or a click.** A \"next steps\" list full of things the agent could have just done is treated as a defect, not as helpfulness.\n\nThe protocol is five rules. **Do everything automatable** — if a step can be done with the tools on hand, do it; don't hand back a to-do you could have executed. **Partial-do the partially-automatable** — generate the file, the config, the draft, the migration, and hand back only the irreducible human remainder. **Tee up the human-only residue** — for the things only a person can do (a click behind their login, a signed approval, a payment, a destructive production action), prepare *everything around* it: pre-fill the values, draft the message or PR or commit, stage the exact inputs, so the human's job shrinks to confirm-or-click, never assemble. **Deep-link, don't narrate** — when the human must go somewhere, give a direct link to the exact destination (the specific settings page, a pre-filled \"create PR\" URL), not a \"go to the portal, then click X, then Y\" recipe; a click beats a recipe. **Report as done vs. your-turn** — the final message separates ✅ *done* from 👉 *your turn*, and the your-turn list is short, ordered, one action each, each with its deep link.\n\nTogether these mean a finished hand-back looks like a tiny, ordered checklist of one-click human actions sitting on top of a pile of already-completed work — not a pile of instructions. Last-Mile is the third leg of the agent-honesty triad: [Capability Grounding](#/learn/capability-grounding-protocol) keeps the agent from under-claiming ability, [Claim Grounding](#/learn/claim-grounding) keeps it from over-claiming certainty, and this one keeps it from under-*delivering*. Domain plugins add their own deep-link sources (portal blade URLs, workspace item links) but never restate the protocol — it's inherited by every plugin through the core constitution.",
      "diagram": "flowchart TD\n  C[Confirmed: I can act] --> S{Is this step<br/>fully automatable?}\n  S -- yes --> DO[Do it now]\n  S -- partly --> PART[Do the automatable part,<br/>tee up the rest]\n  S -- human-only --> TEE[Pre-fill values + draft,<br/>add a deep link]\n  DO --> REP[Report: split done<br/>vs your-turn]\n  PART --> REP\n  TEE --> REP\n  class DO,PART,TEE,REP built\n  class S step",
      "diagram_mini": "flowchart LR\n  A[Automate all you can] --> T[Tee up + deep-link residue] --> R[Done vs your-turn]\n  class A,T,R built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/last-mile-completion.svg",
      "svg_mini": "knowledge/concepts/visuals/last-mile-completion.mini.svg"
    },
    {
      "id": "claim-grounding",
      "title": "Claim Grounding & Source Honesty",
      "category": "Agent disciplines",
      "kind": "ravenclaude-built",
      "order": 53,
      "summary": "For any claim that gates an irreversible action or lands in a durable file, cite the this-session check inline or mark it [unverified — training knowledge]; never stamp a guess as certain.",
      "see_also": [
        "capability-grounding-protocol",
        "structured-output-protocol",
        "last-mile-completion",
        "command-review-tribunal",
        "saga-log"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The Claim Grounding & Source Honesty section in ravenclaude-core/CLAUDE.md changes — the cite-or-mark rule, the marker vocabulary, the scope of consequential claims, or the enforced complements.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Claim Grounding & Source Honesty",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Accuracy discipline (cross-tool pointer)",
          "url": "AGENTS.md"
        }
      ],
      "body_md": "A confident reasoning error is as dangerous as a hallucination and harder to catch: a flawed belief about a tool, API, or platform stated as plain fact, with no hint of doubt — which then drives a bad, irreversible action. [Capability Grounding](#/learn/capability-grounding-protocol) stops the *false negative* (\"I can't\" when you can); Claim Grounding stops the *false positive* (\"this is how it works\" when it isn't). It is the third axis of agent honesty: **don't over-claim certainty.**\n\nThe rule is narrow on purpose, so it doesn't drown every sentence in disclaimers. It triggers only on **consequential** claims — ones that gate an irreversible action, or that get written into a durable knowledge or design file — and only for **factual** claims about systems (versions, API fields, defaults, capabilities, environment requirements), not judgment calls or opinions. For a claim in scope the agent must do one of two things: **cite the this-session check that backs it, inline and falsifiable** (the exact command and its output, or a `file:line`), **or** mark it `[unverified — training knowledge]` and offer to verify before acting. A \"verification\" that came from a fetched web page or tool output is *untrusted data, not a citation*. And there's no High/Medium/Low confidence label — self-rated confidence is uncalibrated and just stamps wrong answers \"High\"; the *basis* is the only signal worth checking. When the claim lands in a file, the marker must be persisted **in the file**, so the next session reads the provenance too and a hedge spoken only in chat doesn't quietly launder into a trusted-looking fact.\n\nBecause these are honesty disciplines for *honest* error — an injected instruction could flip them — they aren't a security control; their teeth are elsewhere (the definition-of-done gate, the [command-review tribunal](#/learn/command-review-tribunal), the audit trail in the [Sága log](#/learn/saga-log)). Claim Grounding completes the triad: it composes with [Capability Grounding](#/learn/capability-grounding-protocol) (\"can I act?\") and [Last-Mile Completion](#/learn/last-mile-completion) (\"how far must I finish?\") to answer the middle question — \"is what I just said *true*, and grounded?\"",
      "diagram": "flowchart TD\n  C[About to state a claim] --> S{Consequential?<br/>gates an action OR<br/>written to a file}\n  S -- no --> SAY[State it normally]\n  S -- yes --> V{Verified<br/>this session?}\n  V -- yes --> CITE[Cite the check inline:<br/>command + output or file:line]\n  V -- no --> MARK[Mark unverified —<br/>training knowledge, offer to verify]\n  class CITE,MARK,SAY built\n  class S,V step",
      "diagram_mini": "flowchart LR\n  C[Consequential claim?] --> V{Verified?} -- yes --> CITE[Cite inline]\n  V -- no --> MARK[Mark unverified]\n  class CITE,MARK built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/claim-grounding.svg",
      "svg_mini": "knowledge/concepts/visuals/claim-grounding.mini.svg"
    },
    {
      "id": "visual-feedback-loop",
      "title": "Visual feedback loop: render → see → iterate",
      "category": "Agent disciplines",
      "kind": "ravenclaude-built",
      "order": 54,
      "summary": "Visual-output agents render their work, SEE it (screenshot or read the layout coordinates), critique it against objective signals, edit, and re-render — iterating to correct, not just 'looks better'.",
      "see_also": [
        "last-mile-completion",
        "capability-grounding-protocol",
        "structured-output-protocol",
        "claim-grounding",
        "runaway-brake",
        "agent-harness-loop"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-09",
      "refresh_when": "The visual-feedback-loop knowledge file or skill changes, the chrome-devtools-mcp / pbir-layout-engine contract drifts, or the set of agents carrying the Visual feedback loop prior changes.",
      "sources": [
        {
          "label": "visual-feedback-loop — the discipline canon",
          "url": "plugins/ravenclaude-core/knowledge/visual-feedback-loop.md"
        },
        {
          "label": "visual-feedback-loop skill — the runnable referee (driver.py)",
          "url": "plugins/ravenclaude-core/skills/visual-feedback-loop/SKILL.md"
        },
        {
          "label": "pbir-layout-engine — the deterministic layout linter",
          "url": "plugins/ravenclaude-core/skills/pbir-layout-engine/SKILL.md"
        }
      ],
      "body_md": "An agent that builds something visual — a web page, a dashboard, a Power BI or Tableau report — should not work blind. The discipline closes the [agent loop](#/learn/agent-harness-loop) one turn tighter: **render the output, _see_ it, critique it against the intent _and_ objective signals, edit, re-render — until the signals pass.** The part that makes it genuinely *improve* rather than wander is the stopping signal: the loop ends on **objective gates** (zero console errors, a Lighthouse accessibility score, no overlapping elements), never on a subjective \"looks better.\"\n\nThere are two ways an agent \"sees,\" and which one leads depends on the surface. For a **web** page it's **visual** — a real browser takes a screenshot the model can actually look at, plus the console errors and a Lighthouse audit. For **Power BI and Tableau** it's **structural** — the agent reads the report definition's *exact coordinates* (PBIR JSON `x/y/width/height`, a Tableau `.twb`'s zone geometry). Layout is just numbers there, so checking it with arithmetic beats eyeballing a picture — and a BI screenshot needs the report published or embedded and authenticated, which the agent often can't reach. So for reporting, **structural-only is a complete pass**, with a screenshot as the secondary check (did the theme apply, did conditional formatting fire). A small runnable **referee** merges whatever evidence was captured — the layout linter plus any screenshot/console/Lighthouse results — into one pass/fail verdict with a concrete `next_action`, and reads back **only numbers and fixed labels**, never the page's raw text (a hostile page can write fake \"instructions\" to the console).\n\n**For consumers — one thing to know.** The *visual* (screenshot) half rides on an **optional, not-bundled** browser tool, the `chrome-devtools-mcp` server. It is **not installed by default** (and its adoption is security-reviewed, because it drives a live browser). When it's present, agents capture screenshots and run the full visual loop; when it's **absent, they fall back to the structural read and say so — they never stall.** So every visual-output agent works out of the box; installing `chrome-devtools-mcp` simply unlocks the richer, pixel-level half. The discipline composes with the rest of the honesty triad: the \"see\" step is a this-session [verification](#/learn/claim-grounding) to cite, and once the signals pass, [Last-Mile Completion](#/learn/last-mile-completion) carries the work the rest of the way.",
      "diagram": "flowchart TD\n  A[Agent builds a visual surface] --> R[Render it]\n  R --> SEE{See it — two ways}\n  SEE -- \"Web: screenshot\" --> V[\"chrome-devtools-mcp<br/>screenshot + console + Lighthouse<br/>(optional, not bundled)\"]\n  SEE -- \"Power BI / Tableau: read coordinates\" --> S[\"pbir-layout-engine / .twb geometry<br/>exact x / y / width / height\"]\n  V -. \"browser tool not installed →<br/>fall back, never stall\" .-> S\n  V --> REF[Referee merges the signals<br/>into one pass/fail verdict]\n  S --> REF\n  REF -- \"fail → next_action\" --> A\n  REF -- \"pass → objective signals met\" --> SHIP[Ship]\n  class V,S,REF,SHIP built\n  class SEE step",
      "diagram_mini": "flowchart LR\n  R[Render] --> SEE{See} --> REF[Referee]\n  REF -- fail --> R\n  REF -- pass --> SHIP[Ship]\n  class REF,SHIP built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/visual-feedback-loop.svg",
      "svg_mini": "knowledge/concepts/visuals/visual-feedback-loop.mini.svg"
    },
    {
      "id": "layout-hook",
      "title": "Layout enforcement",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 50,
      "summary": "A PreToolUse hook plus a CI backstop block off-pattern file creation against .repo-layout.json — because path-scoped rule files load on Read, not Write.",
      "see_also": [
        "audit-gates",
        "hook-lifecycle"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The enforce-layout hook, .repo-layout.json schema, or the validate-layout CI workflow changes.",
      "sources": [
        {
          "label": "AGENTS.md — Layout & boundary rules",
          "url": "AGENTS.md"
        },
        {
          "label": "Claude Code issue #23478",
          "url": "https://github.com/anthropics/claude-code/issues/23478"
        }
      ],
      "body_md": "`hooks/enforce-layout.sh` runs `PreToolUse` on `Write|Edit|MultiEdit`: it reads `.repo-layout.json`, matches the target path against `allowed_globs`, and **denies an off-pattern write with a suggested correct location**. If the manifest is absent it silently allows everything — so consumers who install the plugin without a layout manifest aren't surprised.\n\nWhy a hook **and** CI: Claude Code issue #23478 confirms that path-scoped rule files (`paths:` frontmatter) load only on **Read**, not on **Write** — they cannot prevent off-pattern file *creation*. So the in-loop hook gives fast feedback during a session (Claude only), and `.github/workflows/validate-layout.yml` is the **cross-tool backstop** that catches direct human commits, Cursor/Codex/Aider edits, and any case where the hook didn't fire.",
      "diagram": "flowchart TD\n  W[Write / Edit / MultiEdit] --> H[enforce-layout.sh · PreToolUse]\n  H --> M{path matches an<br/>allowed_glob?}\n  M -- yes --> OK[allow]\n  M -- no --> DENY[deny + suggest correct path]\n  PR[Any commit · any tool] --> CI[validate-layout.yml]\n  CI --> CIM{matches allow-list?}\n  CIM -- no --> FAIL[CI fails]\n  CIM -- yes --> PASS[CI passes]\n  class W,H,M,PR fact\n  class OK,DENY,CI,CIM,FAIL,PASS built",
      "diagram_mini": "flowchart LR\n  W[Write] --> H{matches<br/>allow-list?}\n  H -- no --> D[deny + CI backstop]\n  H -- yes --> A[allow]\n  class D,A built",
      "steps": [
        {
          "caption": "A Write / Edit / MultiEdit fires enforce-layout.sh at PreToolUse.",
          "diagram": "flowchart LR\n  N1[Write or edit] --> N2[Match globs] --> N3[Deny and suggest] --> N4[CI backstop] --> N5[Read cannot block]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/layout-hook.step-1.svg"
        },
        {
          "caption": "It matches the path against allowed_globs in .repo-layout.json.",
          "diagram": "flowchart LR\n  N1[Write or edit] --> N2[Match globs] --> N3[Deny and suggest] --> N4[CI backstop] --> N5[Read cannot block]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/layout-hook.step-2.svg"
        },
        {
          "caption": "No match? Deny the write and suggest the correct location.",
          "diagram": "flowchart LR\n  N1[Write or edit] --> N2[Match globs] --> N3[Deny and suggest] --> N4[CI backstop] --> N5[Read cannot block]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/layout-hook.step-3.svg"
        },
        {
          "caption": "validate-layout.yml is the CI backstop for any commit, any tool.",
          "diagram": "flowchart LR\n  N1[Write or edit] --> N2[Match globs] --> N3[Deny and suggest] --> N4[CI backstop] --> N5[Read cannot block]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/layout-hook.step-4.svg"
        },
        {
          "caption": "Why both: path-scoped rule files load on Read, not Write — they can't block creation.",
          "diagram": "flowchart LR\n  N1[Write or edit] --> N2[Match globs] --> N3[Deny and suggest] --> N4[CI backstop] --> N5[Read cannot block]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/layout-hook.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/layout-hook.svg",
      "svg_mini": "knowledge/concepts/visuals/layout-hook.mini.svg"
    },
    {
      "id": "audit-gates",
      "title": "The gate-audit meta-test",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 52,
      "summary": "Every CI gate is itself tested: audit-gates.sh proves each gate FAILS on a known-bad fixture and PASSES on a known-good one — so a gate can't silently rot into a no-op.",
      "see_also": [
        "layout-hook"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-05-26",
      "refresh_when": "The gate-audit approach or its skip-is-not-a-pass policy changes.",
      "sources": [
        {
          "label": "CI gate audit",
          "url": "docs/best-practices/ci-gate-audit.md"
        },
        {
          "label": "AGENTS.md — Testing instructions",
          "url": "AGENTS.md"
        }
      ],
      "body_md": "A CI gate that never fails is worse than no gate — it gives false confidence. So RavenClaude runs a **meta-test**: `scripts/audit-gates.sh` proves, for each gate, that it **fails on a known-bad fixture AND passes on a known-good one**. If a gate can't be made to fail on input it's supposed to reject, the gate is broken and the audit says so.\n\nThis is required reading before adding or changing any CI step — a new gate ships with its fail/pass fixtures, not just its happy path. And **a skip is not a pass**: when a gate can't run locally (e.g. the actionlint gate needs a Docker daemon), it **loudly** skips (\"THIS IS NOT A PASS\"), and in CI an unrunnable gate is a hard failure, never a silent skip.",
      "diagram": "flowchart TD\n  G[For each CI gate] --> BAD[Run it on a known-BAD fixture]\n  BAD --> FB{does it FAIL?}\n  FB -- no --> BROKEN[Gate is broken · audit fails]\n  FB -- yes --> GOOD[Run it on a known-GOOD fixture]\n  GOOD --> FG{does it PASS?}\n  FG -- no --> BROKEN\n  FG -- yes --> TRUST[Gate trusted]\n  class G,BAD,FB,GOOD,FG built\n  class BROKEN built\n  class TRUST built",
      "diagram_mini": "flowchart LR\n  B[known-bad] --> F{fails?}\n  F -- yes --> P[known-good passes → trusted]\n  F -- no --> X[gate broken]\n  class P,X built",
      "steps": [
        {
          "caption": "For each CI gate...",
          "diagram": "flowchart LR\n  N1[Each gate] --> N2[Known bad] --> N3[Must fail] --> N4[Known good] --> N5[Trusted]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/audit-gates.step-1.svg"
        },
        {
          "caption": "Run it on a known-BAD fixture.",
          "diagram": "flowchart LR\n  N1[Each gate] --> N2[Known bad] --> N3[Must fail] --> N4[Known good] --> N5[Trusted]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/audit-gates.step-2.svg"
        },
        {
          "caption": "Does it FAIL? If not, the gate is broken and the audit fails.",
          "diagram": "flowchart LR\n  N1[Each gate] --> N2[Known bad] --> N3[Must fail] --> N4[Known good] --> N5[Trusted]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/audit-gates.step-3.svg"
        },
        {
          "caption": "Run it on a known-GOOD fixture — does it PASS?",
          "diagram": "flowchart LR\n  N1[Each gate] --> N2[Known bad] --> N3[Must fail] --> N4[Known good] --> N5[Trusted]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/audit-gates.step-4.svg"
        },
        {
          "caption": "Both hold and the gate is trusted. And a skip is NOT a pass (it skips loudly).",
          "diagram": "flowchart LR\n  N1[Each gate] --> N2[Known bad] --> N3[Must fail] --> N4[Known good] --> N5[Trusted]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/audit-gates.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/audit-gates.svg",
      "svg_mini": "knowledge/concepts/visuals/audit-gates.mini.svg"
    },
    {
      "id": "copilot-bridge",
      "title": "GitHub Copilot CLI bridge",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 54,
      "summary": "RavenClaude runs under GitHub Copilot CLI too — via a generated package + a hook adapter — and updating is just `git pull`, never a re-install.",
      "see_also": [
        "layout-hook",
        "hook-lifecycle"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-09",
      "refresh_when": "The Copilot package generator, the hook adapter's envelope mapping, Copilot's plugin-hook bug status, or the GitHub Copilot CLI customization surface (instruction files / agents / skills / hooks) changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution — Copilot CLI bridge",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        },
        {
          "label": "Copilot CLI customization surface (canonical, docs-verified)",
          "url": "plugins/ravenclaude-core/knowledge/copilot-cli-customization.md"
        },
        {
          "label": "generate-copilot-plugin.py",
          "url": "scripts/generate-copilot-plugin.py"
        }
      ],
      "body_md": "Copilot CLI is itself a plugin host with the same lifecycle events (SessionStart / PreToolUse / …), Agent Skills, AGENTS.md, and MCP — so most of the plugin ports. Three pieces make it work: `generate-copilot-plugin.py` **projects** the canonical plugin into a Copilot package (generated, never hand-maintained, `--check`-gated like the dashboard); a **hook adapter** translates the I/O envelopes so the *unmodified* hook scripts run under Copilot (mapping its `toolName`/`toolArgs` ⇄ Claude's `tool_name`/`tool_input`, and the verdict shapes); and **enforcement hooks ship repo-level** in `.github/hooks/` because a Copilot bug stops plugin-level `preToolUse` hooks from firing.\n\nThe design pillar is **frictionless updates**: instead of Copilot's re-install-to-update flow, the plugin loads **live** via `copilot --plugin-dir copilot/`, so an update is just **`git pull`** (`ravenclaude update`). No re-install, ever.",
      "diagram": "flowchart TD\n  CANON[Canonical plugin] --> GEN[generate-copilot-plugin.py]\n  GEN --> PKG[copilot/ package · --check gated]\n  CO[Copilot PreToolUse envelope] --> AD[hook adapter]\n  AD --> CL[unmodified hook scripts]\n  CL --> AD\n  UP[Update] --> PULL[git pull · loads live]\n  class CANON,GEN,PKG,AD,CL,UP,PULL built\n  class CO fact",
      "diagram_mini": "flowchart LR\n  C[Canonical plugin] --> A[adapter] --> CO[Copilot CLI]\n  C --> U[update = git pull]\n  class C,A,U built",
      "steps": [
        {
          "caption": "Copilot CLI is a plugin host with the same hook events, skills, AGENTS.md, and MCP.",
          "diagram": "flowchart LR\n  N1[Copilot host] --> N2[Generate package] --> N3[Hook adapter] --> N4[Repo level hooks] --> N5[Update is git pull]\n  class N1 built",
          "svg": "knowledge/concepts/visuals/copilot-bridge.step-1.svg"
        },
        {
          "caption": "generate-copilot-plugin.py projects the canonical plugin into copilot/ (generated, gated).",
          "diagram": "flowchart LR\n  N1[Copilot host] --> N2[Generate package] --> N3[Hook adapter] --> N4[Repo level hooks] --> N5[Update is git pull]\n  class N2 built",
          "svg": "knowledge/concepts/visuals/copilot-bridge.step-2.svg"
        },
        {
          "caption": "A hook adapter maps the I/O envelopes so the unmodified hooks run under Copilot.",
          "diagram": "flowchart LR\n  N1[Copilot host] --> N2[Generate package] --> N3[Hook adapter] --> N4[Repo level hooks] --> N5[Update is git pull]\n  class N3 built",
          "svg": "knowledge/concepts/visuals/copilot-bridge.step-3.svg"
        },
        {
          "caption": "Enforcement hooks ship repo-level in .github/hooks/ (a Copilot plugin-hook bug).",
          "diagram": "flowchart LR\n  N1[Copilot host] --> N2[Generate package] --> N3[Hook adapter] --> N4[Repo level hooks] --> N5[Update is git pull]\n  class N4 built",
          "svg": "knowledge/concepts/visuals/copilot-bridge.step-4.svg"
        },
        {
          "caption": "Updates are frictionless: it loads live, so an update is just git pull.",
          "diagram": "flowchart LR\n  N1[Copilot host] --> N2[Generate package] --> N3[Hook adapter] --> N4[Repo level hooks] --> N5[Update is git pull]\n  class N5 built",
          "svg": "knowledge/concepts/visuals/copilot-bridge.step-5.svg"
        }
      ],
      "svg": "knowledge/concepts/visuals/copilot-bridge.svg",
      "svg_mini": "knowledge/concepts/visuals/copilot-bridge.mini.svg"
    },
    {
      "id": "bifrost",
      "title": "Bifröst install wizard",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 65,
      "summary": "A 4-step copy-paste wizard for installing a marketplace plugin into a Claude Code project — it never executes a command; you paste each command's output back and it parses that to light the next step.",
      "see_also": [
        "getting-started",
        "copilot-bridge"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The 4-step install sequence, the copy-paste-only design, or the failure-mode accordion changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution §Bifröst install wizard",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "**Bifröst** is the dashboard tab (`#/bifrost`) that walks you through installing a marketplace plugin into a Claude Code project. It's a guided **4-step copy-paste wizard**: (1) `/plugin marketplace add`, (2) `/plugin install <name>@ravenclaude`, (3) `/reload-plugins`, (4) `/init-agent-ready --check`. Each step has a copy-button for the command, a \"what I see now\" paste box, a Verify button, and a status badge that moves from grey to green / amber / red.\n\nThe load-bearing design constraint is that the wizard **never executes a slash command** — it's a wizard, not an orchestrator. You run each command in your own Claude Code session and paste the output back; the wizard's JavaScript only *parses* that pasted output against a per-step success/failure pattern to light the next step's badge, or to auto-expand the matching row of the **\"If the bridge is down…\"** failure-mode accordion (one diagnosis and next step per step). This keeps the consumer in control of what actually runs in their session.\n\nBecause it only parses pasted text, the wizard is **fully client-side** — no server endpoint, no network fetch — so it behaves identically on a static GitHub Pages host and on the served dashboard. It's distinct from the Install & Update tab, which wires RavenClaude into GitHub Copilot CLI for a different audience; Bifröst is specifically the Claude-Code-plugin-into-a-project path.",
      "diagram": "flowchart TD\n  S1[Copy command] --> R1[Run in your session]\n  R1 --> P1[Paste output back]\n  P1 --> V{Parse: success?}\n  V -- yes --> NEXT[Light next step badge]\n  V -- no --> ACC[Expand failure-mode row]\n  NEXT --> S1\n  class S1,R1,P1,V,NEXT,ACC built",
      "diagram_mini": "flowchart LR\n  C[Copy] --> R[Run] --> P[Paste back] --> B[Badge lights]\n  class C,R,P,B built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/bifrost.svg",
      "svg_mini": "knowledge/concepts/visuals/bifrost.mini.svg"
    },
    {
      "id": "ragnarok",
      "title": "Ragnarök — plugin-cache disaster recovery",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 66,
      "summary": "/reset-plugin-cache (alias /ragnarok) is the one high-blast plugin-cache DR command — dry-run by default, atomic and reversible, and executable only by a human, never by an agent on its own.",
      "see_also": [
        "command-review-tribunal",
        "bifrost"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The dry-run default, the user-only execute gate, the atomic-swap/snapshot flow, or the tribunal self-protection concern changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution §High-blast-radius commands — Ragnarök",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "**`/reset-plugin-cache`** (themed alias **`/ragnarok`**) is the marketplace's one **high-blast-radius, cache-mutating** disaster-recovery command — it resets a genuinely-broken plugin cache. Because it can move a lot of files at once, it ships behind a deliberate safety envelope. By default it is **dry-run**: it enumerates exactly what *would* change and moves nothing. The destructive path requires `--execute`, a pinned marketplace SHA (no floating HEAD), and a typed interactive confirmation.\n\nThat confirmation is the human gate: **an agent cannot satisfy it.** An attempt to execute without a real human confirmation fails, and an agent that tries to bypass the command by shelling the underlying script directly is hard-denied — the command-review tribunal carries a self-protection concern that blocks the bypass before any model runs. So the only way the cache is actually reset is a person typing the confirmation.\n\nThe execute path is **atomic and reversible**: snapshot the current cache → fetch a fresh, pinned copy → verify it with the gate suite *before* touching the live cache (a failed verification aborts, leaving the original untouched) → perform a two-rename atomic swap (rolling back the first rename if the second fails) → write an audit record. The pre-reset snapshot is retained, and `MEMORY.md` always survives because the memory directory lives outside the cache the script operates on.",
      "diagram": "flowchart TD\n  CMD[\"/reset-plugin-cache\"] --> D{--execute?}\n  D -- no --> DRY[Dry-run: list changes<br/>move nothing]\n  D -- yes --> H{Human confirm<br/>+ pinned SHA}\n  H -- agent / missing --> BLK[Denied]\n  H -- confirmed --> SNAP[Snapshot → fetch → verify]\n  SNAP --> SWAP[Atomic two-rename swap<br/>MEMORY survives]\n  class CMD,D,DRY,H,BLK,SNAP,SWAP built",
      "diagram_mini": "flowchart LR\n  C[\"/ragnarok\"] --> DR[dry-run] --> H[human confirm] --> A[atomic swap]\n  class C,DR,H,A built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/ragnarok.svg",
      "svg_mini": "knowledge/concepts/visuals/ragnarok.mini.svg"
    },
    {
      "id": "sleipnir",
      "title": "Sleipnir — worktree traversal",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 67,
      "summary": "A labeling convention for parallel, isolated git-worktree work: the agent says 'I'll send Sleipnir to that branch' instead of narrating raw worktree calls; a read-only widget lists current worktrees.",
      "see_also": [
        "copilot-bridge",
        "layout-hook"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The worktree-traversal labeling convention or the read-only Sleipnir's stables widget changes.",
      "sources": [
        {
          "label": "ravenclaude-core constitution §Sleipnir — the worktree-traversal labeling convention",
          "url": "plugins/ravenclaude-core/CLAUDE.md"
        }
      ],
      "body_md": "**Sleipnir** is the name for worktree traversal — Odin's eight-legged horse, the one mount that crosses realm boundaries safely. When the Team Lead fans work across several git branches in parallel, each branch gets its own **isolated worktree**, and in user-facing prose the agent prefers *\"I'll send Sleipnir to that branch\"* over narrating the raw worktree mechanics. The label anchors your intuition about what's happening (a safe crossing into a separate branch's working tree) while the underlying mechanism is unchanged.\n\nThis is **labeling only** — there is deliberately no `/sleipnir` slash command, no Sleipnir agent, and no new component. The convention surfaces in the worktree skills (new-worktree, cleanup-worktrees, spawn-team) and, on the dashboard's Activity tab, as a **read-only \"Sleipnir's stables\"** widget showing the current list and count of worktrees. The widget writes nothing; on a static host it shows an honest empty state rather than pretending to have live data.\n\nThe convention pairs with a load-bearing dispatch rule about worktrees: reading a branch needs no isolation and can be fanned out freely across sub-agents, but **writing** a branch (checkout / commit / push) needs an approval only the main interactive session can obtain — background sub-agents are auto-denied git writes. So Sleipnir carries reads out in parallel, while branch-mutating work stays in the main session, sequentially.",
      "diagram": "flowchart TD\n  TL[Team Lead] --> SL[Send Sleipnir<br/>to a branch]\n  SL --> WT[Isolated worktree<br/>per branch]\n  WT --> RD[Reads: fan out freely]\n  WT --> WR[Writes: main session only]\n  WT --> WID[Read-only<br/>Sleipnir's stables widget]\n  class TL,SL,WT,RD,WR,WID built",
      "diagram_mini": "flowchart LR\n  S[Sleipnir] --> W[isolated worktree] --> V[stables widget]\n  class S,W,V built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/sleipnir.svg",
      "svg_mini": "knowledge/concepts/visuals/sleipnir.mini.svg"
    },
    {
      "id": "brand-extraction",
      "title": "Brand extraction",
      "category": "Marketplace engineering",
      "kind": "ravenclaude-built",
      "order": 68,
      "summary": "A skill that turns a project's home page into a reusable brand kit — logos plus design tokens (colors, fonts, radii) — so generated reports come out on-brand. Role labels are heuristic and flagged.",
      "see_also": [
        "feedback-report"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The brand-extraction skill, the stdlib-only engine, the extracted-token set, or the heuristic-confidence honesty discipline changes.",
      "sources": [
        {
          "label": "brand-extraction skill",
          "url": "plugins/ravenclaude-core/skills/brand-extraction/SKILL.md"
        }
      ],
      "body_md": "**Brand extraction** is the skill that answers \"point at a project's website and make my generated reports match their brand.\" Given a home-page URL, it harvests **every logo variant** (favicon, apple-touch-icon, mask-icon, `og:image` / `twitter:image`, header and footer `<img>` logos, inline header `<svg>`, and light/dark `<picture>` variants) **and** the brand \"schema\" — design tokens: ranked colors with guessed roles, fonts with heading/body roles read from the heading selectors, a border-radius scale, and every color-valued CSS custom property. It emits a ready-to-apply kit: downloaded `logos/`, a schema-validated `brand.json`, a `brand.css` of `--brand-*` custom properties, a wired `report-template.html`, and a `brand-summary.md`.\n\nIt lives in `ravenclaude-core` because brand extraction works for *any* project's brand — it's domain-neutral, so by the house rule it stays in core rather than a vertical plugin. The engine is **stdlib-only Python** (no third-party installs, matching the no-new-deps discipline), and every network operation is **fail-safe**: a failed fetch or parse is recorded in `confidence_notes`, never a crash. On a bare page it returns zero logos/colors/fonts with honest notes; on a rich page it downloads all the variants it finds.\n\nThe honesty discipline is the point. The role labels — which color is \"primary,\" which font is \"heading\" — are **heuristic best-guesses**, marked as such per-item and in `confidence_notes`. The skill routes you to **WebFetch** (with the repo's webfetch-hardening sanitizer, honoring the web-access allow/deny list) as the reasoning layer to sanity-check the primary logo and color pick, and to fall back to when static extraction is defeated by CSS-in-JS or runtime-loaded assets. The script reports *what it actually found* and flags what it couldn't, instead of fabricating a confident palette.",
      "diagram": "flowchart TD\n  URL[Project home page] --> EX[extract_brand.py<br/>stdlib-only]\n  EX --> L[Logo variants → logos/]\n  EX --> T[Design tokens<br/>colors · fonts · radii]\n  T --> JSON[brand.json + brand.css]\n  L --> KIT[Brand kit]\n  JSON --> KIT\n  KIT --> WF[WebFetch sanity-check<br/>heuristic roles flagged]\n  class EX,L,T,JSON,KIT,WF built",
      "diagram_mini": "flowchart LR\n  U[home page] --> E[extract] --> K[brand kit]\n  class E,K built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/brand-extraction.svg",
      "svg_mini": "knowledge/concepts/visuals/brand-extraction.mini.svg"
    },
    {
      "id": "forge",
      "title": "FORGE — the gated planning pipeline",
      "category": "Planning & contribution",
      "kind": "ravenclaude-built",
      "order": 60,
      "summary": "The /forge pipeline turns a raw idea into a fact-grounded, two-panel-reviewed, critic-checked, tiebroken, red-teamed, routed plan — with depth-scaled gates so cheap ideas stay cheap.",
      "see_also": [
        "command-review-tribunal",
        "wrap-and-scenarios"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The FORGE gate set, the depth ladder, or the routing/exit logic changes.",
      "sources": [
        {
          "label": "forge-pipeline skill",
          "url": "plugins/ravenclaude-core/skills/forge-pipeline/SKILL.md"
        },
        {
          "label": "/forge command",
          "url": "plugins/ravenclaude-core/commands/forge.md"
        }
      ],
      "body_md": "**FORGE** is RavenClaude's gated planning pipeline — what `/forge` runs. It formalizes the pattern the maintainer runs by hand: *clarify → research + verify → two divergent panels on different models → critic → gap-analysis → per-conflict expert tiebreak → red-team → synthesize → route → exit.* Each gate is **fail-closed** (no advance without an explicit pass or a recorded waiver) and emits a typed artifact into the run directory, so the whole plan-building process is auditable after the fact.\n\nThe pipeline scales with **depth** rather than running a fixed set of gates: `micro` runs only scope + synthesize + route, `quick` (the default) adds research and the two panels, `standard` adds the critic, tiebreak, and red-team, and `deep` removes the conflict cap and adds checkpoint/resume. Two ideas make FORGE more than a copy of Claude Code's dynamic-workflows deep-plan loop: it runs the two review panels on **different models** (cross-model divergence catches blind spots a same-model critic shares), and it adds a **fact-verification gate** that blocks on any load-bearing claim about anything outside the repo unless it carries a this-session source or an explicit `[unverified]` marker. A correlated-error **critic** then hunts for places the two panels *agree on something wrong* — the failure a disagreement-keyed gap-analysis structurally can't see.\n\nThe final gate routes the plan **deterministically** (no model judgment): a script decides whether to execute locally or hand off to Ultraplan in the cloud, and whether the plan lands on `main` or via a draft PR. FORGE raises the floor on plan quality and shifts the odds against a confidently-wrong plan — it does **not** guarantee correctness; the critic, red-team, and tiebreak reduce, not eliminate, the residual risk.",
      "diagram": "flowchart TD\n  I[Raw idea] --> G0[Scope / clarify]\n  G0 --> G1[Research + verify facts]\n  G1 --> P[Two panels<br/>different models]\n  P --> C[Critic + tiebreak + red-team]\n  C --> S[Synthesize plan]\n  S --> R{Route}\n  R -- local --> EX[ExitPlanMode]\n  R -- cloud --> UP[Ultraplan handoff]\n  class G0,G1,P,C,S,R,EX,UP built",
      "diagram_mini": "flowchart LR\n  I[Idea] --> G[Gated review] --> R[Routed plan]\n  class G,R built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/forge.svg",
      "svg_mini": "knowledge/concepts/visuals/forge.mini.svg"
    },
    {
      "id": "wrap-and-scenarios",
      "title": "/wrap and the scenarios bank",
      "category": "Planning & contribution",
      "kind": "ravenclaude-built",
      "order": 61,
      "summary": "/wrap captures a lesson as a 9-field scenario file; scenario-retrieval later surfaces it as a flagged-unverified fallback, and a finding is promoted to canonical only when ≥2 scenarios corroborate it.",
      "see_also": [
        "feedback-report",
        "run-context-bundle",
        "external-contribution-intake"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The scenario 9-field schema, the unverified-preamble rule, or the corroboration/promotion threshold changes.",
      "sources": [
        {
          "label": "/wrap command",
          "url": "plugins/ravenclaude-core/commands/wrap.md"
        },
        {
          "label": "scenario-retrieval skill",
          "url": "plugins/ravenclaude-core/skills/scenario-retrieval/SKILL.md"
        }
      ],
      "body_md": "**`/wrap`** is the end-of-engagement step that turns a hard-won lesson into a re-readable file. It writes a **scenario** — a dated, scope-tagged narrative of *\"we hit problem X, the context was Y, we tried A/B/C, D worked\"* — into `plugins/<plugin>/scenarios/<date>-<slug>.md` with a **9-field YAML schema** (plugin, product, version, scope, tags, confidence, and so on). The flow drafts from the session transcript so the user isn't asked to \"tell me about your engagement,\" asks only the four minimum questions, scrubs the draft for client-identifying info and secrets before writing, and confirms before committing. A scenario is deliberately **not** a canonical best-practice — it's raw field-note material, written `reviewed: false`.\n\nLater, the **`scenario-retrieval`** skill surfaces these files. Before answering a plugin-domain question, an agent globs the scenarios directory, filters by tags / product / scope, ranks by recency and confidence, and surfaces at most the top 2-3 matches — always behind a **mandatory unverified-scenario preamble** (*\"Based on N unverified scenarios from YYYY-MM tagged [scope] — verify in your environment\"*). The preamble is non-negotiable: scenarios aren't reviewed, so a single contributor's mis-diagnosis must never silently read as canonical. Retrieval is plain file-system glob plus frontmatter parsing — no vector index — and treats scenarios as strictly **secondary** to the curated knowledge files.\n\nThe two banks are connected by a corroboration rule: when **≥2 independent scenarios** corroborate the same finding (from different contribution quarters), an agent eventually proposes **promotion** to the canonical `docs/best-practices/` bank, which carries human review. Until then a finding stays an unverified prior — surfaced, weighted by how often it has proven relevant, but never trusted on repetition alone.",
      "diagram": "flowchart TD\n  E[Engagement<br/>lesson learned] --> W[\"/wrap captures it\"]\n  W --> F[(9-field scenario file<br/>reviewed: false)]\n  F --> R[scenario-retrieval<br/>surfaces with<br/>unverified preamble]\n  F --> P{≥2 corroborate?}\n  P -- yes --> C[Propose promotion<br/>to best-practices]\n  P -- no --> R\n  class W,F,R,P,C built",
      "diagram_mini": "flowchart LR\n  W[\"/wrap\"] --> F[(scenario)] --> R[surfaced unverified]\n  class W,F,R built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/wrap-and-scenarios.svg",
      "svg_mini": "knowledge/concepts/visuals/wrap-and-scenarios.mini.svg"
    },
    {
      "id": "feedback-report",
      "title": "Problems & Resolutions report",
      "category": "Planning & contribution",
      "kind": "ravenclaude-built",
      "order": 62,
      "summary": "A self-contained HTML view over every scenario's problem→resolution, with an organic-vs-seed filter that separates the handful of real-engagement problems from the synthetic bulk-authored starter set.",
      "see_also": [
        "wrap-and-scenarios",
        "run-context-bundle"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The feedback-report generator, the seed-vs-organic classification, or the scenario corpus shape changes.",
      "sources": [
        {
          "label": "generate-feedback-report.py",
          "url": "scripts/generate-feedback-report.py"
        }
      ],
      "body_md": "The **Problems & Resolutions** view (`feedback-report.html`) answers a plain question: *\"What problems are people hitting, and how are they resolving them?\"* The scenario corpus already holds exactly that — each scenario file is a problem → context → tried → resolution field note. This generator **reads** the corpus and writes nothing back, emitting one self-contained, offline HTML page: a filterable table with one row per scenario (problem, resolution, plugin, scope, date) plus a top summary of KPIs and a per-plugin breakdown.\n\nThe load-bearing feature is the **organic-vs-seed** distinction. Most of the corpus is a bulk *seed* drop — synthetic starter scenarios authored en masse over a couple of days — while only a handful are *organic* contributions from real engagements. The seed inflates the raw count and hides the genuine user problems, so the view classifies each scenario by whether its contribution date is a **bulk-drop date** (an unusually large number of scenarios sharing one date, detected dynamically from the data). Everything else is organic. This is deterministic and self-adjusting: when real organic contributions accumulate on ordinary days, they're never mis-tagged as seed. An **Origin** filter (All / Organic / Seed) lets you collapse the table down to just the real-engagement problems.\n\nThe report is built with the same discipline as the rest of the dashboard surface: no CDN, inline CSS/JS, server-side-rendered table with client-side filter/search/sort, shared design tokens, and a derived (not wall-clock) \"as of\" date so the file is byte-stable between runs. By privacy construction it renders **only** fields that already ship in the committed scenario files — it adds no environment, tenant, auth, or role data.",
      "diagram": "flowchart TD\n  S[(scenarios/*.md<br/>problem + resolution)] --> G[generate-feedback-report.py<br/>read-only]\n  G --> C{Bulk-drop date?}\n  C -- yes --> SEED[Seed example]\n  C -- no --> ORG[Organic problem]\n  SEED --> V[feedback-report.html<br/>filterable table]\n  ORG --> V\n  class G,C,SEED,ORG,V built",
      "diagram_mini": "flowchart LR\n  S[(scenarios)] --> G[report] --> V[organic vs seed]\n  class G,V built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/feedback-report.svg",
      "svg_mini": "knowledge/concepts/visuals/feedback-report.mini.svg"
    },
    {
      "id": "run-context-bundle",
      "title": "The safe run-context bundle",
      "category": "Planning & contribution",
      "kind": "ravenclaude-built",
      "order": 63,
      "summary": "A minimal machine-captured bundle attached to a scenario — model, plugin versions, a derived posture label — from a fixed allowlist that structurally never captures an environment identifier.",
      "see_also": [
        "wrap-and-scenarios",
        "external-contribution-intake"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The safe-field allowlist, the posture-label derivation, or the never-capture privacy rule changes.",
      "sources": [
        {
          "label": "capture-run-context.py",
          "url": "plugins/ravenclaude-core/scripts/capture-run-context.py"
        }
      ],
      "body_md": "When `/wrap` writes a scenario, it can attach a **minimal, safe run-context bundle** so a future reader knows the few output-shaping variables that decide whether the lesson generalizes — instead of relying on the contributor's cold scope guess. The bundle holds exactly three things plus a status flag: the contributing session's **model**, the **plugin versions** in play, and a **derived posture label** (open / default / balanced / strict / unknown). The label is a *derivation* of the posture's global default, not the raw posture YAML — a label is not an environment name. A `capture_method` flag records whether any source was absent (`degraded`) versus a clean capture (`auto`).\n\nThe load-bearing constraint is **privacy by construction (R-PRIV)**. Scenario files ship to *every* installer, and an environment name — a tenant slug, a service-principal name, an auth-mechanism label — is itself a sensitive token once shipped. Banning such names by regex is unenforceable, because a scrubber catches secret *shapes*, not arbitrary slugs like `client-acme-prod`. So the bundler doesn't redact environment context — it **structurally never captures it**. A fixed `SAFE_FIELDS` allowlist is the entire universe of fields it can emit, and there is simply no code path that reads `environment-context.md` or any env / role / tenant / auth source. \"Never-capture\" beats \"detect-and-ban\" because the bundler is the only writer, and an audit gate asserts the allowlist contains zero environment fields.\n\nThe capture is fail-safe and stdlib-only: deterministic, no network, no subprocess, no dynamic import. Any missing source degrades gracefully — that field is omitted and `capture_method` flips to `degraded`; the script never raises on an absent or unreadable source. The result is a tiny, shippable provenance block that helps `scenario-retrieval` decide whether a lesson applies, without ever leaking what environment produced it.",
      "diagram": "flowchart TD\n  W[\"/wrap\"] --> B[capture-run-context.py]\n  B --> A[Fixed SAFE allowlist]\n  A --> M[model]\n  A --> P[plugin_versions]\n  A --> L[posture_label<br/>derived, not raw]\n  E[env / tenant / role / auth] -. never captured .-> X((blocked by<br/>construction))\n  M --> O[run_context block<br/>in scenario]\n  P --> O\n  L --> O\n  class B,A,M,P,L,O built\n  class X fact",
      "diagram_mini": "flowchart LR\n  B[bundle] --> S[model · versions · label]\n  E[env names] -. never .-> X((blocked))\n  class B,S built\n  class X fact",
      "steps": [],
      "svg": "knowledge/concepts/visuals/run-context-bundle.svg",
      "svg_mini": "knowledge/concepts/visuals/run-context-bundle.mini.svg"
    },
    {
      "id": "external-contribution-intake",
      "title": "External contribution intake",
      "category": "Planning & contribution",
      "kind": "ravenclaude-built",
      "order": 64,
      "summary": "A GitHub Issue Form lets a consumer without repo access submit a scenario; an Actions workflow deterministically scrubs secrets/PII and opens a review PR — never pushing main, no LLM over the content.",
      "see_also": [
        "wrap-and-scenarios",
        "run-context-bundle"
      ],
      "widget": null,
      "try_it": null,
      "node_links": {},
      "last_verified": "2026-06-08",
      "refresh_when": "The quarantine workflow, the deterministic scrub/strip processor, or the PR-not-push intake invariants change.",
      "sources": [
        {
          "label": "quarantine-intake workflow",
          "url": ".github/workflows/quarantine-intake.yml"
        },
        {
          "label": "process-scenario-submission.py",
          "url": "scripts/process-scenario-submission.py"
        }
      ],
      "body_md": "`/wrap` assumes a contributor can place a file in the repo — which closes out every external consumer **without push access**, the measured bottleneck on real-engagement contributions. The external-contribution intake closes that gap at the repo edge. A consumer files a typed **GitHub Issue Form** (title, plugin dropdown, product, problem/resolution textareas, a scope guess, and a required DCO checkbox attesting the work is theirs with no client-identifying information). Labeling the issue fires the **quarantine workflow**, which turns the submission into a reviewable file without ever giving the consumer write access to the repo.\n\nThe workflow is **security-sensitive** because it processes untrusted input inside GitHub Actions, so it holds a few hard invariants. The issue title and body are **untrusted data** — they never get interpolated into a shell `run:` line; they reach the processor only via environment variables, and only the trusted issue *number* is interpolated elsewhere. The processor (`process-scenario-submission.py`) is **deterministic and model-free** — pure regex reusing the marketplace's secret patterns and injection-strip patterns — so **no LLM ever processes the untrusted content** at intake. A secret or PII shape closes the issue \"not staged\" with no file written; otherwise the body is injection-stripped, capped, and normalized into a staged file under `docs/staging/incoming/external/`.\n\nTwo more rules govern the output. **PR-not-push (R-PR):** the workflow **opens a pull request**, never commits to `main` — branch protection rejects bot pushes, and every staged file arrives as a human-reviewable PR that drains through the existing maintainer review (the second, LLM-backed human gate, downstream of the regex clean). A **spam cap** closes new submissions with \"queue full\" once too many external submissions are already open. The result: anyone can contribute a field note safely, and nothing untrusted reaches a canonical location without two gates in between.",
      "diagram": "flowchart TD\n  C[Consumer<br/>no repo access] --> IF[GitHub Issue Form<br/>+ DCO checkbox]\n  IF --> WF[Actions: quarantine workflow]\n  WF --> SC[Deterministic scrub<br/>+ injection-strip<br/>no LLM]\n  SC --> D{Secret / PII?}\n  D -- yes --> RJ[Close: not staged]\n  D -- no --> PR[Open review PR<br/>never push main]\n  PR --> RV[Maintainer review gate]\n  class IF,WF,SC,D,RJ,PR,RV built",
      "diagram_mini": "flowchart LR\n  IF[Issue Form] --> SC[scrub + strip] --> PR[review PR]\n  class IF,SC,PR built",
      "steps": [],
      "svg": "knowledge/concepts/visuals/external-contribution-intake.svg",
      "svg_mini": "knowledge/concepts/visuals/external-contribution-intake.mini.svg"
    }
  ]
}
